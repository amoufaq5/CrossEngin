# @crossengin/types

Shared TypeScript types for the CrossEngin monorepo.

Holds branded ID types, common error shapes, and structural types
that cross package boundaries. Anything domain-specific (entity
schemas, manifest types) lives in its owning package.

## Status

Skeleton. Branded IDs landed first; remaining types added as
kernel + auth + manifest engine require them.
