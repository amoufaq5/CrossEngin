# CrossEngin manifest — starter template
# Format defined by ADR-0004 (Manifest specification).
# This file is intentionally minimal; populate sections as the manifest
# spec stabilizes in Phase 3.

manifest:
  id: starter-template
  name: Starter Template
  version: 0.0.0
  description: Template for new CrossEngin manifests.
  # compliance_pack: <id>           # per ADR-0012

# entities:
#   - name: Example
#     fields:
#       - { name: id, type: id, primary: true }
#       - { name: name, type: string, required: true }

# relations:
#   - {}

# workflows:
#   - {}

# roles:
#   - {}

# views:
#   - {}

# integrations:
#   - {}
