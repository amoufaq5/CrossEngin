# ADR index

| # | Title | Tier | Status | Date |
|---|---|---|---|---|
| [0000](0000-template.md) | _Template_ | — | — | — |
| [0001](0001-platform-positioning-and-brand-architecture.md) | Platform positioning and brand architecture | 1 | Proposed | 2026-05-11 |
| [0002](0002-multi-tenancy-model.md) | Multi-tenancy model | 1 | Proposed | 2026-05-11 |
| [0003](0003-meta-schema-and-dynamic-entity-engine.md) | Meta-schema and dynamic entity engine | 1 | Proposed | 2026-05-11 |
| [0004](0004-manifest-specification.md) | Manifest specification | 1 | Proposed | 2026-05-11 |
| [0005](0005-ai-architect-contract.md) | AI Architect contract | 1 | Proposed | 2026-05-11 |
| [0006](0006-llm-provider-router.md) | LLM provider router | 2 | Proposed | 2026-05-11 |
| [0007](0007-workflow-engine.md) | Workflow engine | 2 | Proposed | 2026-05-11 |
| [0008](0008-rbac-abac-and-audit.md) | RBAC v2, ABAC, and audit | 1 | Proposed | 2026-05-11 |
| [0009](0009-security-model.md) | Security model | 1 | Proposed | 2026-05-11 |
| [0010](0010-multi-region-and-data-residency.md) | Multi-region and data residency | 2 | Proposed | 2026-05-11 |
| [0011](0011-integration-mesh.md) | Integration mesh | 2 | Proposed | 2026-05-11 |
| [0012](0012-compliance-pack-architecture.md) | Compliance pack architecture | 2 | Proposed | 2026-05-11 |
| [0013](0013-reporting-and-analytics.md) | Reporting and analytics | 2 | Proposed | 2026-05-11 |
| [0014](0014-files-and-storage.md) | Files and storage | 2 | Proposed | 2026-05-11 |
| [0015](0015-jobs-and-async-runtime.md) | Jobs and async runtime | 2 | Proposed | 2026-05-11 |
| [0016](0016-search.md) | Search | 2 | Proposed | 2026-05-11 |
| [0017](0017-observability-and-slos.md) | Observability and SLOs | 2 | Proposed | 2026-05-11 |
| [0018](0018-frontend-renderer-architecture.md) | Frontend renderer architecture | 1 | Proposed | 2026-05-11 |
| [0019](0019-pwa-and-capacitor-mobile.md) | PWA and Capacitor mobile | 3 | Proposed | 2026-05-11 |
| [0020](0020-build-packaging-and-deployment.md) | Build, packaging, and deployment | 2 | Proposed | 2026-05-11 |
| [0021](0021-billing-and-metering.md) | Billing and metering | 3 | Proposed | 2026-05-11 |
| [0022](0022-internationalization-and-localization.md) | Internationalization and localization | 3 | Proposed | 2026-05-11 |
| [0023](0023-testing-strategy.md) | Testing strategy | 2 | Proposed | 2026-05-11 |
| [0024](0024-repository-and-migration-strategy.md) | Repository and migration strategy | 1 | Proposed | 2026-05-11 |
| [0025](0025-ai-architect-safety-and-governance.md) | AI Architect safety and governance | 2 | Proposed | 2026-05-11 |
| [0026](0026-marketplace-and-extensions.md) | Marketplace and extensions | 3 | Proposed | 2026-05-15 |
| [0027](0027-developer-sdk.md) | Developer SDK | 2 | Proposed | 2026-05-15 |
| [0028](0028-migration-and-onboarding.md) | Migration and onboarding | 2 | Proposed | 2026-05-15 |
| [0029](0029-ml-training-and-feedback.md) | ML training and feedback | 3 | Proposed | 2026-05-15 |
| [0030](0030-edge-and-latency-slo.md) | Edge and latency SLO | 2 | Proposed | 2026-05-15 |
| [0031](0031-disaster-recovery.md) | Disaster recovery | 2 | Proposed | 2026-05-15 |
| [0032](0032-multi-region-active-active.md) | Multi-region active-active | 3 | Proposed | 2026-05-15 |
| [0033](0033-cost-attribution-and-finops.md) | Cost attribution and FinOps | 3 | Proposed | 2026-05-15 |
| [0034](0034-sdk-client-libraries.md) | SDK client libraries | 2 | Proposed | 2026-05-15 |
| [0035](0035-audit-and-forensics.md) | Audit and forensics | 3 | Proposed | 2026-05-15 |
| [0036](0036-tenant-lifecycle.md) | Tenant lifecycle | 2 | Proposed | 2026-05-15 |
| [0037](0037-incident-response.md) | Incident response | 2 | Proposed | 2026-05-15 |
| [0038](0038-sso-and-federated-identity.md) | SSO and federated identity | 2 | Proposed | 2026-05-16 |
| [0039](0039-notifications-and-delivery.md) | Notifications and delivery | 2 | Proposed | 2026-05-16 |
| [0040](0040-access-reviews-and-attestations.md) | Access reviews and periodic attestations | 2 | Proposed | 2026-05-16 |
| [0041](0041-workflow-engine-runtime.md) | Workflow engine runtime contracts | 2 | Proposed | 2026-05-16 |
| [0042](0042-data-lineage-and-provenance.md) | Data lineage and provenance | 2 | Proposed | 2026-05-16 |
| [0043](0043-rate-limiting-and-quotas.md) | Rate limiting and quotas | 2 | Proposed | 2026-05-16 |
| [0044](0044-api-gateway-request-lifecycle.md) | API gateway request lifecycle | 2 | Proposed | 2026-05-16 |
| [0045](0045-feature-flags-runtime.md) | Feature flags runtime | 2 | Proposed | 2026-05-16 |
| [0046](0046-phase-2-implementation-plan.md) | Phase 2 implementation plan | 1 | Proposed | 2026-05-16 |
| [0047](0047-kernel-ddl-execution.md) | Kernel DDL execution (Phase 2 M1) | 2 | Proposed | 2026-05-16 |
| [0048](0048-real-cryptography.md) | Real cryptography (Phase 2 M2) | 2 | Proposed | 2026-05-16 |
| [0049](0049-workflow-engine-runtime.md) | Workflow engine runtime (Phase 2 M3) | 2 | Proposed | 2026-05-16 |
| [0050](0050-api-gateway-runtime.md) | API gateway runtime (Phase 2 M4) | 2 | Proposed | 2026-05-16 |
| [0051](0051-architect-cli.md) | Architect CLI (Phase 2 M5) | 2 | Proposed | 2026-05-16 |
| [0052](0052-workflow-signal-bridge.md) | Workflow signal bridge (Phase 2 M6) | 2 | Proposed | 2026-05-16 |
| [0053](0053-anthropic-provider-client.md) | Real Anthropic provider client (Phase 2 M2.7) | 2 | Proposed | 2026-05-17 |
| [0054](0054-architect-cli-chat-mode.md) | Architect CLI chat mode (Phase 2 M5.5) | 2 | Proposed | 2026-05-17 |
| [0055](0055-architect-cli-tool-use.md) | Architect CLI tool-driven chat (Phase 2 M5.6) | 2 | Proposed | 2026-05-17 |
| [0056](0056-architect-cli-write-tools.md) | Architect CLI write tools (Phase 2 M5.8) | 2 | Proposed | 2026-05-17 |
| [0057](0057-chat-persistence.md) | Chat persistence to META_ARCHITECT_* (Phase 2 M5.7) | 2 | Proposed | 2026-05-17 |
| [0058](0058-erp-core-pack.md) | First vertical pack — ERP Core (Phase 2 M7) | 2 | Proposed | 2026-05-17 |
| [0059](0059-ai-router.md) | AI provider router (Phase 2 M6.5) | 2 | Proposed | 2026-05-17 |
| [0060](0060-openai-provider-client.md) | Real OpenAI provider client (Phase 2 M2.8) | 2 | Proposed | 2026-05-17 |
| [0061](0061-architect-cli-router-integration.md) | Architect CLI router integration (Phase 2 M6.5.5) | 2 | Proposed | 2026-05-17 |
| [0062](0062-openai-responses-api.md) | OpenAI Responses API support (Phase 2 M2.8.5) | 2 | Proposed | 2026-05-17 |
| [0063](0063-cli-pack-apply.md) | CLI `--pack` apply (Phase 2 M7-wire) | 2 | Proposed | 2026-05-17 |
| [0064](0064-pack-tenant-scoping.md) | Pack tenant scoping via `tenant_owned` (Phase 2 M7.7) | 2 | Proposed | 2026-05-17 |
| [0065](0065-erp-payments-pack.md) | Second vertical pack — ERP Payments (Phase 2 M7.5) | 2 | Proposed | 2026-05-17 |
| [0066](0066-payment-signal-bridge-wiring.md) | Payment signal-bridge wiring (Phase 2 M7.8) | 2 | Proposed | 2026-05-17 |
| [0067](0067-cli-sessions-subcommands.md) | CLI `sessions` subcommands (Phase 2 M5.9) | 2 | Proposed | 2026-05-17 |
| [0068](0068-cli-extends-resolver-wiring.md) | Kernel `extends` resolver wiring (Phase 2 M7.6.5) | 2 | Proposed | 2026-05-18 |
| [0069](0069-cli-gateway-binding.md) | CLI `gateway start` binding (Phase 2 M4.7) | 2 | Proposed | 2026-05-18 |
| [0070](0070-erp-healthcare-pack.md) | Third vertical pack — ERP Healthcare (Phase 2 M7.9) | 2 | Proposed | 2026-05-18 |
| [0071](0071-bedrock-provider-client.md) | AWS Bedrock LLM provider (Phase 2 M2.9) | 2 | Proposed | 2026-05-18 |
| [0072](0072-bedrock-titan-embeddings.md) | Bedrock Titan + Cohere embeddings (Phase 2 M2.9.5) | 2 | Proposed | 2026-05-18 |
| [0073](0073-architect-cli-bedrock-integration.md) | Architect CLI Bedrock integration (Phase 2 M6.5.6) | 2 | Proposed | 2026-05-18 |
| [0074](0074-gateway-jwt-and-routes.md) | Gateway JWT auth + routes subcommand (Phase 2 M4.7.5) | 2 | Proposed | 2026-05-18 |
| [0075](0075-gateway-jwks-url-and-reload.md) | URL-fetched JWKS + hot-reload (Phase 2 M4.7.6) | 2 | Proposed | 2026-05-18 |
| [0076](0076-bedrock-cache-control-and-parallelism.md) | Bedrock cacheControl + Titan parallelism (Phase 2 M2.9.6) | 2 | Proposed | 2026-05-18 |
| [0077](0077-bedrock-multimodal-embeddings.md) | Bedrock multimodal embeddings + image content blocks (Phase 2 M2.9.7) | 2 | Proposed | 2026-05-18 |
| [0078](0078-llm-message-attachments.md) | Kernel LlmMessage attachments + vision capability (Phase 2 M2.X) | 2 | Proposed | 2026-05-18 |
| [0079](0079-gateway-routes-from-manifest.md) | Gateway routes from pack manifest (Phase 2 M4.8) | 2 | Proposed | 2026-05-18 |
| [0080](0080-gateway-unregister-pack.md) | Gateway routes unregister-pack (Phase 2 M4.8.x) | 2 | Proposed | 2026-05-18 |
| [0081](0081-gateway-routes-sync-pack.md) | Gateway routes sync-pack (Phase 2 M4.8.y) | 2 | Proposed | 2026-05-18 |
| [0082](0082-routes-source-pack-column.md) | routes.source_pack column (Phase 2 M4.10) | 2 | Proposed | 2026-05-19 |
| [0083](0083-unregister-pack-by-source-pack.md) | unregister-pack --by-source-pack (Phase 2 M4.10.x) | 2 | Proposed | 2026-05-19 |
| [0084](0084-bedrock-guardrails.md) | Bedrock Guardrails integration (Phase 2 M2.9.8) | 2 | Proposed | 2026-05-19 |
| [0085](0085-bedrock-per-request-guardrail-override.md) | Bedrock per-request guardrail override (Phase 2 M2.9.8.x) | 2 | Proposed | 2026-05-19 |
| [0086](0086-openai-anthropic-moderation.md) | OpenAI + Anthropic moderation surfaces (Phase 2 M2.X.6) | 2 | Proposed | 2026-05-19 |
| [0087](0087-cross-provider-moderation-helper.md) | Cross-provider moderation helper (Phase 2 M2.X.6.x) | 2 | Proposed | 2026-05-19 |
| [0088](0088-llm-content-discriminated-union.md) | Kernel LlmMessage.content as discriminated union (Phase 2 M2.X.5) | 2 | Proposed | 2026-05-19 |
| [0089](0089-tool-content-blocks.md) | tool_use + tool_result content block variants (Phase 2 M2.X.5.x) | 2 | Proposed | 2026-05-19 |
| [0090](0090-cross-provider-retryable-helper.md) | Cross-provider retryable helper (Phase 2 M2.X.7) | 2 | Proposed | 2026-05-19 |
| [0091](0091-router-uses-kernel-helpers.md) | Router uses kernel cross-provider helpers (Phase 2 M6.6) | 2 | Proposed | 2026-05-19 |
| [0092](0092-openai-moderations-api.md) | Standalone OpenAI Moderations API (Phase 2 M2.X.8) | 2 | Proposed | 2026-05-19 |
| [0093](0093-openai-responses-api-image-inputs.md) | OpenAI Responses API image inputs (Phase 2 M2.8.6) | 2 | Proposed | 2026-05-19 |
| [0094](0094-image-url-content-block.md) | ImageUrlContentBlock — URL-based image inputs (Phase 2 M2.X.5.y) | 2 | Proposed | 2026-05-19 |
| [0095](0095-cross-provider-input-too-large-helper.md) | Cross-provider input-too-large helper (Phase 2 M2.X.9) | 2 | Proposed | 2026-05-19 |
| [0096](0096-anthropic-url-source.md) | Anthropic URL-source image support (Phase 2 M2.X.5.z) | 2 | Proposed | 2026-05-19 |
| [0097](0097-document-content-block.md) | DocumentContentBlock — PDF inputs (Phase 2 M2.X.5.aa) | 2 | Proposed | 2026-05-19 |
| [0098](0098-document-url-content-block.md) | DocumentUrlContentBlock — URL-based document inputs (Phase 2 M2.X.5.aa.y) | 2 | Proposed | 2026-05-19 |
| [0099](0099-document-format-expansion.md) | Document format expansion — txt + md + csv (Phase 2 M2.X.5.aa.x) | 2 | Proposed | 2026-05-19 |
| [0100](0100-office-document-format-expansion.md) | Office document format expansion — doc/docx/xls/xlsx/html (Phase 2 M2.X.5.aa.x.1) | 2 | Proposed | 2026-05-19 |
| [0101](0101-llm-message-name-threading.md) | Kernel LlmMessage.name enforcement + OpenAI threading (Phase 2 M2.X.10) | 2 | Proposed | 2026-05-19 |
| [0102](0102-openai-files-api.md) | OpenAI Files API integration + FileReferenceContentBlock (Phase 2 M2.X.5.aa.z) | 2 | Proposed | 2026-05-19 |
| [0103](0103-anthropic-files-api.md) | Anthropic Files API integration (Phase 2 M2.X.5.aa.z.1) | 2 | Proposed | 2026-05-19 |
| [0104](0104-files-api-listfiles.md) | Files API listFiles() across OpenAI + Anthropic (Phase 2 M2.X.5.aa.z.2) | 2 | Proposed | 2026-05-19 |
| [0105](0105-bedrock-batch-listbatches.md) | Bedrock batch inference listBatches (Phase 2 M2.X.5.aa.z.3) | 2 | Proposed | 2026-05-19 |
| [0106](0106-bedrock-batch-getbatch.md) | Bedrock batch inference getBatch (Phase 2 M2.X.5.aa.z.4) | 2 | Proposed | 2026-05-19 |
| [0107](0107-bedrock-batch-stopbatch.md) | Bedrock batch inference stopBatch (Phase 2 M2.X.5.aa.z.5) | 2 | Proposed | 2026-05-19 |
| [0108](0108-bedrock-batch-createbatch.md) | Bedrock batch inference createBatch (Phase 2 M2.X.5.aa.z.6) | 2 | Proposed | 2026-05-19 |
| [0109](0109-bedrock-list-guardrails.md) | Bedrock listGuardrails — second control-plane enumeration surface (Phase 2 M2.X.5.aa.z.7) | 2 | Proposed | 2026-05-19 |
| [0110](0110-bedrock-get-guardrail.md) | Bedrock getGuardrail with policy detail (Phase 2 M2.X.5.aa.z.8) | 2 | Proposed | 2026-05-19 |
| [0111](0111-bedrock-list-inference-profiles.md) | Bedrock listInferenceProfiles — third control-plane enumeration (Phase 2 M2.X.5.aa.z.9) | 2 | Proposed | 2026-05-19 |
| [0112](0112-bedrock-get-inference-profile.md) | Bedrock getInferenceProfile (Phase 2 M2.X.5.aa.z.10) | 2 | Proposed | 2026-05-19 |
| [0113](0113-bedrock-list-imported-models.md) | Bedrock listImportedModels — fourth control-plane enumeration (Phase 2 M2.X.5.aa.z.11) | 2 | Proposed | 2026-05-19 |
| [0114](0114-bedrock-get-imported-model.md) | Bedrock getImportedModel with data-source provenance (Phase 2 M2.X.5.aa.z.12) | 2 | Proposed | 2026-05-19 |
| [0115](0115-bedrock-list-custom-models.md) | Bedrock listCustomModels — fifth control-plane enumeration (Phase 2 M2.X.5.aa.z.13) | 2 | Proposed | 2026-05-19 |
| [0116](0116-bedrock-get-custom-model.md) | Bedrock getCustomModel with training/validation detail (Phase 2 M2.X.5.aa.z.14) | 2 | Proposed | 2026-05-19 |
| [0117](0117-bedrock-list-model-import-jobs.md) | Bedrock listModelImportJobs — sixth control-plane enumeration (Phase 2 M2.X.5.aa.z.15) | 2 | Proposed | 2026-05-19 |
| [0118](0118-conflict-error-classifier.md) | conflict_error kernel kind + isConflictError cross-provider classifier (Phase 2 M2.X.12) | 2 | Proposed | 2026-05-19 |
| [0119](0119-chat-substrate-content-blocks.md) | Chat REPL widens user input to LlmContentBlock[] (Phase 2 M5.10.5) | 2 | Proposed | 2026-05-19 |
| [0120](0120-workflow-instrumentation-hooks.md) | Workflow runtime instrumentation hooks + META_WORKFLOW_TRACES (Phase 2 M8) | 2 | Proposed | 2026-05-19 |
| [0121](0121-bedrock-get-model-import-job.md) | Bedrock getModelImportJob with import-job detail (Phase 2 M2.X.5.aa.z.16) | 2 | Proposed | 2026-05-19 |
| [0122](0122-bedrock-list-model-customization-jobs.md) | Bedrock listModelCustomizationJobs — seventh control-plane enumeration (Phase 2 M2.X.5.aa.z.17) | 2 | Proposed | 2026-05-19 |
| [0123](0123-bedrock-get-model-customization-job.md) | Bedrock getModelCustomizationJob with training/validation detail (Phase 2 M2.X.5.aa.z.18) | 2 | Proposed | 2026-05-19 |
| [0124](0124-bedrock-stop-model-customization-job.md) | Bedrock stopModelCustomizationJob (Phase 2 M2.X.5.aa.z.19) | 2 | Proposed | 2026-05-19 |
| [0125](0125-cache-breakpoint-content-block.md) | cacheBreakpoint field on LlmContentBlock + Anthropic prompt caching (Phase 2 M2.X.11) | 2 | Proposed | 2026-05-19 |
| [0126](0126-bedrock-cache-point-translator.md) | Bedrock cachePoint translator for cacheBreakpoint (Phase 2 M2.X.11.x) | 2 | Proposed | 2026-05-19 |
| [0127](0127-not-found-error-classifier.md) | not_found_error kernel kind + isNotFoundError cross-provider classifier (Phase 2 M2.X.13) | 2 | Proposed | 2026-05-19 |
| [0128](0128-authentication-error-classifier.md) | authentication_error kernel kind + isAuthenticationError cross-provider classifier (Phase 2 M2.X.14) | 2 | Proposed | 2026-05-19 |
| [0129](0129-permission-error-classifier.md) | permission_error kernel kind + isPermissionError cross-provider classifier (Phase 2 M2.X.15) | 2 | Proposed | 2026-05-19 |
| [0130](0130-invalid-request-error-classifier.md) | invalid_request_error kernel kind + isInvalidRequestError cross-provider classifier (Phase 2 M2.X.16) | 2 | Proposed | 2026-05-19 |
| [0131](0131-bedrock-create-model-customization-job.md) | Bedrock createModelCustomizationJob — programmatic fine-tunes (Phase 2 M2.X.5.aa.z.20) | 2 | Proposed | 2026-05-19 |
| [0132](0132-workflow-activity-instrumentation.md) | Workflow runtime activity execution instrumentation (Phase 2 M8.1) | 2 | Proposed | 2026-05-19 |
| [0133](0133-router-conflict-short-circuit.md) | ai-router special-cases isConflictError for retry chain short-circuit (Phase 2 M6.6.x) | 2 | Proposed | 2026-05-19 |
| [0134](0134-router-not-found-short-circuit.md) | ai-router special-cases isNotFoundError for retry chain short-circuit (Phase 2 M6.6.y) | 2 | Proposed | 2026-05-20 |
| [0135](0135-postgres-cost-tracker.md) | PostgresCostTracker — first persisted ai-router cost accumulator (Phase 2 M6.7) | 2 | Proposed | 2026-05-20 |
| [0136](0136-bedrock-delete-surfaces.md) | Bedrock DELETE control-plane surfaces — first three deletes (Phase 2 M2.X.5.aa.z.21) | 2 | Proposed | 2026-05-20 |
| [0137](0137-per-tenant-cost-ceiling.md) | Per-tenant cost ceiling — META_LLM_COST_CEILINGS + PostgresCostCeilingResolver (Phase 2 M6.7.x) | 2 | Proposed | 2026-05-20 |
| [0138](0138-bedrock-delete-inference-profile.md) | Bedrock deleteInferenceProfile with system-profile guard (Phase 2 M2.X.5.aa.z.22) | 2 | Proposed | 2026-05-20 |
| [0139](0139-chat-max-cost-usd-flag.md) | `crossengin chat --max-cost-usd` session budget flag (Phase 2 M5.11) | 2 | Proposed | 2026-05-20 |
| [0140](0140-postgres-latency-tracker.md) | PostgresLatencyTracker + LatencyTracker contract async-ification (Phase 2 M6.7.y) | 2 | Proposed | 2026-05-20 |
| [0141](0141-router-instrumentation.md) | RouterInstrumentation interface + META_LLM_CALL_TRACES + PostgresRouterInstrumentation (Phase 2 M6.7.z) | 2 | Proposed | 2026-05-20 |
| [0142](0142-bedrock-create-inference-profile.md) | Bedrock createInferenceProfile (APPLICATION-only via copyFrom) (Phase 2 M2.X.5.aa.z.23) | 2 | Proposed | 2026-05-20 |
| [0143](0143-trace-retention-substrate.md) | META_RETENTION_POLICIES + PostgresTraceRetention — cross-cutting trace retention substrate (Phase 2 M6.7.zz) | 2 | Proposed | 2026-05-20 |
| [0144](0144-llm-cost-tiers.md) | META_LLM_COST_TIERS + per-tenant tier memberships — closes ADR-0137 Q2 (Phase 2 M6.8) | 2 | Proposed | 2026-05-20 |
| [0145](0145-bedrock-cross-resource-tagging.md) | Bedrock cross-resource tagging — tagResource + untagResource + listTagsForResource (Phase 2 M2.X.5.aa.z.24) | 2 | Proposed | 2026-05-20 |
| [0146](0146-bedrock-update-inference-profile.md) | Bedrock updateInferenceProfile (PATCH with APPLICATION-only guard) (Phase 2 M2.X.5.aa.z.25) | 2 | Proposed | 2026-05-20 |
| [0147](0147-bedrock-provisioned-throughput-inspection.md) | Bedrock provisioned-throughput inspection — getProvisionedModelThroughput + listProvisionedModelThroughputs (Phase 2 M2.X.5.aa.z.26) | 2 | Proposed | 2026-05-20 |
| [0148](0148-bedrock-create-provisioned-throughput.md) | Bedrock createProvisionedModelThroughput — first PT mutation with mandatory clientRequestToken (Phase 2 M2.X.5.aa.z.27) | 2 | Proposed | 2026-05-20 |
| [0149](0149-bedrock-update-provisioned-throughput.md) | Bedrock updateProvisionedModelThroughput — PATCH for model migration + rename (Phase 2 M2.X.5.aa.z.28) | 2 | Proposed | 2026-05-20 |
| [0150](0150-bedrock-delete-provisioned-throughput.md) | Bedrock deleteProvisionedModelThroughput — completes PT lifecycle (Phase 2 M2.X.5.aa.z.29) | 2 | Proposed | 2026-05-20 |
| [0151](0151-bedrock-foundation-model-discovery.md) | Bedrock foundation model discovery — getFoundationModel + listFoundationModels (Phase 2 M2.X.5.aa.z.30) | 2 | Proposed | 2026-05-20 |
| [0152](0152-router-instrumentation-embed-path.md) | RouterInstrumentation extends to embed() path — embed_call_started/completed/failed (Phase 2 M6.7.z.embed) | 2 | Proposed | 2026-05-20 |
| [0153](0153-trace-retention-preview-prune.md) | PostgresTraceRetention.previewPrune — dry-run mode for retention dashboards (Phase 2 M6.7.zz.dry-run) | 2 | Proposed | 2026-05-20 |
| [0154](0154-cost-ceiling-resolve-detailed.md) | PostgresCostCeilingResolver.resolveDetailed — source attribution for ceiling resolution (Phase 2 M6.8.x) | 2 | Proposed | 2026-05-20 |
| [0155](0155-per-tenant-retention-policies.md) | META_TENANT_RETENTION_POLICIES — per-tenant retention overrides (Phase 2 M6.7.zz.tenant) | 2 | Proposed | 2026-05-20 |
| [0156](0156-workflow-timer-instrumentation.md) | Workflow runtime timer_set + timer_cancelled instrumentation (Phase 2 M8.2) | 2 | Proposed | 2026-05-20 |
| [0157](0157-ceiling-resolved-event.md) | `ceiling_resolved` RouterInstrumentation event + getTenantCostCeilingDetailed callback (Phase 2 M6.8.x.trace) | 2 | Proposed | 2026-05-20 |
| [0158](0158-bedrock-set-exact-tags.md) | Bedrock `setExactTags` operator helper (Phase 2 M6.8.y) | 2 | Proposed | 2026-05-20 |
| [0159](0159-effective-retention-resolver.md) | `effectiveRetention` resolver on PostgresTraceRetention (Phase 2 M6.7.zz.tenant.dashboard) | 2 | Proposed | 2026-05-20 |
| [0160](0160-tenant-retention-opt-out.md) | Per-tenant retention `opt_out` flag (Phase 2 M6.7.zz.tenant.opt-out) | 2 | Proposed | 2026-05-20 |
| [0161](0161-opt-out-reason.md) | `opt_out_reason` audit context column (Phase 2 M6.7.zz.tenant.opt-out.reason) | 2 | Proposed | 2026-05-20 |
| [0162](0162-opt-out-until-expiry.md) | `opt_out_until` time-bound opt-outs (Phase 2 M6.7.zz.tenant.opt-out.expiry) | 2 | Proposed | 2026-05-20 |
| [0163](0163-expiring-opt-outs-resolver.md) | `expiringOptOuts` resolver for alert pipelines (Phase 2 M6.7.zz.tenant.opt-out.alerts) | 2 | Proposed | 2026-05-20 |
| [0164](0164-retention-expiring-cli.md) | `crossengin retention expiring` CLI subcommand (Phase 2 M6.7.zz.tenant.opt-out.cli) | 2 | Proposed | 2026-05-21 |
| [0165](0165-retention-effective-cli.md) | `crossengin retention effective` CLI action (Phase 2 M6.7.zz.tenant.opt-out.cli.effective) | 2 | Proposed | 2026-05-21 |
| [0166](0166-retention-mutation-cli.md) | `crossengin retention opt-out` / `opt-in` mutation actions (Phase 2 M6.7.zz.tenant.opt-out.cli.mutate) | 2 | Proposed | 2026-05-21 |
| [0167](0167-retention-list-policies-cli.md) | `crossengin retention list-policies` CLI action (Phase 2 M6.7.zz.tenant.opt-out.cli.list) | 2 | Proposed | 2026-05-21 |
| [0168](0168-retention-set-cli.md) | `crossengin retention set` CLI action + `setTenantRetention` adapter (Phase 2 M6.7.zz.tenant.retention-set) | 2 | Proposed | 2026-05-21 |
| [0169](0169-retention-delete-cli.md) | `crossengin retention delete` CLI action + `deleteTenantPolicy` adapter (Phase 2 M6.7.zz.tenant.retention-delete) | 2 | Proposed | 2026-05-21 |
| [0170](0170-retention-opt-out-history.md) | META_TENANT_RETENTION_OPT_OUT_HISTORY append-only audit log (Phase 2 M6.7.zz.tenant.opt-out.history) | 2 | Proposed | 2026-05-21 |
| [0171](0171-retention-restore-cli.md) | `crossengin retention restore` CLI action + `restoreTenantPolicy` adapter (Phase 2 M6.7.zz.tenant.opt-out.cli.restore) | 2 | Proposed | 2026-05-21 |
| [0172](0172-history-table-retention.md) | History-table retention (Phase 2 M6.7.zz.tenant.opt-out.history-retention) | 2 | Proposed | 2026-05-21 |
| [0173](0173-retention-diff-history-cli.md) | `crossengin retention diff-history` CLI action + `diffHistoryEntries` adapter (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history) | 2 | Proposed | 2026-05-21 |
| [0174](0174-retention-prune-cli.md) | `crossengin retention prune [--dry-run]` CLI action (Phase 2 M6.7.zz.tenant.opt-out.cli.prune) | 2 | Proposed | 2026-05-21 |
| [0175](0175-retention-history-cursor-pagination.md) | `retention history --after-id` cursor pagination (Phase 2 M6.7.zz.tenant.opt-out.cli.history.cursor) | 2 | Proposed | 2026-05-21 |
| [0176](0176-retention-restore-dry-run.md) | `retention restore --dry-run` + `previewRestoreTenantPolicy` adapter (Phase 2 M6.7.zz.tenant.opt-out.cli.restore.dry-run) | 2 | Proposed | 2026-05-21 |
| [0177](0177-effective-retention-batch.md) | `effectiveRetentionBatch` resolver for dashboard performance (Phase 2 M6.7.zz.tenant.batch) | 2 | Proposed | 2026-05-21 |
| [0178](0178-retention-diff-tenants-cli.md) | `crossengin retention diff` cross-tenant policy comparison (Phase 2 M6.7.zz.tenant.opt-out.cli.diff) | 2 | Proposed | 2026-05-21 |
| [0179](0179-retention-diff-vs-platform-cli.md) | `crossengin retention diff --vs-platform` tenant-vs-default comparison (Phase 2 M6.7.zz.tenant.opt-out.cli.diff.vs-platform) | 2 | Proposed | 2026-05-21 |
| [0180](0180-retention-diff-cross-table-cli.md) | `crossengin retention diff --cross-table` cross-table-within-tenant comparison (Phase 2 M6.7.zz.tenant.opt-out.cli.diff.cross-table) | 2 | Proposed | 2026-05-21 |
| [0181](0181-retention-diff-exit-on-divergence-cli.md) | `crossengin retention diff --exit-on-divergence` CI gate flag (Phase 2 M6.7.zz.tenant.opt-out.cli.diff.exit-on-divergence) | 2 | Proposed | 2026-05-21 |
| [0182](0182-retention-effective-batch-cli.md) | `crossengin retention effective-batch --pairs-file` CLI exposure (Phase 2 M6.7.zz.tenant.opt-out.cli.effective-batch) | 2 | Proposed | 2026-05-21 |
| [0183](0183-retention-diff-add-tenant-cli.md) | `crossengin retention diff --add-tenant` N-way cross-tenant comparison (Phase 2 M6.7.zz.tenant.opt-out.cli.diff.add-tenant) | 2 | Proposed | 2026-05-21 |
| [0184](0184-retention-diff-threshold-cli.md) | `crossengin retention diff --threshold N` fuzzy CI-gate threshold (Phase 2 M6.7.zz.tenant.opt-out.cli.diff.threshold) | 2 | Proposed | 2026-05-21 |
| [0185](0185-retention-history-actor-join-cli.md) | `crossengin retention history --with-actor-names` actor display name surfacing (Phase 2 M6.7.zz.tenant.opt-out.history.actor-join) | 2 | Proposed | 2026-05-21 |
| [0186](0186-retention-history-actor-filter-cli.md) | `crossengin retention history --actor-id` filter (Phase 2 M6.7.zz.tenant.opt-out.cli.history.actor-filter) | 2 | Proposed | 2026-05-21 |
| [0187](0187-retention-mutations-attributes-cli.md) | `--attributes <json>` flag on retention mutation CLI actions (Phase 2 M6.7.zz.tenant.opt-out.cli.history.attributes) | 2 | Proposed | 2026-05-21 |
| [0188](0188-retention-diff-add-table-cli.md) | `crossengin retention diff --cross-table --add-table` N-way cross-table comparison (Phase 2 M6.7.zz.tenant.opt-out.cli.diff.add-table) | 2 | Proposed | 2026-05-21 |
| [0189](0189-retention-diff-timeline-cli.md) | `crossengin retention diff-timeline <tenant-a> <tenant-b> <table>` cross-tenant chronological event merge (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline) | 2 | Proposed | 2026-05-21 |
| [0190](0190-retention-diff-timeline-with-actor-names-cli.md) | `crossengin retention diff-timeline --with-actor-names` actor display name surfacing on diff-timeline (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.with-actor-names) | 2 | Proposed | 2026-05-21 |
| [0191](0191-retention-diff-timeline-add-tenant-cli.md) | `crossengin retention diff-timeline --add-tenant` N-way cross-tenant chronological merge (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.add-tenant) | 2 | Proposed | 2026-05-21 |
| [0192](0192-retention-diff-timeline-cross-table-cli.md) | `crossengin retention diff-timeline --cross-table` cross-table-within-tenant chronological merge (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.cross-table) | 2 | Proposed | 2026-05-21 |
| [0193](0193-retention-diff-timeline-actor-filter-cli.md) | `crossengin retention diff-timeline --actor-id` actor filter across all three diff-timeline paths (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.actor-filter) | 2 | Proposed | 2026-05-21 |
| [0194](0194-retention-diff-timeline-kind-filter-cli.md) | `crossengin retention diff-timeline --kind` event-kind filter across all three diff-timeline paths (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.kind-filter) | 2 | Proposed | 2026-05-21 |
| [0195](0195-retention-diff-timeline-cursor-cli.md) | `crossengin retention diff-timeline --after-id` cursor pagination across all three diff-timeline paths (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.cursor) | 2 | Proposed | 2026-05-21 |
| [0196](0196-retention-history-before-id-cli.md) | `crossengin retention history --before-id` reverse cursor pagination (Phase 2 M6.7.zz.tenant.opt-out.history.before-id) | 2 | Proposed | 2026-05-21 |
| [0197](0197-retention-diff-timeline-before-id-cli.md) | `crossengin retention diff-timeline --before-id` reverse cursor pagination across all three diff-timeline paths (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.before-id) | 2 | Proposed | 2026-05-21 |
| [0198](0198-retention-diff-history-kind-filter-cli.md) | `crossengin retention diff-history --kind` event-kind expectation check (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.kind-filter) | 2 | Proposed | 2026-05-22 |
| [0199](0199-retention-diff-timeline-multi-actor-cli.md) | `crossengin retention diff-timeline --actor-id` repeatable for OR-semantic multi-actor filter (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.multi-actor) | 2 | Proposed | 2026-05-22 |
| [0200](0200-retention-diff-timeline-multi-kind-cli.md) | `crossengin retention diff-timeline --kind` repeatable for OR-semantic multi-kind filter (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.multi-kind) | 2 | Proposed | 2026-05-22 |
| [0201](0201-retention-history-range-cursor-cli.md) | `crossengin retention history --range <after-id>..<before-id>` window-cursor flag (Phase 2 M6.7.zz.tenant.opt-out.cli.history.range) | 2 | Proposed | 2026-05-22 |
| [0202](0202-retention-diff-timeline-range-cursor-cli.md) | `crossengin retention diff-timeline --range <after-id>..<before-id>` window-cursor flag across all three diff-timeline paths (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.range) | 2 | Proposed | 2026-05-22 |
| [0203](0203-retention-diff-history-actor-filter-cli.md) | `crossengin retention diff-history --actor-id` actor expectation check (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.actor-filter) | 2 | Proposed | 2026-05-22 |
| [0204](0204-retention-diff-history-with-actor-names-cli.md) | `crossengin retention diff-history --with-actor-names` actor display name surfacing (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.with-actor-names) | 2 | Proposed | 2026-05-22 |
| [0205](0205-retention-diff-history-actor-not-cli.md) | `crossengin retention diff-history --actor-id-not` actor exclusion expectation check (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.actor-not) | 2 | Proposed | 2026-05-22 |
| [0206](0206-retention-history-actor-not-cli.md) | `crossengin retention history --actor-id-not` actor exclusion filter (Phase 2 M6.7.zz.tenant.opt-out.cli.history.actor-not) | 2 | Proposed | 2026-05-22 |
| [0207](0207-retention-diff-timeline-actor-not-cli.md) | `crossengin retention diff-timeline --actor-id-not` actor exclusion filter across all three diff-timeline paths (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.actor-not) | 2 | Proposed | 2026-05-22 |
| [0208](0208-retention-diff-history-kind-not-cli.md) | `crossengin retention diff-history --kind-not` event-kind exclusion expectation check (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.kind-not) | 2 | Proposed | 2026-05-22 |
| [0209](0209-retention-history-system-only-cli.md) | `crossengin retention history --system-only` / `--no-system` actor-presence filter (Phase 2 M6.7.zz.tenant.opt-out.cli.history.system-only) | 2 | Proposed | 2026-05-22 |
| [0210](0210-retention-history-actor-not-multi-cli.md) | `crossengin retention history --actor-id-not` repeatable for multi-value OR-semantic exclusion filter (Phase 2 M6.7.zz.tenant.opt-out.cli.history.actor-not.multi) | 2 | Proposed | 2026-05-22 |
| [0211](0211-retention-history-actor-filter-multi-cli.md) | `crossengin retention history --actor-id` repeatable for multi-value OR-semantic positive filter (Phase 2 M6.7.zz.tenant.opt-out.cli.history.actor-filter.multi) | 2 | Proposed | 2026-05-22 |
| [0212](0212-retention-diff-history-system-only-cli.md) | `crossengin retention diff-history --system-only` / `--no-system` actor-presence expectation check (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.system-only) | 2 | Proposed | 2026-05-22 |
| [0213](0213-retention-diff-timeline-system-only-cli.md) | `crossengin retention diff-timeline --system-only` / `--no-system` actor-presence filter across all 3 dispatch paths (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.system-only) | 2 | Proposed | 2026-05-22 |
| [0214](0214-retention-diff-history-kind-not-multi-cli.md) | `crossengin retention diff-history --kind-not` repeatable for multi-value OR-semantic exclusion expectation check (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.kind-not.multi) | 2 | Proposed | 2026-05-22 |
| [0215](0215-retention-diff-history-per-side-cli.md) | `crossengin retention diff-history` per-side asymmetric expectation checks (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.per-side) | 2 | Proposed | 2026-05-22 |
| [0216](0216-retention-diff-history-per-side-system-only-cli.md) | `crossengin retention diff-history` per-side `--system-only` / `--no-system` actor-presence expectation check (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.per-side.system-only) | 2 | Proposed | 2026-05-22 |
| [0217](0217-retention-diff-history-kind-multi-cli.md) | `crossengin retention diff-history --kind` repeatable for multi-value OR-semantic tuple expectation check (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.kind.multi) | 2 | Proposed | 2026-05-23 |
| [0218](0218-retention-diff-history-actor-id-multi-cli.md) | `crossengin retention diff-history --actor-id` repeatable for multi-value OR-semantic tuple expectation check (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.actor-id.multi) | 2 | Proposed | 2026-05-23 |
| [0219](0219-retention-diff-history-actor-id-not-multi-cli.md) | `crossengin retention diff-history --actor-id-not` repeatable for multi-value OR-semantic tuple exclusion check (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.actor-id-not.multi) | 2 | Proposed | 2026-05-23 |
| [0220](0220-retention-diff-history-per-side-multi-cli.md) | `crossengin retention diff-history` per-side multi-value tuple expectations (bulk: --kind-a/b + --actor-id-a/b + --actor-id-not-a/b) (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-history.per-side.multi) | 2 | Proposed | 2026-05-23 |
| [0221](0221-retention-history-kind-multi-cli.md) | `crossengin retention history --kind` repeatable for multi-value OR-semantic substrate-side IN filter (Phase 2 M6.7.zz.tenant.opt-out.cli.history.kind.multi) | 2 | Proposed | 2026-05-23 |
| [0222](0222-retention-history-kind-not-multi-cli.md) | `crossengin retention history --kind-not` repeatable for multi-value OR-semantic substrate-side NOT IN exclusion filter (Phase 2 M6.7.zz.tenant.opt-out.cli.history.kind-not.multi) | 2 | Proposed | 2026-05-23 |
| [0223](0223-retention-diff-timeline-kind-not-multi-cli.md) | `crossengin retention diff-timeline --kind-not` repeatable for multi-value OR-semantic substrate-side NOT IN exclusion filter across 3 dispatch paths (Phase 2 M6.7.zz.tenant.opt-out.cli.diff-timeline.kind-not.multi) | 2 | Proposed | 2026-05-23 |
| [0224](0224-retention-json-envelope-shape-conventions.md) | Retention JSON envelope shape conventions (family-wide canonical conventions for multi-value/single-value/boolean envelope fields across all 3 retention surfaces) (Phase 2 M6.7.zz.tenant.opt-out.cli.json-envelope.conventions) | 2 | Proposed | 2026-05-23 |
| [0225](0225-retention-history-envelope-rename.md) | Retention history JSON envelope rename to canonical conventions (eventKinds → kinds, add action discriminator, add withActorNames echo — closes ADR-0224 future Qs 1-3) (Phase 2 M6.7.zz.tenant.opt-out.cli.history.envelope-rename) | 2 | Proposed | 2026-05-23 |
| [0226](0226-retention-cli-contradiction-detection.md) | Retention CLI cross-flag contradiction detection (CLI-side eager errors for set-intersection contradictions on positive + negative multi-value flag pairs across all 3 surfaces) (Phase 2 M6.7.zz.tenant.opt-out.cli.contradiction-detection) | 2 | Proposed | 2026-05-23 |
| [0227](0227-retention-cli-csv-format.md) | Retention CLI `--format=csv` output format (RFC 4180-compliant CSV across all 3 surfaces with surface-appropriate column schemas) (Phase 2 M6.7.zz.tenant.opt-out.cli.csv-format) | 2 | Proposed | 2026-05-23 |
| [0228](0228-retention-cli-explain-flag.md) | Retention CLI `--explain` flag (query plan output without database round-trip across all 3 surfaces) (Phase 2 M6.7.zz.tenant.opt-out.cli.explain-flag) | 2 | Proposed | 2026-05-23 |
| [0229](0229-retention-adapter-query-builders.md) | Retention adapter query-builder methods (5 public `buildXxxQuery` methods returning `{sql, params}`; raw PG SQL output for `--explain` across all 3 surfaces) (Phase 2 M6.7.zz.tenant.opt-out.cli.explain-flag.raw-sql) | 2 | Proposed | 2026-05-23 |
| [0230](0230-retention-cli-semantic-contradiction-detection.md) | Retention CLI cross-dimensional semantic contradiction detection (`--system-only` + `--actor-id` empty-by-construction; eager exit-2 across all 3 surfaces + diff-history per-side variants) (Phase 2 M6.7.zz.tenant.opt-out.cli.semantic-contradiction) | 2 | Proposed | 2026-05-23 |
| [0231](0231-retention-cli-format-tsv-ndjson-separator.md) | Retention CLI output format expansion (`--format=tsv` + `--format=ndjson` + `--csv-separator` — closes ADR-0227 Qs 1+3+4 bulk) (Phase 2 M6.7.zz.tenant.opt-out.cli.format-tsv-ndjson-separator) | 2 | Proposed | 2026-05-23 |
| [0232](0232-retention-cli-summary-action.md) | Retention CLI `summary` action (aggregate opt-out history counts grouped by kind/tenant/actor/table with the existing filter family) (Phase 2 M6.7.zz.tenant.opt-out.cli.summary) | 2 | Proposed | 2026-05-23 |
| [0233](0233-retention-json-envelope-two-level-field-naming.md) | Retention JSON envelope two-level field naming separation (envelope echoes operator INPUT; result/entries contain actual DATA; intentionally different naming conventions) (Phase 2 M6.7.zz.tenant.opt-out.cli.json-envelope.two-level-naming) | 2 | Proposed | 2026-05-23 |
| [0234](0234-retention-summary-time-bucket-grouping.md) | Retention summary time-bucket grouping (`--group-by day\|hour\|week\|month` via date_trunc UTC; chronological ordering for histogram-style time-series) (Phase 2 M6.7.zz.tenant.opt-out.cli.summary.time-bucket) | 2 | Proposed | 2026-05-23 |
| [0235](0235-retention-summary-cross-tab.md) | Retention summary cross-tab grouping (`--then-by` second dimension; composite {key, subKey, count} buckets for any × any dimension grid) (Phase 2 M6.7.zz.tenant.opt-out.cli.summary.cross-tab) | 2 | Proposed | 2026-05-23 |
| [0236](0236-retention-summary-gap-filling.md) | Retention summary gap-filling (`--fill-gaps` zero-count buckets via generate_series + LEFT JOIN for continuous temporal histograms) (Phase 2 M6.7.zz.tenant.opt-out.cli.summary.gap-fill) | 2 | Proposed | 2026-05-23 |
| [0237](0237-retention-summary-custom-timezone.md) | Retention summary custom timezone bucketing (`--timezone <iana-tz>`; parameterized injection-safe `AT TIME ZONE` for local-time temporal buckets) (Phase 2 M6.7.zz.tenant.opt-out.cli.summary.timezone) | 2 | Proposed | 2026-05-23 |
| [0238](0238-retention-summary-top-min-count.md) | Retention summary result limiting (`--top N` highest-count buckets + `--min-count N` HAVING threshold) (Phase 2 M6.7.zz.tenant.opt-out.cli.summary.top-min-count) | 2 | Proposed | 2026-05-23 |
| [0239](0239-operator-guides-retention.md) | Operator guides convention + retention operator guide (consolidates the retention CLI surface into docs/operator-guides/retention.md) (Phase 2 M6.7.zz.tenant.opt-out.cli.operator-guide) | 2 | Proposed | 2026-05-23 |
| [0240](0240-retention-cli-explain-analyze.md) | Retention CLI `--explain-analyze` (executes EXPLAIN ANALYZE for the real PG execution plan with timing + row counts across the 4 read surfaces) (Phase 2 M6.7.zz.tenant.opt-out.cli.explain-analyze) | 2 | Proposed | 2026-05-23 |
| [0241](0241-retention-cli-format-yaml.md) | Retention CLI `--format=yaml` output (same structured envelope as json, in YAML, wired everywhere json is via printStructured — closes ADR-0231 Q1) (Phase 2 M6.7.zz.tenant.opt-out.cli.format-yaml) | 2 | Proposed | 2026-05-23 |
| [0242](0242-retention-summary-top-per-group.md) | Retention summary `--top-per-group N` (per-group leaderboards for cross-tab grids via ROW_NUMBER() window — top N sub-keys within each primary group; closes ADR-0238 Q1) (Phase 2 M6.7.zz.tenant.opt-out.cli.summary.top-per-group) | 2 | Proposed | 2026-05-24 |
| [0243](0243-typecheck-test-debt-paydown.md) | Type-check + test-suite debt paydown (workspace genuinely green — fixes 13 type errors + 1 failing test: labelForIndex dup, chat stale `.mediaType`/`.data` field access + readonly content, chat `--format` human/json contract, stale 128→129 table-count assertions) (Phase 2 M-maint.typecheck-test-debt) | 2 | Proposed | 2026-05-24 |
| [0244](0244-typecheck-test-files-workspace-wide.md) | Type-check `*.test.ts` workspace-wide (single-root-pass `tsconfig.typecheck-tests.json` + `typecheck:tests` guard script; fixes all 134 pre-existing test-file type errors across 24 packages — stale fixtures, `vision` capability completeness, `readonly[]` `.sort()`, unused imports, closure-narrow-to-`never`, undeclared cross-package import; closes ADR-0243 future Q1) (Phase 2 M-maint.typecheck-tests) | 2 | Proposed | 2026-05-25 |
| [0245](0245-ci-gate-workspace-invariant.md) | CI gate enforcing the workspace invariant (first `.github/workflows/ci.yml` — one `verify` job: install + build + `pnpm -r typecheck` + `pnpm typecheck:tests` + `pnpm -r test` on PR + main/master push; machine-enforces the all-green, zero-type-errors (src + test) invariant so ADR-0243 drift can't recur; closes ADR-0244 future Q1) (Phase 2 M-maint.ci-gate) | 2 | Proposed | 2026-05-25 |
| [0246](0246-eslint-flat-config-lint-gate.md) | ESLint v9 flat config + lint gate (non-type-aware `recommended`; `**/*.test.ts` fully bans `@ts-expect-error`/`@ts-ignore` + `no-explicit-any` — closes ADR-0244 Q2; root `eslint .` + CI lint step; fixed 25 surfaced violations) (Phase 2 M-maint.lint-flat-config) | 2 | Proposed | 2026-05-25 |
| [0247](0247-retention-summary-bottom-per-group.md) | Retention summary `--bottom-per-group N` (per-group bottom-N sub-keys for cross-tab grids via ASC `ROW_NUMBER()` window — mirror of `--top-per-group`; requires `--then-by`, mutually exclusive with `--top` + `--top-per-group`; closes ADR-0242 Q1) (Phase 2 M6.7.zz.tenant.opt-out.cli.summary.bottom-per-group) | 2 | Proposed | 2026-05-25 |
| [0248](0248-embed-ceiling-enforcement.md) | Cost-ceiling enforcement on the embed() path (generalize `enforceCeilingPreflight` to a `{tenantId, sessionId, task}` context; `embed()` now resolves + enforces ceilings and emits `ceiling_resolved` — embeddings respect per-request/tenant/tier/global budgets; closes ADR-0157 Q1) (Phase 2 M6.8.x.trace.embed) | 2 | Proposed | 2026-05-25 |
| [0249](0249-workflow-cancel-timer.md) | Implement the workflow `cancel_timer` action (was "not implemented in M3"); resolves active timers by name → emits the reserved `timer_cancelled` instrumentation + event-log event; shared `activeTimersFromEvents` helper; `cancel_timer requires timerName` validation — closes ADR-0156's reserved-event gap (Phase 2 M8.3) | 2 | Proposed | 2026-05-25 |
| [0250](0250-prettier-format-gate.md) | Prettier config + `format`/`format:check` scripts + CI gate (full one-time reformat of 636 TS/JS files, whitespace-only; root `.prettierrc.cjs` re-exports the shared `@crossengin/config/prettier` base; `.prettierignore` scopes to TS/JS source — markdown/JSON/YAML hand-maintained; `prettier` root devDependency; `Format check` CI step after Lint — closes ADR-0246 Q2; tests unchanged at 9,405) (Phase 2 M-maint.prettier-gate) | 2 | Proposed | 2026-05-25 |
| [0251](0251-workflow-send-signal.md) | Implement the workflow `send_signal` action (was "not implemented in M3"); delegates to `submitSignal` for tenant-scoped correlated delivery (saga-coordinator → participants); sender threaded as `sourceSystem` on receivers' `signal_received`; new `signal_emitted` instrumentation kind (16→17) + `META_WORKFLOW_TRACES` CHECK; `send_signal requires signalName + correlationKey` validation — closes ADR-0249's `send_signal` gap; `spawn_child_workflow` now the only unimplemented action (Phase 2 M8.4) | 2 | Proposed | 2026-05-25 |
| [0252](0252-router-per-model-pricing.md) | Per-model pricing for router cost estimates — new optional `LlmProvider.pricingFor(modelId)` surfaces each provider's existing per-model rate tables (OpenAI/Bedrock/Anthropic); router uses `pricingFor(choice.modelId) ?? pricing` in both the embed + complete preflight estimates so the embed ceiling uses the chosen embedding model's real rate (e.g. text-embedding-3-small $0.02/M, not the $0.15/M chat default) — closes ADR-0248 Q1; backward-compatible (mocks/providers without it fall back); real cost still from usage_final (Phase 2 M6.8.x.trace.embed.pricing) | 2 | Proposed | 2026-05-26 |
| [0253](0253-workflow-spawn-child.md) | Implement the workflow `spawn_child_workflow` action (the last throwing dispatch branch) — parent starts a child instance by `childDefinitionKey` (linked via `parentInstanceId`, with input + correlationKey), records `child_workflow_spawned`, and on the child's terminal fires the parent's `child_workflow_completed` transition; new `waiting_for_child` status (12→13) + `META_WORKFLOW_INSTANCES`/`TRACES` CHECKs; 2 new instrumentation kinds (17→19); `spawn_child_workflow requires childDefinitionKey` validation — the `applyAction` switch is now complete (Phase 2 M8.5) | 2 | Proposed | 2026-05-26 |
| [0254](0254-multi-trigger-wait-drivers.md) | Multi-trigger wait drivers — `submitSignal` accepts `waiting_for_timer` so a signal can pre-empt a deadline (the on-entry `schedule_timer` projects to `waiting_for_timer`, not `waiting_for_signal`); symmetric `tickTimers` accepts `waiting_for_signal` so cross-state SLA timers fire on signal-only states — closes ADR-0251/0249's shared Q; two one-line filter widenings; other `waiting_for_*` statuses still gated (Phase 2 M8.6) | 2 | Proposed | 2026-05-26 |
| [0255](0255-workflow-submit-manual-action-driver.md) | Implement the workflow `submitManualAction` driver — completes the *trigger* vocabulary (signals/timers/activity/child/automatic all had drivers, `manual_action` did not despite the trigger + `waiting_for_manual` status + event kind + projection case all existing); addresses by `instanceId` (not `correlationKey`), status filter `running`/`waiting_for_manual`, enforces trigger's `requiresFourEyes` (distinct second approver) + `requiredRole` (throws before audit), emits `manual_action_taken` instrumentation (19→20) + appends `manual_action_taken` event for audit completeness even when no transition fires, applies + runs step loop if matched (Phase 2 M8.7) | 2 | Proposed | 2026-05-26 |
| [0256](0256-workflow-definition-validator.md) | Workflow definition pre-publish validator — new `validateDefinition(def)` in `@crossengin/workflow-engine/src/validation.ts` distinct from the parse-time schema; structured `{ok, issues[]}` with 5 stable codes (`dead_end_state` error, `unreachable_state` warning, `unknown_variable_in_action` / `unknown_timer_in_action` / `unknown_signal_in_action` errors) + JSON-pointer paths for tooling; catches three classes of authoring bug the schema misses (non-terminal states with no outgoing transitions, dead code, action-target refs to undeclared variables/timers/signals); cross-registry refs (`schedule_activity.activityKey`, `spawn_child_workflow.childDefinitionKey`) deferred (need registry access); pure additive module, no schema change, no engine change (Phase 2 M8.8) | 2 | Proposed | 2026-05-26 |
| [0257](0257-cli-workflow-validate.md) | `crossengin workflow validate <def.json> [--strict]` CLI gate exposing ADR-0256's validator — new `workflow` subcommand + action-verb dispatch joining the established pattern (`sessions list`, `gateway routes register-pack`, `retention diff-history`); pipeline reads JSON → `WorkflowDefinitionSchema.safeParse` → `validateDefinition` → render; exit codes follow ADR-0181 (0 ok, 1 runtime, 2 misuse/schema-reject, 3 validation errors); `--strict` promotes warnings to exit 3; human + JSON output (`{action, ok, definitionId, definitionKey, errorCount, warningCount, issues[]}` envelope; distinct `schemaError: true` shape on parse-time rejection); future Qs cover bulk glob path, per-rule severity overrides, auto-format detection, `workflow lint` stylistic action (Phase 2 M8.8.x) | 2 | Proposed | 2026-05-26 |
| [0258](0258-gateway-pipeline-retention.md) | Gateway pipeline-execution retention — 5th and final platform-prunable table; `META_GATEWAY_PIPELINE_EXECUTIONS` (timeColumn=started_at) joins workflow_traces + llm_call_traces + tenant_retention_opt_out_history + llm_latency_samples in PRUNABLE_TABLES; META CHECK constraints on retention_policies + tenant_retention_policies widened additively (no migration); new optional `PrunableTableSpec.nullableTenantId` flag emits `tenant_id IS NULL OR tenant_id NOT IN (...)` in the platform-default DELETE/COUNT so NULL-tenant rows (anonymous /__ping, /__health) get swept (default `NOT IN` silently excludes NULL rows — the SQL semantic trap); per-tenant DELETE unchanged (`WHERE tenant_id = $1`); existing 3 tenant-bearing tables' SQL is byte-identical (additive flag); operator surfaces (set/opt-out/effective/history/expiring) work unchanged on the new table (Phase 2 M4.11) | 2 | Proposed | 2026-05-26 |
| [0259](0259-gateway-idempotency-prune-cli.md) | Gateway idempotency-record prune CLI — closes ADR-0258 Q6; `crossengin gateway prune-idempotency [--dry-run]` as a new gateway action wrapping `PostgresIdempotencyStore.deleteExpired(now)` (existing since M4.5) + new `previewDeleteExpired(now)` adapter method (ADR-0153 preview/run dual-method pattern); deliberately NOT in PRUNABLE_TABLES since `expires_at` is the row-owned TTL (different semantic from retention_days policy); JSON envelope `{action, dryRun, asOf, deletedCount | wouldDeleteCount}`; exit codes follow ADR-0181 (0 ok, 1 PG/adapter failure); no schema change, no CHECK widening, no policy table — pure additive substrate method + CLI action; clockOverride for deterministic tests (Phase 2 M4.12) | 2 | Proposed | 2026-05-26 |
| [0260](0260-gateway-prune-idempotency-scope-flags.md) | Gateway prune-idempotency scope flags — closes ADR-0259 Q1+Q2; adds `--operation-id <id>` / `--method <POST|PUT|PATCH|DELETE>` / `--limit <n>` to the M4.12 CLI action + new `PruneExpiredOptions` second argument on `PostgresIdempotencyStore.deleteExpired` / `previewDeleteExpired`; LIMIT uses `record_id IN (SELECT ... LIMIT)` subquery since PG DELETE has no native LIMIT; preview wraps the LIMIT in a sub-SELECT so wouldDeleteCount matches the live DELETE; baseline M4.12 SQL is byte-identical when no options pass; validation fires at CLI boundary before PG resolution (invalid --method → exit 2, --limit 0/non-int → exit 2, no side effects); JSON envelope always emits all three scope fields (null when unset); ADR-0259 Q3 `--older-than` deliberately NOT in scope (breaks idempotency contract for in-flight TTL keys) (Phase 2 M4.13) | 2 | Proposed | 2026-05-26 |
| [0261](0261-coverage-gate.md) | Workspace-wide coverage gate — completes the static-quality gate quartet (lint / format / typecheck / coverage); `@vitest/coverage-v8` as root devDep; shared `vitestPreset` gains thresholds (statements 80% / branches 70% / functions 80% / lines 80%) + exclusions (`**/index.ts` barrel re-exports, `*.d.ts`, `*.test.ts`, `dist/`); root `pnpm coverage` script wraps `pnpm -r --filter !@crossengin/config exec vitest run --coverage` (pnpm 9's `--` arg-forward is broken for `-r run`, so exec bypasses it); CI step added after Test, verify-job timeout 20 → 30min; all 57 workspace packages comfortably exceed thresholds at landing (lowest: api-gateway-runtime 86.44% statements / 80.4% branches); no schema or substrate change — pure tooling additive (Phase 2 M-maint.coverage) | 2 | Proposed | 2026-05-26 |
| [0262](0262-rate-limit-decisions-retention.md) | Rate-limit-decisions retention — 6th and final operational hot-write table covered by the retention substrate; `META_RATE_LIMIT_DECISIONS` (timeColumn=decided_at, NULLABLE tenant_id, RLS tenant_or_platform — same shape as `gateway_pipeline_executions`); reuses ADR-0258's `nullableTenantId` flag verbatim so the SQL is byte-identical to M4.11's path; 2 META CHECK widenings (5→6 platform, 4→5 per-tenant) + 1 PRUNABLE_TABLES entry; existing `crossengin retention set/opt-out/history/effective/expiring` actions work unchanged on the new table — zero new CLI surface; substrate is now retention-complete across workflow + llm + history + gateway pipeline + idempotency + rate-limit surfaces (Phase 2 M4.11.x) | 2 | Proposed | 2026-05-26 |
| [0263](0263-gateway-housekeeping-dashboard.md) | Gateway housekeeping unified dashboard — closes ADR-0262 Q5; `crossengin gateway housekeeping` action aggregates total row count + oldest row + would-prune count + retention-days + last-pruned-at across the 3 gateway housekeeping tables (gateway_pipeline_executions, gateway_idempotency_records, rate_limit_decisions); read-only (no DELETEs); 5 PG round-trips total (listPolicies + previewPrune + 3 table-stats SELECTs); human + JSON output with stable null fields; HOUSEKEEPING_TABLES hardcoded so operator-domain scope is honest (workflow tables stay under hypothetical workflow housekeeping action); reuses existing adapter APIs (listPolicies + previewPrune + previewDeleteExpired) — zero new substrate methods; pure additive (Phase 2 M4.14) | 2 | Proposed | 2026-05-29 |
| [0264](0264-retention-housekeeping-dashboard.md) | Retention housekeeping unified dashboard — closes ADR-0263 Q2 (via substrate-centric naming); `crossengin retention housekeeping` action aggregates total row count + oldest row + would-prune count + retention-days + last-pruned-at + per-tenant-policy-count across ALL 6 PRUNABLE_TABLES (workflow_traces + llm_call_traces + llm_latency_samples + tenant_retention_opt_out_history + gateway_pipeline_executions + rate_limit_decisions); deliberate overlap with ADR-0263's gateway housekeeping on the 3 gateway-retention tables is intentional (substrate-centric vs operator-domain views); naming pivot from ADR-0263 Q2's "workflow housekeeping" framing because the 4 non-gateway tables span 3 distinct operator domains (workflow + ai-router + retention substrate self-management); 16th retention CLI action; new `perTenantPolicyCount` field beyond ADR-0263's shape gives operators per-table override-noise signal; 5 PG round-trips total (listPolicies + listTenantPolicies + previewPrune + 6 table-stats SELECTs); reuses existing adapter APIs — zero new substrate methods; pure additive (Phase 2 M4.14.x) | 2 | Proposed | 2026-05-29 |
| [0265](0265-housekeeping-watch-mode.md) | Housekeeping `--watch` mode for incident-room monitoring — closes ADR-0263 Q3 + ADR-0264 Q6; adds `--watch [--watch-interval N]` to both `crossengin gateway housekeeping` and `crossengin retention housekeeping`; loops in same process (one PG connection vs N from `watch -n`); default 5-second interval in [1, 3600]; `--watch` requires `--format human|json` (csv/tsv/ndjson/yaml rejected as batch formats); human format ANSI clears screen between ticks for live single-screen UX, json format streams compact NDJSON-of-envelopes for log aggregators; new shared `housekeeping-watch.ts` module with generic `runHousekeepingWatchLoop<R>` + `parseWatchFlags` helper; test injection via `watchOverride: {maxIterations, abortSignal, setTimeoutFn, clearTimeoutFn}`; `gather()` errors propagate exit 1 (no silent swallow); SIGINT defaults to Node exit 130 (graceful shutdown is future Q); pure additive (Phase 2 M4.14.w) | 2 | Proposed | 2026-05-29 |
| [0266](0266-housekeeping-threshold-alert.md) | Housekeeping `--threshold-alert <field>:<op><value>` CI-gate flag — closes ADR-0263 Q4 + ADR-0264 Q4 + ADR-0265 Q6 in one milestone; repeatable flag on both `gateway housekeeping` + `retention housekeeping`; exits 3 (ADR-0181 convention) when any alert trips on any table; operators: `>`/`>=`/`<`/`<=`/`=`; values: numeric (e.g., `:>1000000`), duration with units s/m/h/d/w/y (e.g., `lastPrunedAt:>24h`), or absolute ISO 8601 timestamp; per-surface field registries enforce type matching (numeric vs timestamp_nullable) at parse time; null timestamps treated as "infinitely old" for GT/GTE duration checks (matches "never pruned" intent), null numeric fields skip evaluation; composes with `--watch` — first tripping tick halts the loop with exit 3 (via new `"halt" \| void` return from render callback); JSON envelope always emits `alerts: []` (stable parsing) populated with tripped entries; new shared `threshold-alert.ts` module (~340 lines) with parser + evaluator + renderer; pure additive (Phase 2 M4.14.t) | 2 | Proposed | 2026-05-29 |
| [0267](0267-housekeeping-watch-keep-going.md) | Housekeeping `--watch-keep-going` resilient watch mode — closes ADR-0265 Q2; opt-in flag on both housekeeping actions (requires --watch); when set, `gather()` errors are caught + passed to caller's `errorRender` + loop continues, AND threshold-alert trips are tracked stickily without halting early; exit code matrix: keep-going + no trip + no/some errors = 0, keep-going + ANY trip = 3 (sticky across ticks). Error envelope renders as human "(error this tick: <msg>)" or compact NDJSON `{action, asOf, error: {message}}` depending on format. Designed for long-running incident monitoring where transient PG blips should not crash the dashboard. Composes naturally with `--threshold-alert` for "fail if any tick during this window tripped" CI gates (e.g., `timeout 600 crossengin retention housekeeping --watch --watch-keep-going --threshold-alert wouldPruneCount:>1M`). `WatchLoopInput` gains `keepGoing?: boolean` + `errorRender?: (err) => void`; production callers leave undefined for current behavior. Pure additive (Phase 2 M4.14.s) | 2 | Proposed | 2026-05-29 |
| [0268](0268-housekeeping-sigint-bridge.md) | Housekeeping `--watch` SIGINT-to-AbortController bridge — closes ADR-0265 Q1; Ctrl-C during a watch loop now exits cleanly (exit 0, or 3 if any tick tripped under sticky tracking from M4.14.s) with PG connection closed via the action's `finally` block, instead of Node's default exit 130 that drops the connection abruptly. Bridge installs `process.on("SIGINT", () => controller.abort())` when --watch is set AND caller has NOT supplied an `abortSignal` override (tests skip the bridge by passing their own signal); RAII-style cleanup via paired install/remove in the action's try/finally. New `installSigintBridge(register?: SignalRegistrar)` exported helper + `SignalRegistrar` type alias; test-injection via `signalRegistrar?: SignalRegistrar` field on WatchOverride for capture-and-replay simulation without poisoning the test runner's actual SIGINT events. SIGTERM bridging deferred (future Q for managed-service operators). Pure additive (Phase 2 M4.14.r) | 2 | Proposed | 2026-05-29 |
| [0269](0269-retention-housekeeping-tenant-filter.md) | Retention housekeeping `--tenant <uuid>` drill-down filter — closes ADR-0264 Q3; `crossengin retention housekeeping --tenant <uuid>` adds per-table `tenantPolicy` sections showing the tenant's effective override OR explicit "(no override — inherits platform default)" placeholder; cross-tenant aggregates (totalRowCount, oldestAt, wouldPruneCount, perTenantPolicyCount) stay intact for mental-model continuity (deliberate Option B vs full per-tenant scoping); UUID_REGEX validation at CLI boundary (exit 2 on invalid syntax); JSON envelope echoes `tenantFilter: string \| null` + each `TableReport.tenantPolicy?: TenantRetentionPolicyRow \| null` field; composes with `--threshold-alert` + `--watch` + `--watch-keep-going` + SIGINT bridge transparently (validation fires BEFORE PG resolution so misuse exits 2 without burning a connection); backward-compat — omitting `--tenant` preserves existing behavior verbatim (`tenantPolicy` field absent from envelope, no behavior change); zero new substrate queries (filters existing `listTenantPolicies` result in-process by tenantId); pure additive (Phase 2 M4.14.u) | 2 | Proposed | 2026-05-29 |
| [0270](0270-gateway-housekeeping-tenant-filter.md) | Gateway housekeeping `--tenant <uuid>` drill-down filter — closes ADR-0263 Q1; mirrors ADR-0269's drill-down on the operator-domain gateway dashboard. Surfaces the 3-table-2-semantic asymmetry — retention-substrate tables (gateway_pipeline_executions + rate_limit_decisions) render the tenant's override row OR "(no override — inherits platform default)"; the expires_at-managed table (gateway_idempotency_records) always renders `tenantPolicy: null` with "(not applicable — expires_at-managed)" because per-tenant overrides don't exist on the TTL surface. Option B drill-down semantic carried forward verbatim from ADR-0269 — aggregates stay cross-tenant, zero new substrate queries (filters existing listTenantPolicies in-process). UUID_REGEX validation at CLI boundary, fail-fast on misuse. Composes with `--threshold-alert` + `--watch` + `--watch-keep-going` + SIGINT bridge transparently. The two operator-facing dashboards (gateway from ADR-0263 + retention from ADR-0264) now both support symmetric tenant drill-down via the same flag. Pure additive (Phase 2 M4.14.v) | 2 | Proposed | 2026-05-29 |
| [0271](0271-housekeeping-all-tenants-matrix.md) | Housekeeping `--all-tenants` matrix mode — closes ADR-0269 Q2 + ADR-0270 Q2; adds `--all-tenants` boolean flag to BOTH dashboards emitting a per-table `tenantOverrides: ReadonlyArray<TenantRetentionPolicyRow>` sorted by tenantId for stable diff-able JSON exports. Chose long-format per-table grouping over wide-format matrix (existing-shape additive, sparse-by-default at realistic scale, operator-workflow match). Mutually exclusive with `--tenant <uuid>` at CLI boundary (the two flags answer different questions — one tenant vs every tenant). Aggregates stay cross-tenant (same Option B semantic). Gateway's expires_at-managed idempotency table renders empty `tenantOverrides: []` with "(not applicable — expires_at-managed)" placeholder — same asymmetry surfacing as ADR-0270. Top-level `allTenants: true` discriminator in JSON envelope. Zero new substrate queries (pivots existing listTenantPolicies fetch into two lookup shapes). Composes with `--threshold-alert` + `--watch` + `--watch-keep-going` + SIGINT bridge transparently. Pure additive (Phase 2 M4.14.q) | 2 | Proposed | 2026-05-29 |
| [0272](0272-housekeeping-sigterm-bridge.md) | Housekeeping `--watch` SIGTERM bridge alongside SIGINT — closes ADR-0268 Q1. Extends the M4.14.r bridge to register handlers for BOTH SIGINT (Ctrl-C from operators) AND SIGTERM (Kubernetes / systemd / container managers sending "stop gracefully") under a shared AbortController. Renamed `installSigintBridge` → `installShutdownBridge` to reflect broader scope; deprecated alias preserved for backward compat. The single-controller design reflects operator mental model — from the dashboard's perspective, "stop gracefully" is one event regardless of which signal triggered it. Cleanup removes both handlers in the action's finally block; PG closes; sticky-trip exit-3 from M4.14.s survives SIGTERM so managed-service CI gates that depend on "any tick during this watch window tripped" continue working. SignalRegistrar test mechanism widened to `Map<string, () => void>` so tests can verify per-signal abort semantics + register/remove pairs. Help text on both housekeeping actions updated. Pure additive (Phase 2 M4.14.p) | 2 | Proposed | 2026-05-29 |
| [0273](0273-housekeeping-tenant-slug-lookup.md) | Housekeeping `--tenant <uuid\|slug>` via meta.tenants lookup — closes ADR-0269 Q3 + ADR-0270 Q3 + ADR-0271 Q3. Widens --tenant on both housekeeping dashboards to accept slugs alongside UUIDs. New shared `resolveTenantIdentifier(conn, value)` helper in tenant-resolver.ts uses UUID regex as discriminator — UUID-shaped values short-circuit (zero PG cost preserved for scripted callers), slug-shaped values resolve via one extra `SELECT id FROM meta.tenants WHERE slug = $1` BEFORE the gather closure runs. Under --watch the slug resolves ONCE before the loop starts (slug-to-UUID mapping is stable for the duration of the watch). Unknown slug exits 2 with "no tenant with slug '<value>'" explanatory error. JSON envelope continues to echo the RESOLVED UUID in tenantId field so operators chaining commands get the stable canonical identifier. Eliminates UUID copy-paste friction during interactive debugging while preserving scripted-caller performance. Pure additive — pre-existing UUID-only behavior unchanged (Phase 2 M4.14.o) | 2 | Proposed | 2026-05-29 |
| [0274](0274-housekeeping-compound-thresholds.md) | Housekeeping `--threshold-alert` compound AND/OR expressions — closes ADR-0266 Q1. Extends the flag grammar to accept compound expressions within a single flag (`<clause>[ AND <clause>...]` or `<clause>[ OR <clause>...]`). AND trips when EVERY clause matches; OR trips when ANY matches (semantically equivalent to multiple flags but syntactically convenient). Cross-flag implicit OR preserved verbatim (backward compat). Mixed AND+OR within one flag rejected with explanatory exit 2 — operators wanting mixed semantics use multiple flags. Case-sensitive uppercase keyword + required surrounding spaces avoids collisions with values containing "and"/"or" substrings. ThresholdAlertSpec gains combinator: "SINGLE"|"AND"|"OR" + clauses[] array; field/op/value at top level mirror clauses[0] for backward compat. TrippedAlert similarly gains combinator + trippedClauses[]. New `evaluateAlertCompound` owns per-clause iteration + combinator logic; legacy `evaluateAlertOnRow` preserved for backward compat with existing unit tests + external callers. Renderer differentiates compound vs single-clause output (compound emits "trips compound threshold" + per-clause detail lines). Per-clause field + value-kind validation at parse time so typos surface before any dashboard runs (Phase 2 M4.14.n) | 2 | Proposed | 2026-05-29 |
| [0275](0275-retention-actions-tenant-slug.md) | `--tenant <uuid\|slug>` on retention list-policies / history / summary — closes ADR-0273 Q5 mechanically. Wires the M4.14.o resolveTenantIdentifier helper into all three remaining retention actions that accept --tenant; matches M4.14.o behavior verbatim (UUID short-circuits, slug resolves via meta.tenants, unknown slug exits 2). Shared private `resolveTenantFlagFor(raw, conn, ctx, actionLabel)` wraps the lower-level helper with action-label prefix on the error. ResolvedHandle from resolveRetention extended to expose the raw PG conn so per-action dispatchers thread it through. --all-tenants deliberately NOT added (omitting --tenant already produces "all tenants" behavior on each of these three surfaces; adding a no-op alias would confuse help-text readers). Asymmetric design with housekeeping documented + intentional — housekeeping needed --all-tenants because it changes DATA shape, these surfaces change only FILTER scope. Pure additive — pre-existing UUID-only behavior unchanged (Phase 2 M4.14.m) | 2 | Proposed | 2026-05-29 |
| [0276](0276-cross-dashboard-tenant-housekeeping.md) | Cross-dashboard `crossengin tenant housekeeping` combined view — closes ADR-0270 Q7. New top-level `tenant` subcommand with first action `housekeeping` runs BOTH gateway housekeeping AND retention housekeeping under one envelope: single PG conn, single `--tenant` resolution (slug→UUID happens once and applies to both dashboards), `--threshold-alert` evaluates across the UNION of all tables from both sides so alerts on either surface trip exit 3. Human format prints two clearly-separated sections (`=== Gateway housekeeping ===` + `=== Retention housekeeping ===`); JSON envelope merges both reports under `{action: "tenant.housekeeping", gateway, retention, alerts, ...}`. --watch / --watch-keep-going deferred for v1 (combining two watch loops has subtle ordering concerns; one-shot compliance audits are the primary use case). Reserves the `tenant` top-level namespace for future cross-dashboard tenant-scoped operations (lifecycle, policies). Pure additive — both individual dashboards unchanged (Phase 2 M4.14.l) | 2 | Proposed | 2026-05-29 |
| [0277](0277-tenants-standalone-subcommand.md) | `crossengin tenants` standalone subcommand (list + resolve) — closes ADR-0273 Q3 + ADR-0275 Q6. New top-level `tenants` (plural) subcommand reserves a namespace for collection-level tenant operations distinct from the `tenant` (singular) subcommand from ADR-0276 which holds per-tenant actions. `tenants list` enumerates from `meta.tenants` with `--status` / `--table-filter` / `--has-overrides` filters (human table + filter suffix in header; JSON envelope `{action: "tenants.list", count, tenants[]}`). `tenants resolve <slug\|uuid>` is the one-shot slug→UUID helper for shell scripting — human format prints JUST the UUID + newline (`crossengin tenants resolve acme-prod \| xargs -I {} crossengin gateway housekeeping --tenant {}` works without jq); JSON envelope `{action: "tenants.resolve", input, tenantId}`. Reuses `resolveTenantIdentifier` from M4.14.o verbatim. ORDER BY slug for stable diff-able list output. EXISTS subqueries against `meta.tenant_retention_policies` for the override filters (operators don't need to know the JOIN syntax). Pure additive — no existing surface affected (Phase 2 M4.14.k) | 2 | Proposed | 2026-05-29 |

## Tiers

ADRs are written in three tiers reflecting how foundational they are:

- **Tier 1 — Foundation.** Must exist before any code. ADRs 0001, 0002, 0003, 0004, 0005, 0008, 0009, 0018, 0024, 0046.
- **Tier 2 — Phase 1-3.** Written during kernel + workflow + AI Architect build-out. ADRs 0006, 0007, 0010, 0011, 0012, 0013, 0014, 0015, 0016, 0017, 0020, 0023, 0025, 0027, 0028, 0030, 0031, 0034, 0036, 0037, 0038, 0039, 0040, 0041, 0042, 0043, 0044, 0045, 0047, 0048, 0049, 0050, 0051, 0052, 0053, 0054, 0055, 0056, 0057, 0058, 0059, 0060, 0061, 0062, 0063, 0064, 0065, 0066, 0067, 0068, 0069, 0070, 0071, 0072, 0073, 0074, 0075, 0076, 0077, 0078, 0079, 0080, 0081, 0082, 0083, 0084, 0085, 0086, 0087, 0088, 0089, 0090, 0091, 0092, 0093, 0094, 0095, 0096, 0097, 0098, 0099, 0100, 0101, 0102, 0103, 0104, 0105, 0106, 0107, 0108, 0109, 0110, 0111, 0112, 0113, 0114, 0115, 0116, 0117, 0118, 0119, 0120, 0121, 0122, 0123, 0124, 0125, 0126, 0127, 0128, 0129, 0130, 0131, 0132, 0133, 0134, 0135, 0136, 0137, 0138, 0139, 0140, 0141, 0142, 0143, 0144, 0145, 0146, 0147, 0148, 0149, 0150, 0151, 0152, 0153, 0154, 0155, 0156, 0157, 0158, 0159, 0160, 0161, 0162, 0163, 0164, 0165, 0166, 0167, 0168, 0169, 0170, 0171, 0172, 0173, 0174, 0175, 0176, 0177, 0178, 0179, 0180, 0181, 0182, 0183, 0184, 0185, 0186, 0187, 0188, 0189, 0190, 0191, 0192, 0193, 0194, 0195, 0196, 0197, 0198, 0199, 0200, 0201, 0202, 0203, 0204, 0205, 0206, 0207, 0208, 0209, 0210, 0211, 0212, 0213, 0214, 0215, 0216, 0217, 0218, 0219, 0220, 0221, 0222, 0223, 0224, 0225, 0226, 0227, 0228, 0229, 0230, 0231, 0232, 0233, 0234, 0235, 0236, 0237, 0238, 0239, 0240, 0241, 0242, 0243, 0244, 0245, 0246, 0247, 0248, 0249, 0250, 0251, 0252, 0253, 0254, 0255, 0256, 0257, 0258, 0259, 0260, 0261, 0262, 0263, 0264, 0265, 0266, 0267, 0268, 0269, 0270, 0271, 0272, 0273, 0274, 0275, 0276, 0277.
- **Tier 3 — Phase 4+.** Written as we approach mobile, billing, i18n, marketplace, ML, FinOps, forensics, and multi-region active-active work. ADRs 0019, 0021, 0022, 0026, 0029, 0032, 0033, 0035.

## Statuses

- **Proposed** — drafted, in review, not yet binding.
- **Accepted** — adopted; constrains design.
- **Superseded by ADR-XXXX** — a later ADR replaces this.
- **Deprecated** — no longer applies, no successor.

## Batching plan

The 25 initial + 12 follow-up ADRs are written in 11 review-sized batches across multiple sessions:

| Batch | ADRs | Approx. words | Status |
|---|---|---|---|
| 1 | vision.md + 0001 + 0024 | 14,000 | Drafted |
| 2 | 0002 + 0003 | 10,000 | Drafted |
| 3 | 0004 + 0005 | 12,000 | Drafted |
| 4 | 0008 + 0009 + 0018 | 9,400 | Drafted |
| 5 | 0006 + 0007 + 0010 + 0011 | 10,200 | Drafted |
| 6 | 0012 + 0013 + 0014 + 0015 | 10,200 | Drafted |
| 7 | 0016 + 0017 + 0020 + 0023 + 0025 | 12,200 | Drafted |
| 8 | 0019 + 0021 + 0022 | 7,400 | Drafted |
| 9 | 0026 + 0027 + 0028 + 0029 + 0030 + 0031 + 0033 + 0035 + 0036 + 0037 | 18,000 | Drafted (backfilled from shipped packages) |
| 10 | 0032 | 2,000 | Drafted alongside `@crossengin/active-active` package |
| 11 | 0034 | 2,000 | Drafted alongside `@crossengin/sdk-clients` package |
| 12 | 0038 | 2,000 | Drafted alongside `@crossengin/sso` package |
| 13 | 0039 | 2,000 | Drafted alongside `@crossengin/notifications` package |
| 14 | 0040 | 2,000 | Drafted alongside `@crossengin/access-reviews` package |
| 15 | 0041 | 2,000 | Drafted alongside `@crossengin/workflow-engine` package |
| 16 | 0042 | 2,000 | Drafted alongside `@crossengin/data-lineage` package |
| 17 | 0043 | 2,000 | Drafted alongside `@crossengin/rate-limiting` package |
| 18 | 0044 | 2,000 | Drafted alongside `@crossengin/api-gateway` package |
| 19 | 0045 | 2,000 | Drafted alongside `@crossengin/feature-flags` package |
| 20 | 0046 | 3,500 | Phase 2 implementation plan — bridge from contracts to runtime |
| 21 | 0047 | 3,500 | Drafted alongside `@crossengin/kernel-pg` package (Phase 2 M1) |
| 22 | 0048 | 3,500 | Drafted alongside `@crossengin/crypto` package (Phase 2 M2) |
| 23 | 0049 | 4,000 | Drafted alongside `@crossengin/workflow-runtime` package (Phase 2 M3) |
| 24 | 0050 | 4,000 | Drafted alongside `@crossengin/api-gateway-runtime` package (Phase 2 M4) |
| 25 | 0051 | 4,000 | Drafted alongside `apps/architect-cli` (Phase 2 M5) |
| 26 | 0052 | 4,000 | Drafted alongside `@crossengin/workflow-signal-bridge` package (Phase 2 M6) |
| 27 | 0053 | 4,000 | Drafted alongside `@crossengin/ai-providers-anthropic` package (Phase 2 M2.7) |
| 28 | 0054 | 3,500 | Drafted alongside `architect-cli` chat mode (Phase 2 M5.5) |
| 29 | 0055 | 4,000 | Drafted alongside `architect-cli` tool-driven chat (Phase 2 M5.6) |
| 30 | 0056 | 4,000 | Drafted alongside `architect-cli` write tools (Phase 2 M5.8) |
| 31 | 0057 | 4,500 | Drafted alongside `@crossengin/ai-architect-pg` (Phase 2 M5.7) |
| 32 | 0058 | 4,500 | Drafted alongside `@crossengin/pack-erp-core` (Phase 2 M7) |
| 33 | 0059 | 4,500 | Drafted alongside `@crossengin/ai-router` (Phase 2 M6.5) |
| 34 | 0060 | 4,000 | Drafted alongside `@crossengin/ai-providers-openai` (Phase 2 M2.8) |
| 35 | 0061 | 3,500 | Drafted alongside `architect-cli` router integration (Phase 2 M6.5.5) |
| 36 | 0062 | 3,500 | Drafted alongside OpenAI Responses API support (Phase 2 M2.8.5) |
| 37 | 0063 | 2,500 | Drafted alongside CLI `--pack` apply (Phase 2 M7-wire) |
| 38 | 0064 | 3,000 | Drafted alongside pack tenant scoping (Phase 2 M7.7) |
| 39 | 0065 | 3,500 | Drafted alongside `@crossengin/pack-erp-payments` (Phase 2 M7.5) |
| 40 | 0066 | 3,000 | Drafted alongside payment signal-bridge wiring (Phase 2 M7.8) |
| 41 | 0067 | 3,000 | Drafted alongside CLI `sessions` subcommands (Phase 2 M5.9) |
| 42 | 0068 | 2,500 | Drafted alongside kernel `extends` resolver wiring (Phase 2 M7.6.5) |
| 43 | 0069 | 3,500 | Drafted alongside CLI `gateway start` binding (Phase 2 M4.7) |
| 44 | 0070 | 3,500 | Drafted alongside `@crossengin/pack-erp-healthcare` (Phase 2 M7.9) |
| 45 | 0071 | 4,000 | Drafted alongside `@crossengin/ai-providers-bedrock` (Phase 2 M2.9) |
| 46 | 0072 | 3,000 | Drafted alongside Bedrock Titan + Cohere embeddings (Phase 2 M2.9.5) |
| 47 | 0073 | 2,500 | Drafted alongside architect-cli Bedrock integration (Phase 2 M6.5.6) |
| 48 | 0074 | 3,000 | Drafted alongside gateway JWT auth + routes subcommand (Phase 2 M4.7.5) |
| 49 | 0075 | 3,000 | Drafted alongside URL-fetched JWKS + hot-reload (Phase 2 M4.7.6) |
| 50 | 0076 | 2,500 | Drafted alongside Bedrock cacheControl + Titan parallelism (Phase 2 M2.9.6) |
| 51 | 0077 | 3,000 | Drafted alongside Bedrock multimodal embeddings + image content blocks (Phase 2 M2.9.7) |
| 52 | 0078 | 3,500 | Drafted alongside kernel LlmMessage.attachments + vision capability (Phase 2 M2.X) |
| 53 | 0079 | 3,500 | Drafted alongside gateway routes from pack manifest (Phase 2 M4.8) |
| 54 | 0080 | 2,500 | Drafted alongside gateway routes unregister-pack (Phase 2 M4.8.x) |
| 55 | 0081 | 3,000 | Drafted alongside gateway routes sync-pack (Phase 2 M4.8.y) |
| 56 | 0082 | 3,500 | Drafted alongside routes.source_pack column (Phase 2 M4.10) |
| 57 | 0083 | 2,500 | Drafted alongside unregister-pack --by-source-pack (Phase 2 M4.10.x) |
| 58 | 0084 | 3,000 | Drafted alongside Bedrock Guardrails integration (Phase 2 M2.9.8) |
| 59 | 0085 | 2,500 | Drafted alongside Bedrock per-request guardrail override (Phase 2 M2.9.8.x) |
| 60 | 0086 | 3,000 | Drafted alongside OpenAI + Anthropic moderation surfaces (Phase 2 M2.X.6) |
| 61 | 0087 | 2,500 | Drafted alongside cross-provider moderation helper (Phase 2 M2.X.6.x) |
| 62 | 0088 | 3,500 | Drafted alongside kernel LlmMessage.content discriminated union (Phase 2 M2.X.5) |
| 63 | 0089 | 3,500 | Drafted alongside tool_use + tool_result content blocks (Phase 2 M2.X.5.x) |
| 64 | 0090 | 2,500 | Drafted alongside cross-provider retryable helper (Phase 2 M2.X.7) |
| 65 | 0091 | 3,000 | Drafted alongside router uses kernel cross-provider helpers (Phase 2 M6.6) |
| 66 | 0092 | 3,000 | Drafted alongside standalone OpenAI Moderations API (Phase 2 M2.X.8) |
| 67 | 0093 | 2,500 | Drafted alongside OpenAI Responses API image inputs (Phase 2 M2.8.6) |
| 68 | 0094 | 2,500 | Drafted alongside ImageUrlContentBlock URL variant (Phase 2 M2.X.5.y) |
| 69 | 0095 | 2,500 | Drafted alongside cross-provider input-too-large helper (Phase 2 M2.X.9) |
| 70 | 0096 | 2,000 | Drafted alongside Anthropic URL-source image support (Phase 2 M2.X.5.z) |
| 71 | 0097 | 2,500 | Drafted alongside DocumentContentBlock PDF inputs (Phase 2 M2.X.5.aa) |
| 72 | 0098 | 2,500 | Drafted alongside DocumentUrlContentBlock URL variant (Phase 2 M2.X.5.aa.y) |
| 73 | 0099 | 2,500 | Drafted alongside document format expansion txt/md/csv (Phase 2 M2.X.5.aa.x) |
| 74 | 0100 | 2,500 | Drafted alongside office document format expansion (Phase 2 M2.X.5.aa.x.1) |
| 75 | 0101 | 2,500 | Drafted alongside LlmMessage.name enforcement + OpenAI threading (Phase 2 M2.X.10) |
| 76 | 0102 | 3,000 | Drafted alongside OpenAI Files API integration (Phase 2 M2.X.5.aa.z) |
| 77 | 0103 | 3,000 | Drafted alongside Anthropic Files API integration (Phase 2 M2.X.5.aa.z.1) |
| 78 | 0104 | 2,500 | Drafted alongside Files API listFiles() (Phase 2 M2.X.5.aa.z.2) |
| 79 | 0105 | 3,000 | Drafted alongside Bedrock batch inference listBatches (Phase 2 M2.X.5.aa.z.3) |
| 80 | 0106 | 2,500 | Drafted alongside Bedrock batch inference getBatch (Phase 2 M2.X.5.aa.z.4) |
| 81 | 0107 | 2,500 | Drafted alongside Bedrock batch inference stopBatch (Phase 2 M2.X.5.aa.z.5) |
| 82 | 0108 | 3,500 | Drafted alongside Bedrock batch inference createBatch (Phase 2 M2.X.5.aa.z.6) |
| 83 | 0109 | 2,500 | Drafted alongside Bedrock listGuardrails second control-plane enumeration (Phase 2 M2.X.5.aa.z.7) |
| 84 | 0110 | 3,000 | Drafted alongside Bedrock getGuardrail policy detail (Phase 2 M2.X.5.aa.z.8) |
| 85 | 0111 | 2,500 | Drafted alongside Bedrock listInferenceProfiles third control-plane enumeration (Phase 2 M2.X.5.aa.z.9) |
| 86 | 0112 | 2,000 | Drafted alongside Bedrock getInferenceProfile (Phase 2 M2.X.5.aa.z.10) |
| 87 | 0113 | 2,500 | Drafted alongside Bedrock listImportedModels fourth control-plane enumeration (Phase 2 M2.X.5.aa.z.11) |
| 88 | 0114 | 2,500 | Drafted alongside Bedrock getImportedModel data-source provenance (Phase 2 M2.X.5.aa.z.12) |
| 89 | 0115 | 3,000 | Drafted alongside Bedrock listCustomModels fifth control-plane enumeration (Phase 2 M2.X.5.aa.z.13) |
| 90 | 0116 | 3,000 | Drafted alongside Bedrock getCustomModel training/validation detail (Phase 2 M2.X.5.aa.z.14) |
| 91 | 0117 | 2,500 | Drafted alongside Bedrock listModelImportJobs sixth control-plane enumeration (Phase 2 M2.X.5.aa.z.15) |
| 92 | 0118 | 3,000 | Drafted alongside conflict_error kernel kind + isConflictError classifier (Phase 2 M2.X.12) |
| 93 | 0119 | 3,000 | Drafted alongside chat REPL widens to LlmContentBlock[] (Phase 2 M5.10.5) |
| 94 | 0120 | 4,000 | Drafted alongside workflow instrumentation hooks + META_WORKFLOW_TRACES (Phase 2 M8) |
| 95 | 0121 | 2,500 | Drafted alongside Bedrock getModelImportJob import-job detail (Phase 2 M2.X.5.aa.z.16) |
| 96 | 0122 | 2,500 | Drafted alongside Bedrock listModelCustomizationJobs seventh control-plane enumeration (Phase 2 M2.X.5.aa.z.17) |
| 97 | 0123 | 3,000 | Drafted alongside Bedrock getModelCustomizationJob training/validation detail (Phase 2 M2.X.5.aa.z.18) |
| 98 | 0124 | 2,000 | Drafted alongside Bedrock stopModelCustomizationJob (Phase 2 M2.X.5.aa.z.19) |
| 99 | 0125 | 2,500 | Drafted alongside cacheBreakpoint LlmContentBlock field + Anthropic translator (Phase 2 M2.X.11) |
| 100 | 0126 | 2,000 | Drafted alongside Bedrock cachePoint translator wiring (Phase 2 M2.X.11.x) |
| 101 | 0127 | 2,500 | Drafted alongside not_found_error 5th cross-provider classifier (Phase 2 M2.X.13) |
| 102 | 0128 | 2,500 | Drafted alongside authentication_error 6th cross-provider classifier (Phase 2 M2.X.14) |
| 103 | 0129 | 2,500 | Drafted alongside permission_error 7th cross-provider classifier (Phase 2 M2.X.15) |
| 104 | 0130 | 3,000 | Drafted alongside invalid_request_error 8th cross-provider classifier — canonical 4xx sweep complete (Phase 2 M2.X.16) |
| 105 | 0131 | 3,500 | Drafted alongside Bedrock createModelCustomizationJob programmatic fine-tunes (Phase 2 M2.X.5.aa.z.20) |
| 106 | 0132 | 3,000 | Drafted alongside workflow activity execution instrumentation (Phase 2 M8.1) |
| 107 | 0133 | 2,000 | Drafted alongside ai-router isConflictError short-circuit (Phase 2 M6.6.x) |
| 108 | 0134 | 2,000 | Drafted alongside ai-router isNotFoundError short-circuit (Phase 2 M6.6.y) |
| 109 | 0135 | 3,500 | Drafted alongside `@crossengin/ai-router-pg` package PostgresCostTracker (Phase 2 M6.7) |
| 110 | 0136 | 3,000 | Drafted alongside Bedrock DELETE control-plane surfaces — first three deletes (Phase 2 M2.X.5.aa.z.21) |
| 111 | 0137 | 3,500 | Drafted alongside META_LLM_COST_CEILINGS + PostgresCostCeilingResolver per-tenant ceiling (Phase 2 M6.7.x) |
| 112 | 0138 | 2,500 | Drafted alongside Bedrock deleteInferenceProfile with system-profile guard (Phase 2 M2.X.5.aa.z.22) |
| 113 | 0139 | 3,500 | Drafted alongside chat REPL `--max-cost-usd` session budget flag (Phase 2 M5.11) |
| 114 | 0140 | 4,000 | Drafted alongside PostgresLatencyTracker + LatencyTracker async-ification (Phase 2 M6.7.y) |
| 115 | 0141 | 4,500 | Drafted alongside RouterInstrumentation + META_LLM_CALL_TRACES + PostgresRouterInstrumentation (Phase 2 M6.7.z) |
| 116 | 0142 | 3,500 | Drafted alongside Bedrock createInferenceProfile (APPLICATION-only via copyFrom) (Phase 2 M2.X.5.aa.z.23) |
| 117 | 0143 | 4,000 | Drafted alongside META_RETENTION_POLICIES + PostgresTraceRetention cross-cutting trace retention (Phase 2 M6.7.zz) |
| 118 | 0144 | 4,000 | Drafted alongside META_LLM_COST_TIERS + tier memberships per-tenant tier substrate (Phase 2 M6.8) |
| 119 | 0145 | 4,000 | Drafted alongside Bedrock cross-resource tagging — tagResource + untagResource + listTagsForResource (Phase 2 M2.X.5.aa.z.24) |
| 120 | 0146 | 3,500 | Drafted alongside Bedrock updateInferenceProfile PATCH with APPLICATION-only guard (Phase 2 M2.X.5.aa.z.25) |
| 121 | 0147 | 4,000 | Drafted alongside Bedrock provisioned-throughput inspection — get + list (Phase 2 M2.X.5.aa.z.26) |
| 122 | 0148 | 4,500 | Drafted alongside Bedrock createProvisionedModelThroughput first PT mutation (Phase 2 M2.X.5.aa.z.27) |
| 123 | 0149 | 3,500 | Drafted alongside Bedrock updateProvisionedModelThroughput PATCH for model migration + rename (Phase 2 M2.X.5.aa.z.28) |
| 124 | 0150 | 3,000 | Drafted alongside Bedrock deleteProvisionedModelThroughput completes PT lifecycle (Phase 2 M2.X.5.aa.z.29) |
| 125 | 0151 | 4,000 | Drafted alongside Bedrock foundation model discovery — get + list (Phase 2 M2.X.5.aa.z.30) |
| 126 | 0152 | 4,000 | Drafted alongside RouterInstrumentation extends to embed() path (Phase 2 M6.7.z.embed) |
| 127 | 0153 | 3,500 | Drafted alongside PostgresTraceRetention.previewPrune dry-run (Phase 2 M6.7.zz.dry-run) |
| 128 | 0154 | 3,500 | Drafted alongside PostgresCostCeilingResolver.resolveDetailed source attribution (Phase 2 M6.8.x) |
| 129 | 0155 | 4,000 | Drafted alongside META_TENANT_RETENTION_POLICIES per-tenant retention overrides (Phase 2 M6.7.zz.tenant) |
| 130 | 0156 | 3,500 | Drafted alongside Workflow timer_set + timer_cancelled instrumentation (Phase 2 M8.2) |
| 131 | 0157 | 4,000 | Drafted alongside ceiling_resolved RouterInstrumentation event + getTenantCostCeilingDetailed (Phase 2 M6.8.x.trace) |
| 132 | 0158 | 3,500 | Drafted alongside Bedrock setExactTags operator helper (Phase 2 M6.8.y) |
| 133 | 0255 | 3,500 | Drafted alongside Workflow `submitManualAction` driver — closes the manual_action trigger vocabulary gap (Phase 2 M8.7) |
| 134 | 0256 | 3,500 | Drafted alongside Workflow `validateDefinition` pre-publish validator — catches dead-end + unreachable + unknown-action-ref bugs at authoring time (Phase 2 M8.8) |
| 135 | 0257 | 3,500 | Drafted alongside `crossengin workflow validate <def.json>` CLI gate exposing ADR-0256 (Phase 2 M8.8.x) |
| 136 | 0258 | 3,500 | Drafted alongside gateway pipeline-execution retention — adds META_GATEWAY_PIPELINE_EXECUTIONS as 5th prunable table with nullableTenantId flag for null-tenant platform rows (Phase 2 M4.11) |
| 137 | 0259 | 3,500 | Drafted alongside `crossengin gateway prune-idempotency [--dry-run]` CLI + `PostgresIdempotencyStore.previewDeleteExpired` adapter (Phase 2 M4.12) |
| 138 | 0260 | 3,500 | Drafted alongside gateway prune-idempotency scope flags — --operation-id / --method / --limit with subquery LIMIT pattern (Phase 2 M4.13) |
| 139 | 0261 | 3,500 | Drafted alongside workspace-wide coverage gate completing the static-quality quartet — @vitest/coverage-v8 + preset thresholds + CI step (Phase 2 M-maint.coverage) |
| 140 | 0262 | 3,000 | Drafted alongside rate-limit-decisions retention — 6th prunable table reusing M4.11's nullableTenantId flag (Phase 2 M4.11.x) |
| 141 | 0263 | 3,500 | Drafted alongside `crossengin gateway housekeeping` unified dashboard (Phase 2 M4.14) |
| 142 | 0264 | 4,000 | Drafted alongside `crossengin retention housekeeping` unified dashboard — substrate-centric view across all 6 PRUNABLE_TABLES (Phase 2 M4.14.x) |
| 143 | 0265 | 4,000 | Drafted alongside housekeeping `--watch` mode — shared loop for incident-room monitoring across gateway + retention dashboards (Phase 2 M4.14.w) |
| 144 | 0266 | 4,500 | Drafted alongside housekeeping `--threshold-alert` CI-gate flag — exit-3 on tripped threshold; numeric + duration + ISO 8601 values; composes with --watch (Phase 2 M4.14.t) |
| 145 | 0267 | 3,500 | Drafted alongside housekeeping `--watch-keep-going` resilient watch mode — error tolerance + sticky trip tracking for long-running incident dashboards (Phase 2 M4.14.s) |
| 146 | 0268 | 3,200 | Drafted alongside housekeeping `--watch` SIGINT bridge — Ctrl-C exits cleanly with PG close instead of Node default 130 (Phase 2 M4.14.r) |
| 147 | 0269 | 3,800 | Drafted alongside retention housekeeping `--tenant <uuid>` drill-down filter — per-table override surfaces while aggregates stay cross-tenant for mental-model continuity (Phase 2 M4.14.u) |
| 148 | 0270 | 3,200 | Drafted alongside gateway housekeeping `--tenant <uuid>` drill-down filter — mirrors ADR-0269's drill-down on the operator-domain dashboard with explicit "(not applicable — expires_at-managed)" placeholder for the idempotency table (Phase 2 M4.14.v) |
| 149 | 0271 | 3,500 | Drafted alongside housekeeping `--all-tenants` matrix mode — cross-cutting drill-down on BOTH dashboards with long-format per-table sorted-by-tenantId override arrays; closes ADR-0269 Q2 + ADR-0270 Q2 (Phase 2 M4.14.q) |
| 150 | 0272 | 3,000 | Drafted alongside housekeeping `--watch` SIGTERM bridge alongside SIGINT — extends M4.14.r bridge for Kubernetes/systemd graceful shutdown; closes ADR-0268 Q1 (Phase 2 M4.14.p) |
| 151 | 0273 | 3,500 | Drafted alongside housekeeping `--tenant <uuid\|slug>` via meta.tenants lookup — eliminates UUID copy-paste friction during interactive debugging while preserving scripted-caller performance; closes ADR-0269 Q3 + ADR-0270 Q3 + ADR-0271 Q3 (Phase 2 M4.14.o) |
| 152 | 0274 | 4,000 | Drafted alongside housekeeping `--threshold-alert` compound AND/OR expressions — AND within a single flag for "trip when BOTH match" semantic, OR as syntactic convenience equivalent to multiple flags, cross-flag OR preserved verbatim, mixed AND+OR rejected; closes ADR-0266 Q1 (Phase 2 M4.14.n) |
| 153 | 0275 | 3,200 | Drafted alongside `--tenant <uuid\|slug>` on retention list-policies / history / summary — wires M4.14.o resolveTenantIdentifier helper into all three remaining retention actions accepting --tenant; --all-tenants deliberately NOT added (omitting --tenant already produces "all tenants"); closes ADR-0273 Q5 (Phase 2 M4.14.m) |
| 154 | 0276 | 3,800 | Drafted alongside cross-dashboard `crossengin tenant housekeeping` combined view — new top-level tenant subcommand runs BOTH gateway + retention housekeeping under one envelope; single PG conn, single slug resolution, threshold-alerts across union of all tables; closes ADR-0270 Q7 (Phase 2 M4.14.l) |
| 155 | 0277 | 4,000 | Drafted alongside `crossengin tenants` standalone subcommand (list + resolve) — new top-level tenants (plural) subcommand for collection-level tenant operations distinct from per-tenant `tenant` (singular); `tenants list` enumerates from meta.tenants with status / table-filter / has-overrides filters via EXISTS subqueries; `tenants resolve <slug\|uuid>` is the one-shot slug→UUID helper for shell scripting (UUID-only human output enables `xargs` composition without jq); closes ADR-0273 Q3 + ADR-0275 Q6 (Phase 2 M4.14.k) |
| 156 | 0278 | 3,500 | Drafted alongside auto-suggest similar slugs on `resolveTenantIdentifier` failure — Levenshtein-distance match against `meta.tenants.slug` when lookup fails, error becomes `no tenant with slug 'foo' — did you mean 'foo-prod', 'foo-bar'?` across all 7 callers without per-callsite changes; distance ≤ 2 threshold matches git/npm convention; max 3 suggestions sorted by distance then alphabetically; pure CLI shared helper; closes ADR-0273 Q6 + ADR-0275 Q3 + ADR-0276 Q8 + ADR-0277 Q6 (Phase 2 M4.14.j) |
| 157 | 0279 | 3,500 | Drafted alongside `crossengin tenants get <slug\|uuid>` — third action on the tenants namespace returning the full 11-field TenantRowFull (id + slug + name + status + tier + region + schema_name + residency + search_locale + created_at + updated_at); resolves slug→UUID via resolveTenantIdentifier (inherits M4.14.j "did you mean" suggestions on slug typos); unknown-UUID surfaces as distinct `no tenant with id` error so operators can tell typo-input from absent-data apart; SQL `to_char(... AT TIME ZONE 'UTC')` pins timestamp format across environments; closes ADR-0277 Q1 (Phase 2 M4.14.i) |
| 158 | 0280 | 3,500 | Drafted alongside `crossengin tenant policies <slug\|uuid>` — second action on the singular `tenant` namespace aggregating per-tenant policies across three substrate axes (retention overrides from meta.tenant_retention_policies + cost-ceiling override from meta.llm_cost_ceilings + tier membership from meta.llm_tenant_tier_memberships JOIN meta.llm_cost_tiers); three concurrent queries under one envelope; NUMERIC(18,8) preserved as strings to avoid JS precision loss; rate-limit per-tenant overrides deliberately excluded from v1 since no substrate exists for them yet; closes ADR-0276 Q3 + ADR-0277 Q2 (Phase 2 M4.14.h) |
| 159 | 0281 | 3,500 | Drafted alongside `tenant policies --effective` flag — precedence-resolved view (override→tier→none walk) appended as 4th block; pure client-side derivation from already-fetched axes via `deriveEffectivePolicy` helper (no extra PG query); TypeScript discriminated union mirrors PostgresCostCeilingResolver.resolveDetailed contract; backward compatible (opt-in via flag); `source: "none"` has no ceiling field — substrate has nothing to say, runtime falls back to router-level global; closes ADR-0280 Q2 + ADR-0154 Q1 (Phase 2 M4.14.g) |
| 160 | 0282 | 3,500 | Drafted alongside `tenant policies --diff <other-slug|uuid>` flag — side-by-side comparison across the three policy axes (retention per-table, costCeiling, tier); pure client-side `computePolicyFieldDiffs` over two already-fetched reports (no SQL-side diff); both tenants resolved concurrently with independent "did you mean" suggestions per side; left/right qualifier in error messages identifies which side failed; self-diff guard exits 2 fast (typo prevention); `exists` diffs collapse N undefined→value lines into one; tier diffs only fire on tierId mismatch (tier policies intrinsic to tier, identical by JOIN when tierIds match); composes with --effective (both sides populate effective field independently); CI integration via --exit-on-divergence + --threshold mirrors retention diff convention (exit 3 when fieldDiffs.length ≥ threshold); closes ADR-0280 Q5 (Phase 2 M4.14.f) |
| 161 | 0283 | 3,500 | Drafted alongside `tenant policies --explain` flag — what-if precedence walk surfacing `withoutOverride` (what the walk yields with override stripped) + `withoutTier` (with tier stripped) as a `TenantPolicyExplain` block; pure client-side composition via `deriveExplainView` calling `deriveEffectivePolicy` twice with each input null (zero parallel implementation, zero drift risk vs the ADR-0281 effective walk); `--explain` implies `--effective` (the baseline comparison is needed for the what-if to make sense — forcing both flags is friction); mutually exclusive with `--diff` in v1 (rejected with clear error; combining muddles which side gets explained); operators audit override redundancy by comparing `effective` to `explain.withoutOverride` (same → override can be cleared; different → override is shadowing the tier); closes ADR-0281 Q1 (Phase 2 M4.14.e) |
| 162 | 0284 | 3,500 | Drafted alongside `tenant housekeeping --watch` flag — combines two independent watch loops (gateway + retention housekeeping) under one cross-dashboard view via atomic per-tick gather (Promise.all both reports → single render → eliminates the interleaved-render layout garbling concern from ADR-0276 Q1); reuses the shared housekeeping-watch.ts infrastructure (parseWatchFlags / runHousekeepingWatchLoop / installShutdownBridge); single PG connection across all ticks; NDJSON-of-envelopes under --format json (no ANSI clears that would break log aggregators); SIGINT + SIGTERM bridge drains cleanly; threshold-alert evaluation across union of tables drives exit-3 semantic (immediate halt by default, sticky-tracked under --watch-keep-going); closes ADR-0265 Q1 + ADR-0276 Q1 (Phase 2 M4.14.d) |
| 163 | 0285 | 3,400 | Drafted alongside `tenant policies --format csv\|tsv` flag — single-tenant emits long-format one-row-per-axis schema with wide 17-column header (retention rows sorted by tableName, then cost_ceiling, tier, effective under --effective, explain.without_override + explain.without_tier under --explain); --diff emits one-row-per-fieldDiff with tenant_a/tenant_b columns and header-only when policies match (still valid CSV for spreadsheet workflows); NUMERIC(18,8) ceiling values preserved as strings to survive Excel/pandas round-trips without float widening; --csv-separator flag reuses retention.ts pattern (rejects '"' and newline); composes with --effective + --explain + --exit-on-divergence + --threshold; closes ADR-0280 Q6 + ADR-0282 Q4 (Phase 2 M4.14.c) |
| 164 | 0286 | 3,500 | Drafted alongside `tenant policies --vs-tier <tier-id>` flag — synthetic-RHS comparison constructing a fake right-hand-side from the SAME tenant with same retention + same cost-ceiling override but tier replaced via meta.llm_cost_tiers lookup; operators preview tier-change impact before committing membership update via "what would change if I moved this tenant to <tier-id>?"; pair with --effective to surface canonical "your override is doing all the work" finding (both sides show effective.source=override + identical ceilings when per-tenant override shadows tier); RHS uses `vs-tier:<tier-id>` input marker so human + CSV output makes synthetic nature obvious; no self-diff guard (vs-tier against current tier yields empty diff = useful "moving here changes nothing" answer not typo); mutually exclusive with --diff (both define RHS) and --explain (matches ADR-0283 rule); composes with --effective + --format + --exit-on-divergence; extracted emitDiffOutput helper for reuse between runTenantPoliciesDiff and runTenantPoliciesVsTier; gatherTierDefinition helper queries meta.llm_cost_tiers directly (not via membership); closes ADR-0282 Q2 + ADR-0283 Q3 (Phase 2 M4.14.b) |
| 165 | 0287 | 3,400 | Drafted alongside `tenant policies` N-way comparison (`--add-tenant` repeatable + repeated `--vs-tier`) — extends M4.14.f --diff and M4.14.b --vs-tier into N-way cohort/multi-tier comparisons; --add-tenant requires --diff (first RHS comes from --diff; additional RHSes via --add-tenant); --vs-tier accepts repetition for multi-tier preview matrix; envelope shape switches when N>1: action becomes `tenant.policies.diff.multi` with `anchor` + `comparisons[]` array (single-comparison preserves M4.14.f shape for backward compat); CSV adds `comparison_index` column 0..N-1; human render emits one "=== Comparison i/N ===" section per pair using existing renderPoliciesDiffHuman; max-divergence exit code (any comparison's fieldDiffs.length ≥ threshold trips exit 3); duplicate-target guards (multiple RHS slots resolving to same tenantId rejected; multiple --vs-tier slots with same tier-id rejected); --add-tenant + --vs-tier mutually exclusive (tenant-vs-tenant + tenant-vs-tier can't mix in one envelope); closes ADR-0282 Q1 + ADR-0286 Q1 (Phase 2 M4.14.a) |
| 166 | 0288 | 2,400 | Drafted alongside `tenant housekeeping --diff <other-uuid\|slug>` pair-wise dashboard comparison — extends the housekeeping cross-substrate view (M4.14.l) with side-by-side diff semantics; requires --tenant (LHS comes from existing --tenant filter; --diff supplies RHS); 4 reports gathered concurrently via Promise.all (gateway×2 + retention×2); diff focuses on per-table tenantPolicy fields across BOTH gateway + retention axes (global per-table stats excluded since tenant-agnostic under same PG snapshot — divergence would be race noise not policy difference); one-sided overrides surface compact `tenantPolicy.exists` diff (not N field-level diffs); BOTH sides with overrides emit per-field diffs (retentionDays, enabled, optOut, optOutReason, optOutUntil); JSON envelope action `tenant.housekeeping.diff` with left/right/fieldDiffs; human render mirrors policies --diff layout but [axis] tableName.field shape for per-table keying; mutually exclusive with --all-tenants (diff vs matrix don't compose), --watch (looped diff layouts garble), --threshold-alert (alert clauses target single-tenant view not pair-wise divergence); self-diff + unknown-RHS guards exit 2 BEFORE gather; --exit-on-divergence + --threshold integration via shared diffDivergenceExitCode helper; closes ADR-0284 Q5 (Phase 2 M4.15.a) |
| 167 | 0289 | 1,700 | Drafted alongside `tenants list --format csv\|tsv` bulk-export — adds CSV + TSV output to the existing `tenants list` JSON + human-table command (M4.14.k); 5-column row-per-tenant shape (id, slug, name, status, tier) matching the compact TenantRow shape (NOT TenantRowFull — that's the `tenants get` path; bulk-full export deferred to future `--format csv-full` flag); empty result still emits header row (matches retention/policies CSV convention; spreadsheet column-naming auto-population); --csv-separator <c> overrides comma default with validation rejecting `"` + newlines (inlined in runTenantsList; 4-line check stays local rather than extracted to format.ts); silently ignored under non-CSV formats matching retention precedent (avoids shell-alias friction); composes with all existing list filters (--status, --table-filter, --has-overrides) without change (filter logic in SQL WHERE clause; format selection happens after result set comes back); closes ADR-0285 Q4 (Phase 2 M4.15.b) |
| 168 | 0290 | 2,100 | Drafted alongside `tenant housekeeping --add-tenant` N-way pair-wise dashboard comparison — extends M4.15.a single-pair diff into N-way cohort comparison; --add-tenant requires --diff (first RHS from --diff; additional RHSes via repeated --add-tenant); envelope shape switches when N>1: action becomes `tenant.housekeeping.diff.multi` with `anchor` + `comparisons[]` array (single-comparison preserves M4.15.a shape for backward compat); 2*(1+N) reports gathered concurrently via Promise.all (gateway anchor + N RHSes + retention anchor + N RHSes); stable slice ordering gateway_anchor / gateway_rhs[0..N-1] / retention_anchor / retention_rhs[0..N-1]; max-divergence exit code (any comparison's fieldDiffs.length ≥ threshold trips exit 3 matching policies M4.14.a semantic ANY cohort drift trips gate); self-diff guard iterates every RHS slot with "right N" labeling when N>1 (preserves "right" label for N=1 M4.15.a backward compat); duplicate-RHS-UUID guard catches "appears in multiple RHS slots"; --add-tenant inherits --diff mutual-exclusivity rules from M4.15.a (--all-tenants/--watch/--threshold-alert); human render via renderHousekeepingMultiDiffHuman emits one "=== Comparison i/N ===" section per pair using existing renderHousekeepingDiffHuman shape; closes ADR-0288 Q1 (Phase 2 M4.15.c) |
| 169 | 0291 | 1,900 | Drafted alongside `tenant housekeeping --diff --format csv\|tsv` bulk-export — adds CSV/TSV output to M4.15.a single-pair AND M4.15.c N-way housekeeping diff; single-comparison emits 9-column shape (tenant_a_id, tenant_a_input, tenant_b_id, tenant_b_input, axis, table_name, field, value_a, value_b — the table_name column distinguishes from policies CSV which keys only by axis+field since housekeeping fieldDiffs are per-table by nature with both gateway + retention dashboards listing per-table rows); N-way prepends comparison_index column 0..N-1 at position 0 matching M4.14.a policies multi-CSV precedent; empty fieldDiffs still emits header-only valid CSV (matches retention/policies/tenants list convention; spreadsheet column-naming auto-population); --csv-separator <c> overrides comma default reusing existing validatePoliciesCsvSeparator helper (rejects `"` + newlines); silently ignored under non-CSV formats matching retention/policies precedent; new HOUSEKEEPING_DIFF_CSV_HEADERS constant + buildHousekeepingDiffCsvRows helper; CSV branch added to BOTH n===1 and n>1 paths in runTenantHousekeepingDiff each validating --csv-separator before emission; closes ADR-0288 Q2 + ADR-0285 Q5 (Phase 2 M4.15.d) |
| 170 | 0292 | 2,400 | Drafted alongside `--format gh-summary` Markdown output across the diff family — adds new format value emitting Markdown table suitable for $GITHUB_STEP_SUMMARY redirection from CI workflows; closes 5 deferred Qs simultaneously since each surface (policies --diff, policies --vs-tier, policies N-way, housekeeping --diff, housekeeping N-way) called for the same rendering shape; operators redirect via shell `crossengin <cmd> ... --format gh-summary >> "$GITHUB_STEP_SUMMARY"` (CLI does NOT read $GITHUB_STEP_SUMMARY directly to avoid GitHub-specific side effects entangling the CLI); single-comparison shape: ## header + Left/Right metadata + ### Field changes (N) table + verdict emoji footer (:white_check_mark: for no diffs, :warning: for divergence); multi-comparison wraps each pair in ### Comparison i/N section with own table then --- separator + **Summary:** footer with max-divergence; policies table is 4-col (Axis Field Left Right); housekeeping is 5-col (extra Table column since HousekeepingFieldDiff keys by axis+tableName+field); formatMdValue helper escapes pipes (\|) backticks (\`) and backslashes in string values + wraps every value in backticks for visual distinction; null/undefined renders as `null` for visibility (distinct from empty string); 4 new renderer helpers (renderPoliciesDiffGhSummary + renderPoliciesMultiDiffGhSummary + renderHousekeepingDiffGhSummary + renderHousekeepingMultiDiffGhSummary); branches added to emitDiffOutput / emitMultiDiffOutput / runTenantHousekeepingDiff (single + multi paths); --vs-tier picks up gh-summary automatically via shared emit helpers; closes ADR-0282 Q5 + ADR-0286 Q7 + ADR-0287 Q3 + ADR-0288 Q3 + ADR-0290 Q2 (Phase 2 M4.15.e) |
| 171 | 0293 | 1,800 | Drafted alongside `tenants list --format csv-full` 11-column bulk export — adds wider CSV variant emitting all TenantRowFull columns (id, slug, name, status, tier, region, schema_name, residency, search_locale, created_at, updated_at) matching the same columns as `tenants get` single-row output; complements the compact 5-column `--format csv` from M4.15.b without breaking backward compat (compact stays default); residency JSONB column serializes to compact JSON string for CSV cell embedding via JSON.stringify; printCsv quote-wrapping handles embedded commas + quotes automatically (`"{""primary"":""us-east-1""}"`); null residency renders as empty CSV cell; timestamps emitted in ISO UTC format via to_char (same shape as `tenants get`); new buildListQueryFull mirrors buildListQuery structure with wider SELECT + ISO timestamp formatting; filter logic (--status, --table-filter, --has-overrides) composes identically; csv-full format value added to OUTPUT_FORMATS + parser whitelist (cli.ts); honored by `tenants list` only — other surfaces fall through to human format matching gh-summary M4.15.e precedent; `csv-full` naming chosen over `csv2`/`csv-extended` (semantically clear "full version of CSV" without versioning baggage); closes ADR-0289 Q1 (Phase 2 M4.15.f) |
| 172 | 0294 | 1,600 | Drafted alongside `tenants list --include-policy-count` computed-column flag — adds opt-in `policy_count` column reporting per-tenant retention override count via LEFT JOIN against meta.tenant_retention_policies aggregated by tenant_id with COALESCE-to-0 for tenants with no overrides; ::int cast ensures pg.js returns JS number not string; composes with ALL format modes (json adds policy_count field per tenant, csv appends 6th column, csv-full appends 12th column, tsv appends 6th tab-separated column, human appends right-padded "policies" column with 8-char min width); LEFT JOIN subquery aggregates once (faster than per-row COUNT subquery for large lists); --has-overrides composes (both flags set returns only tenants with policy_count > 0 since EXISTS gates inclusion); --status + --table-filter compose identically (WHERE clauses independent of the JOIN); opt-in flag avoids paying query cost when count not needed (~10ms typical at 10k tenants); name "policy_count" specific to retention overrides (NOT cost-ceiling or other override types — documented in CLI help); closes ADR-0289 Q2 + ADR-0293 Q1 (Phase 2 M4.15.g) |
| 173 | 0295 | 1,400 | Drafted alongside `tenant housekeeping --diff --axis gateway\|retention` substrate filter — narrows fieldDiffs to one substrate surface; post-processing step on computed fieldDiffs (no extra PG cost since gather already retrieves both axes); composes with all formats (json/csv/tsv/human/gh-summary), N-way --add-tenant (per-pair filter), and --exit-on-divergence (filtered count gates exit 3); strict value validation (--axis bogus exits 2 with "invalid --axis 'bogus' (expected 'gateway' or 'retention')"); axis values map directly to HousekeepingFieldDiff.axis field discriminator; positive-form phrasing ("show me only X") more common in audit workflows than --exclude-axis; rejected — server-side filter that skips non-matching gather (complicates orchestration for marginal benefit), allow multiple --axis flags (meaningless with 2 axes), --include-axis gateway,retention comma-separated (over-engineered), --exclude-axis (inverted phrasing less common), filter at compute layer inside computeHousekeepingFieldDiffs (would thread filter through pure helper; filtering output keeps helper axis-agnostic), default to all if unset value isn't gateway/retention (silently ignoring typos surprises), axis-prefixed CSV columns gateway_field/retention_field (axis-as-column is established pattern from M4.15.d); useful for substrate-scoped audit + substrate-scoped CI gates + incident triage scoping; closes ADR-0291 Q2 (Phase 2 M4.15.h) |
| 174 | 0296 | 1,900 | Drafted alongside `retention diff --format gh-summary` Markdown across 5 retention diff surfaces — extends M4.15.e gh-summary pattern to retention diff family closing ADR-0292 Q4; 5 separate renderers since result shapes differ: pair (Field/Tenant A/Tenant B), vs-platform (Field/Tenant/Platform), cross-table (Field/Table A/Table B), N-way pair (Field/Distinct values with inline grouping), cross-table N-way (same N-way shape with table-name labels); N-way renders use `value (label1, label2)` inline grouping since fieldVariations encodes "tenants matching this value" naturally and Markdown tables don't support nesting; verdict footer reflects per-surface semantic (Divergence/Override/Cross-table divergence/Variations detected); formatMdRetentionValue helper distinguishes undefined → `absent` from null → `null` (HistoryEntryFieldDiff uses both meaningfully — undefined means field doesn't exist on this side, null means explicitly null); reuses GitHub-flavored Markdown shortcode emojis (:white_check_mark:/:warning:); CSV/TSV for retention deferred since N-way distinct-values grouping needs column-per-tenant rather than row-per-field shape; closes ADR-0292 Q4 (Phase 2 M4.15.i) |
| 175 | 0297 | 1,400 | Drafted alongside `tenants get --format csv\|tsv\|csv-full` single-row export — extends M4.15.b list-csv + M4.15.f list-csv-full patterns to per-tenant fetch closing ADR-0293 Q3; csv/tsv (5 cols id/slug/name/status/tier matching `tenants list --format csv` exactly) + csv-full (11 cols TenantRowFull matching `tenants list --format csv-full` exactly with residency JSONB → compact JSON cell quoted by printCsv via RFC-4180 escape, search_locale, created_at/updated_at ISO strings via to_char); single-row = header + 1 data row; column-order parity with list-bulk is the critical operational property (per-tenant fetches concat'd via `tenants get <slug> --format csv-full \| tail -1 >> all.csv` onto bulk-list output without column realignment); --csv-separator honored on both csv variants (printCsv quotes any cell containing embedded `"` regardless of separator so residency stays quoted under `;`); null residency → empty cell (RFC-4180-ish convention matches pandas + Excel); slug→UUID resolve path unchanged (CSV output is presentation concern only — 2 queries for slug, 1 for UUID input); rejected — match human format's 9-col subset (breaks list/get concat workflows), --include-policy-count on get (operators have hypothetical `tenants list --filter slug=X` future surface), emit only data row no header (concat-friendly but breaks pandas read_csv standalone usage; defer --no-header to future Q), different column order timestamps first (breaks list/get column parity), special-case null residency to "null" string (printCsv's "" convention matches RFC-4180 + pandas + Excel), Markdown/gh-summary single-row (gh-summary is verdict format not data dump; single tenant row is wrong shape); useful for incident-context export + bulk + per-tenant concat audit workflows + pandas pipeline integration; closes ADR-0293 Q3 (Phase 2 M4.15.j) |
| 176 | 0298 | 1,600 | Drafted alongside `tenants list --min-policy-count N` server-side cohort filter — closes ADR-0294 Q4 deferred Q; WHERE clause on `COALESCE(pc.policy_count, 0) >= $N` reusing the M4.15.g LEFT JOIN infra; flag forces the JOIN even if --include-policy-count is off (WHERE references JOIN expression so JOIN must be present); when both flags set, only ONE JOIN exists in SQL (shared between SELECT column + WHERE clause via single subquery); N must be positive integer >= 1 (rejects 0/negative/non-numeric/floats with strict round-trip check String(parsed) !== input.trim() to reject `5.5` with explicit "integer-only" signal rather than parseInt silent truncation); composes with --status (AND'd, params indexed by insertion order), --table-filter (AND'd), --has-overrides (AND'd; redundant when N >= 1 but not in error), --format (all output paths receive filtered rows including csv-full via buildListQueryFull threading), --include-policy-count (composes: filtered AND column exposed); outer SELECT does NOT add COALESCE outer column unless --include-policy-count set (inner JOIN subquery has `COUNT(*)::int AS policy_count` unavoidably for outer COALESCE reference but invisible to callers); rejected — use HAVING (HAVING is for aggregates over GROUP BY; pre-aggregated JOIN exposes policy_count as regular column so WHERE is semantically correct), add `--max-policy-count` counterpart (useful for inverse cohort but defer until asked; common need is over-policied case), allow `--min-policy-count 0` as no-op (silently accepting widens results on typos; reject loudly), single `--policy-count <range>` flag accepting `>=5` syntax (operator-parseable ranges over-engineered; two flags compose naturally), mutual-exclude with --has-overrides (they overlap on N=1 but compose cleanly via AND), round-robin parseInt floats silently 5.5 → 5 (silent truncation hides typos like `5.5 policies`), accept --min-policy-count without value as shorthand for N=1 (conflates with --has-overrides + breaks flag-with-value parsing convention); drawbacks — N=0 rejection surprises scripts that conditionally set flag (gate in shell), no min-by-table for "tenants with >= 3 policies on workflow_traces" (future Q), filter applies to total override count not unique (table,axis) count (same row can have retention + costCeiling axis tweaks on same table inflating count; consistent with M4.15.g but possibly surprising); useful for cohort analysis + ranking + customization-density gates + tenant-tier audits; closes ADR-0294 Q4 (Phase 2 M4.15.k) |
| 177 | 0299 | 1,500 | Drafted alongside `tenant policies --diff --axis retention\|costCeiling\|tier` 3-axis substrate filter — extends M4.15.h housekeeping --axis pattern from 2-axis (gateway/retention) to 3-axis since PolicyFieldDiff has retention/costCeiling/tier; closes ADR-0295 Q1; post-compute filter on `fieldDiffs.filter((d) => d.axis === axis)` after computePolicyFieldDiffs runs (no extra PG cost since the 3 axis queries are already in parallel via Promise.all and each is sub-50ms; the post-filter is fine); applied in both emit paths emitDiffOutput pair-wise + emitMultiDiffOutput N-way (each comparison's fieldDiffs narrowed independently); validation rejects invalid values up-front with `invalid --axis '<value>' (expected one of: retention, costCeiling, tier)` before any PG round-trip (early-exit on typo'd axis values); composes with --exit-on-divergence --threshold N gating on FILTERED count (--axis retention --exit-on-divergence only trips exit 3 when retention-axis fields diverge), --add-tenant N-way per-comparison filter, --vs-tier synthetic-RHS same filter applies, all output formats json/csv/tsv/human/gh-summary emit filtered set; rejected — allow multiple --axis values --axis retention --axis costCeiling (doable but adds complexity and negative-filter case isn't addressed; with only 3 axes needing 2 of them is rare 2 separate runs cheap), --exclude-axis tier inverted phrasing (show-me-only-X more common in audit workflows than show-everything-except-X), filter at compute layer inside computePolicyFieldDiffs (post-filtering keeps helper axis-agnostic + testable in isolation), treat --axis bogus as no filter silent (silent typos surprise; loud error pattern from ADR-0291), server-side axis-specific gather skip costCeiling query when --axis retention (complicates orchestration for marginal benefit), pre-set --axis retention as default (surprising default that hides cost-ceiling + tier divergence in CI gates), use --substrate instead of --axis (housekeeping established the term; consistency matters more than slight imprecision); drawbacks — 3-axis ergonomics differ from housekeeping 2-axis costCeiling axis is singleton (one row per tenant or null) so its fieldDiffs are sparser than retention's per-table can look like filter is broken when 0/1 results, no --axis field-level filter operators wanting just tier.tierId vs full tier axis use jq, two-axis-parity issue housekeeping uses retention|gateway policies uses retention|costCeiling|tier (share "retention" literal but not others CI workflow scripting both can't use single variable); useful for substrate-scoped CI gates + substrate-scoped incident triage + axis-isolated cohort audits; closes ADR-0295 Q1 (Phase 2 M4.15.l) |
| 178 | 0300 | 1,400 | Drafted alongside `retention diff-history` + `retention diff-timeline --format gh-summary` Markdown rendering — closes ADR-0296 Q1 deferred ("same Markdown gh-summary pattern for retention diff-history + diff-timeline — these are different shapes history entries / timeline events so each needs its own Markdown renderer"); 2 new renderers scoped to common case (pair-wise diff-history + single-tenant diff-timeline pair); cross-table + N-way timeline variants deferred (3 more surfaces ship common case first see demand for rarer ones); diff-history Markdown shape mirrors M4.15.i pair-diff exactly since HistoryEntryFieldDiff is same shape as DiffTenantPoliciesResult fieldDiffs (## Diff: retention history events header + Tenant/Table metadata + Event A/B `<id>` @ `<timestamp>` (`<event_kind>`) [by <actor>] event-metadata lines + Field changes (N) table | Field | A | B | + verdict footer); reuses formatMdRetentionValue from M4.15.i for backtick-wrap + escaping + null/absent distinction; withActorNames opt adds "by <name>" suffix to event lines; diff-timeline Markdown shape is event-per-row table since timeline entries are log not pair-wise diff (## Timeline: retention history events header + Table metadata + Entries: N count + Events (N) table | Time | Side | Kind | Tenant [| Actor] | + pagination cursor blockquote footers > Next page: `--after-id <id>` / > Previous page: `--before-id <id>` for operators chaining pages from CI step output); empty states verdict-emoji-positive (diff-history empty → :white_check_mark: **No differences** both events captured same policy state, diff-timeline empty → :white_check_mark: **No events in window** timeline is empty); rejected — render timeline as pair-of-diffs one per entry (would duplicate prev_state/next_state per row; log-shape kind column more readable), add <details> block per timeline entry with full prev/next state (useful for deep forensics but gh-summary meant to be scannable at glance), use same compact shape for cross-table + N-way timeline now (defer until demand emerges; pair timeline is common case), add prev/next change column to timeline table (would require diffing prev vs next per row; operators wanting per-event diff use diff-history on that pair), use event_kind emoji ✏️ for retention_set 🚫 for opt_out_set (cute but non-accessible in text readers; kind text stays in backticks), inline pagination cursors as <a> anchor links to next CI step (CI step URLs aren't known at render time; --after-id shell command is correct abstraction); drawbacks — timeline rows can be many (limit default 50 per page renders OK but pushes step summary long), no prev/next state column in timeline gh-summary (operators wanting state changes per event drill in via diff-history; acceptable for audit-log consumers), tenant Side column is A/B only (cross-table case hasn't shipped table-side labels would be more meaningful there; defer), cross-table + N-way timeline not yet covered (3 more surfaces; same shape applies with label tweaks; defer to future M unless demand emerges); useful for audit-log CI gates + retention-modification alerts + history forensics CI step summaries; closes ADR-0296 Q1 (Phase 2 M4.15.m) |
| 179 | 0301 | 1,200 | Drafted alongside `retention diff-timeline --format gh-summary` for cross-table + N-way variants — closes ADR-0300 Q1 deferred; 2 new renderers (formatTimelineCrossTableDiffGhSummary, formatTimelineNwayDiffGhSummary) covering the last gh-summary gaps in the retention family; same event-per-row Markdown shape as M4.15.m pair-wise renderer but with bulleted label legend at top mapping per-row labels A/B/C/... back to actual table names (cross-table) or tenant IDs (N-way) since the per-row labelForIndex output is meaningless without the legend; column labels differ per variant (cross-table | Time | Table | Kind | Tenant [| Actor] | the Table column shows per-event tableLabel resolved via legend, N-way | Time | Tenant | Kind [| Actor] | the Tenant column shows per-event tenantLabel resolved via legend); cross-table also keeps the Tenant column showing the single tenant ID per row since header already states it would be redundant but kept for grep parity with pair-wise shape; empty-state verdict messages tailored per-variant (cross-table → "no history events for this tenant on any of these tables" N-way → "no history events for any of these tenants on this table") with bulleted legend still emitted on empty so operators see what was queried; pagination cursor blockquote footers identical to M4.15.m; rejected — combine cross-table + N-way into single renderer parameterized by "label kind" (header wording differs Multi-tenant vs Cross-table + column label differs Tenant vs Table; inlining both renderers is simpler), omit bulleted label legend to save space (per-row labels A/B/C/... meaningless without legend; two renders without legend would be hard to distinguish), use 2-column per-event row with full table/tenant name instead of label (would widen each row significantly UUIDs 36 chars; label + legend is compact), add Tenant column to cross-table render too (redundant every row has same tenant in cross-table case; header already states it), add prev/next state inline diff column (same forensics concern as M4.15.m; defer), use labels T1/T2/T3 instead of A/B/C (matches human render labelForIndex convention; consistency wins); drawbacks — bulleted legend duplicates table/tenant list from human render preamble (Markdown is the point; legend meant to be read), wide legends for many tables/tenants (e.g., N=10 legend + 50 event rows ~60 lines of step summary; acceptable CI step summaries scroll), same cross-table edge cases as M4.15.m pair (no prev/next inline no per-kind rendering inherited here); the retention family gh-summary coverage is now COMPLETE — every diff/timeline/history surface emits Markdown for $GITHUB_STEP_SUMMARY redirection; closes ADR-0300 Q1 (Phase 2 M4.15.n) |
| 180 | 0302 | 1,500 | Drafted alongside `retention diff --add-tenant/--add-table --format csv\|tsv` wide-format N-way export — closes ADR-0296 future Q ("CSV/TSV for retention N-way — distinct-values grouping benefits from column-per-tenant shape"); wide format = 1 row per field, 1 column per tenant (N-way per-tenant) or per table (cross-table N-way); column headers use full tenant UUID (or table name) prefixed with `value_` for self-documenting CSV without out-of-band legend; column order matches tenantIds[] / tableNames[] input order (anchor, RHS, --add chain); tenants matching same distinct-value group get same cell value (group lookup via .find((g) => g.labels.includes(label))); shared buildNwayCsvRows + serializeNwayCellValue helpers cover both N-way variants (per-tenant + cross-table per-table) — labels[] parameter encapsulates which dimension the columns represent; serializeNwayCellValue passes through strings/numbers/booleans, renders null/undefined as null cell (empty per printCsv contract), object/array → compact JSON.stringify (same convention as tenants list csv-full residency); --csv-separator honored with shared validateNwayCsvSeparator (rejects `"` + newline matching existing retention csv paths); degenerate input guard (label absent from all distinct-value groups → null cell) — shouldn't happen via computeFieldVariations but defensive; rejected — long format (tenant_id, field, value) more pandas-idiomatic but spreadsheet workflows prefer wide for at-a-glance comparison (operators wanting long can melt() in pandas), column headers use labels (value_A, value_B) + emit separate legend via stderr (out-of-band knowledge to map labels back; UUIDs in headers are verbose but self-documenting), include label as second header row (CSV doesn't support multi-row headers cleanly; pandas read_csv skiprows loses info), add --format csv-long variant for pandas case (adds surface to maintain; defer until asked), strip value_ prefix from headers (would conflict with `field` column if tenant slug matched `field`; prefix prevents collisions), emit CSV preamble comment line # tenants: uuid-a uuid-b ... (not a real CSV convention; most parsers error on it), include resolution source tenant/platform/tier as suffix in each cell (source field is itself a fieldVariation row so already in output if varies); drawbacks — header width scales with N (10 tenant UUIDs ~400+ char header; acceptable for spreadsheet + pandas), value_ prefix noisy on every column (workaround pandas df.columns.str.replace('value_', '')), cell values with embedded separators get quoted by printCsv per RFC-4180 (correct but invisible until cell contains separator), no long-format variant for pandas pivot workflows (defer), null cells vs empty strings (pandas read_csv treats both as NaN by default fine); useful for cohort comparison + spreadsheet workflows + pandas-pipeline integration without jq pivot; closes ADR-0296 future Q (Phase 2 M4.15.o) |
| 181 | 0303 | 1,400 | Drafted alongside `tenants list/get --no-header` CSV header suppression — closes ADR-0297 Q1 deferred ("--no-header flag for per-tenant fetches that will be concat'd onto list output"); shared `opts: { noHeader?: boolean }` parameter added to formatCsv/printCsv/formatTsv/printTsv in format.ts (4th/5th optional positional opts param so all 17 existing printCsv callers remain backward-compatible — they pass no opts and get default header-included behavior); wired into both `runTenantsGet` + `runTenantsList` via `getBooleanFlag(command, "no-header")` (honored on csv|tsv|csv-full, silently ignored under json/human matching --csv-separator precedent); empty result + --no-header → empty output (no trailing newline) so shell `>> all.csv` redirection is true no-op on empty result sets (rather than appending a blank line); operational workflow simplified from `tenants get <slug> --format csv-full \| tail -1 >> all.csv` to `tenants get <slug> --format csv-full --no-header >> all.csv` eliminating the fragile tail -1 ceremony (loses data when output happens to be empty); rejected — default --no-header on `tenants get` (would break standalone usage without redirect; opt-in preserves both workflows), use --bare or --data-only as flag name (--no-header matches established conventions in xsv/mlr/csvtool/pandas read_csv header=None), use --header={true,false} for future expansion (boolean is simpler for 90% case; future variations could be additional flags), add --csv-envelope instead emit action/metadata as header-row prefix (different feature entirely not a substitute), apply only to `tenants get` skip list (list-bulk concat "bulk-fetch 2 sets into one file" is real workflow too; consistent surface), apply universally to every CSV emit site in CLI (primary use case is list/get concat; broader surfaces are per-command data shapes so header suppression less meaningful future Q), wire flag into global format layer auto-honored without per-site plumbing (tempting but printCsv doesn't have access to command; per-site plumbing is correct abstraction level); drawbacks — concat workflow still requires operator discipline (run list without --no-header then every get with --no-header no auto-detect), --no-header + empty rows = empty output may surprise someone expecting newline-only "empty file" result (matches shell pipeline conventions but worth ADR callout), other CSV surfaces (retention diff policies diff housekeeping diff) still emit headers unconditionally future Q; useful for incident-context bulk export workflows + cohort audit pipelines + pandas read_csv(header=None) integration; closes ADR-0297 Q1 (Phase 2 M4.15.p) |
| 182 | 0304 | 1,500 | Drafted alongside `tenants list/get --columns col1,col2` CSV column subset filter — closes ADR-0297 Q2 deferred ("--columns col1,col2 filter to select a subset of columns to emit"); shared `applyColumnsFilter(headers, rows, columns)` + `parseColumnsFlag(raw)` helpers exported from format.ts so future surfaces (retention diff / policies diff / housekeeping diff) can reuse the same validation + projection logic without duplicating the column-order contract; wired into both runTenantsList (5-col csv + 11-col csv-full paths) and runTenantsGet (single-row csv/tsv + csv-full); validation rejects empty list ("requires at least one column name"), unknown column ("unknown column 'bogus' (valid: id, slug, name, ...)"), duplicate references ("duplicate column 'slug'") — all exit 2 before any data is written; columns appear in operator-specified order not canonical header order (--columns tier,slug,name emits headers in that sequence; operators get both subset + reorder semantics from one flag); valid column set is format-shape-specific (5 cols for csv/tsv, 11 for csv-full, +1 policy_count when --include-policy-count on); composes with --no-header from M4.15.p (single narrowed data row with no header — drop-in for CSV concat pipelines); --columns affects only csv|tsv|csv-full silently ignored under json/human matching --csv-separator/--no-header precedent; rejected — allow --columns * wildcard for "all columns" (same as omitting flag; adds surface for no benefit), --exclude-columns inverse filter (useful but defer until demand; --columns covers common case), use --fields instead of --columns (columns matches CSV domain vocabulary appearing in error messages "unknown column"), allow duplicates (--columns slug,slug,tier) for spreadsheet side-by-side formulas (confusing in most cases; silently accepts typos; reject loudly), index-based selectors (--columns 1,3,5 fragile against shape changes; name-based more stable), validation at parse time vs emit time (emit-time gives correct valid-column list per format variant 5-col vs 11-col; parse-time would require pre-computing format shape), glob patterns (--columns *_at over-engineered; operators can list the few they want), auto-detect --columns from ~/.crossengin profile config (stateful behavior surprises; explicit per-invocation simpler); drawbacks — --columns affects only csv/tsv/csv-full json/human ignore (matches --csv-separator/--no-header precedent but ADR callout), validation list shown on error depends on format shape (5 cols for csv 11 for csv-full operator switching format may see different "valid" lists), no short-form -c flag (conflicts with potential future --columns-preset; stay long-form), composition with --include-policy-count implicit (policy_count column added to valid set only when flag on; --columns slug,policy_count without --include-policy-count gets "unknown column" correct signal but could surprise); useful for narrow audit reports + tier-residency cohort analysis + custom column ordering for downstream pipelines; closes ADR-0297 Q2 (Phase 2 M4.15.q) |
| 183 | 0305 | 1,300 | Drafted alongside `tenants list --max-policy-count N` inverse-cohort filter — closes ADR-0298 Q1 deferred ("--max-policy-count counterpart for inverse cohort queries"); inverse of M4.15.k --min-policy-count for "under-customized" / "platform-default" tenants cohort (e.g., `--max 0` = tenants on PURE platform defaults no overrides at all; `--max 5` = lightly-customized cohort ≤ 5 overrides); WHERE clause `COALESCE(pc.policy_count, 0) <= $N` reusing the M4.15.k LEFT JOIN infra; N >= 0 is valid (unlike --min where N=0 is no-op and rejected) since N=0 has meaningful interpretation for --max (pure platform defaults); negative + non-numeric + float rejected with same round-trip String(parsed) !== input.trim() check as --min; composes with --min for range queries `--min 3 --max 10 → tenants with 3-10 overrides` (both bounds in WHERE via AND, single shared JOIN); range consistency check rejects `--min > --max` with explanatory error "--min-policy-count (10) cannot exceed --max-policy-count (3)" (almost always a typo; silent empty result would confuse); rejected — allow --max 0 silently to mean "no limit" (conflates with "no overrides" semantic; N=0 has meaningful interpretation no limit is omission), use --policy-count-range min,max as single flag (ranges work but two-flag composition simpler and doesn't require range parsing), treat --min > --max as intentional empty result (silent typos surprise; reject loudly), reject N=0 for --max to match --min semantics (N=0 meaningful for --max representing pure platform tenants; asymmetric validation reflects asymmetric semantics), add --exact-policy-count N shorthand for --min N --max N (over-engineered; operators can use both flags --min 3 --max 3), server-side HAVING instead of WHERE (same reasoning as M4.15.k JOIN subquery pre-aggregates so WHERE on COALESCE is correct shape), apply --min/--max on `tenants get` (doesn't make sense get is single-tenant no cohort filtering); drawbacks — --max 0 = "no overlaps with hypothetical inverse-has-overrides flag (but --has-overrides is positive form; no current flag is its inverse; --max 0 serves that role), range validation is one-way (--min > --max rejected but --min == --max is the exact-count case which is allowed; operators using --min 3 --max 3 get exactly-3-policy tenants documented semantic but no alias flag yet), composition with --include-policy-count implicit same way as --min (JOIN shared WHERE bound applied before COALESCE outer column exposure); useful for "untouched tenants audit" + lightly-customized cohort gating + onboarding-tier health checks; closes ADR-0298 Q1 (Phase 2 M4.15.r) |
| 184 | 0306 | 1,300 | Drafted alongside `tenant policies --diff --axis --format gh-summary` axis-aware Markdown rendering — closes ADR-0299 Q4 deferred ("gh-summary table header reflects filter 'Retention field changes (N)' when --axis retention for clearer CI step summary output"); when axisFilter is non-null in pair-wise + N-way gh-summary renderers, customizes 4 surfaces: title gets axis suffix `## Diff: tenant policies (retention axis)`, section header gets axis label `### Retention field changes (N)`, table drops redundant Axis column (saves horizontal space for value columns; new shape `| Field | Left | Right |`), verdict text scopes the divergence message `:warning: **Retention divergence detected**` / `:white_check_mark: **No retention differences** — both tenants match on this axis`; sentence-case axis labels via POLICIES_AXIS_LABELS Record<PoliciesDiffAxisValue, string> with TS exhaustiveness check ("Cost ceiling" two-words reads better mid-line than camelCase costCeiling; title suffix keeps the camelCase flag-value form for grep-back-to-flag correspondence); N-way parity (renderPoliciesMultiDiffGhSummary gets same title-suffix + per-comparison Axis-column drop + axis-scoped success/failure verdict text "All comparisons match on the tier axis." / ":warning: **Tier divergence detected** in at least one comparison"); backward-compatible — axisFilter null preserves the M4.15.e generic shape exactly (no existing parsers / log matchers break); rejected — keep Axis column even under filter for shape consistency (every row same value pure noise; operators prefer wider value cols), use axis name verbatim in verdict "Cost Ceiling divergence" (sentence-case reads better mid-paragraph), emit separate ### Axis scope: retention subheader (adds noisy line; inlining compact), use axis emoji ⚖️/💰 (non-accessible; consistent with broader ADR-0292 rejection of decorative emoji), show axis scope only in verdict not title (operators scan titles first; title scope is most important tell), drop axis suffix from title when only one axis exists (every --axis-filtered run has exactly one axis; suffix always meaningful; don't special-case), move axis metadata to <details> block (hidden metadata defeats summary format purpose), drop --axis compose with --include-policy-count treat axis as pre-compute (out of scope; M4.15.s is strictly rendering); drawbacks — two rendering shapes per surface × two modes = 4 effective shapes (test matrix expands), operators grepping `'## Diff: tenant policies$'` literal-match in CI pipelines may miss filtered runs (workaround: prefix-match `'^## Diff: tenant policies'`), axis label vocabulary must stay in sync with flag values + schema axis names (Record exhaustiveness check catches 4th-axis addition forgotten labels), housekeeping diff gh-summary doesn't get same treatment yet (own --axis from M4.15.h 2-axis gateway/retention; defer); useful for substrate-scoped CI step summaries + axis-focused incident triage + operator clarity on what scope a gate is checking; closes ADR-0299 Q4 (Phase 2 M4.15.s) |
| 185 | 0307 | 1,400 | Drafted alongside `sessions list --format csv\|tsv\|csv-full\|gh-summary` — pivots from the tenants/policies/retention/housekeeping diff family to a NON-diff list-style surface that still only answered in json + human; brings the M4.15.x polish (csv bulk export + csv-full all-fields + gh-summary Markdown + --no-header + --columns subset filter) to sessions; compact csv = 6 cols matching human-format columns exactly (session_id, model, started_at, turns, cost_usd, status with status derived `in_progress`/`ended` from endedAt); csv-full = 12 cols all ArchitectSessionRecord fields (adds id, tenant_id, system_prompt_sha256, ended_at, input_tokens, output_tokens, cached_input_tokens); gh-summary = `## Sessions for tenant <id>` header + cost-summary line (Sessions/Total cost/Total turns/Input tokens/Output tokens with toLocaleString thousands separators for human-readable counts) + per-session Markdown table (no verdict emoji since sessions list is query surface not gate); cost_usd always `.toFixed(6)` for stable currency precision (no floating-point jitter in spreadsheets); empty endedAt/systemPromptSha256 render as empty cells (printCsv null convention); --no-header + --columns + --csv-separator honored via shared format-layer helpers from M4.15.p/q; empty-state per format: csv/tsv/csv-full emit header-only no human "no sessions" text (CSV contract is data shape), gh-summary emits `_No sessions found._` italic marker, human preserves existing "no sessions for tenant X" message; rejected — add csv/gh-summary to sessions show + sessions replay too (show returns 4 result shapes each needs own CSV schema; replay is chat-transcript ordering not tabular; defer), add --include-cost-summary flag to gh-summary (over-engineered; summary is 1 line trivial to ignore), add --sort-by cost_usd desc flag (stores return started_at DESC; operators can pipe sort), use --format markdown instead of --format gh-summary (gh-summary is established alias from M4.15.e for GitHub-Step-Summary-shaped Markdown), match csv-full column order to ArchitectSessionRecord declaration order vs reorder to put sessionId first (stuck with declaration order conventional pattern from tenants list), drop derived status column from csv-full (status useful in compact shape where raw timestamp not present; csv-full has raw ended_at), include verdict emoji ✅/⚠️ in gh-summary based on total cost threshold (sessions list is query surface not gate; no threshold semantic; pipe through separate verdict step); drawbacks — empty-state behavior differs between human "no sessions" text and csv header-only (spreadsheet workflows expect this; shell operators switching formats may be surprised), gh-summary cost-summary line is wide 5 metrics comma-separated may wrap on narrow CI displays, turns column emits raw integer while cost_usd uses .toFixed(6) inconsistent formatting (integers don't benefit from fixed decimal; toFixed on cost is for precision; acceptable asymmetry), no cohort filter flags --min-cost-usd / --max-cost-usd comparable to tenants list --min/--max-policy-count defer to future M; useful for LLM cost auditing + spreadsheet exports + CI session-spend gates + pandas pipeline integration; pivots from tenants surface to sessions M4.15.t |
| 186 | 0308 | 1,400 | Drafted alongside `gateway routes list --format csv\|tsv\|csv-full\|gh-summary` — extends the M4.15.t pivot pattern (list-style surface that only answered in json + human) to the gateway routes registry; compact csv = 7 cols matching formatRoutesTable (route_id, method, path, version, operation, scopes, deprecated) with path rendered via formatPath(r) (`/v1/tenants/:tenant_id` format) and scopes joined with `;` semicolon (not comma — keeps cell parseable in pandas under default comma separator without RFC-4180 quote-escape overhead); csv-full = 15 cols all RouteDefinition fields (adds operation_id, deprecated_since, sunset_at, successor_operation_id, required_scopes, rate_limit_policy_id, idempotency_required, request_schema_sha256, response_schema_sha256, source_pack — widest CSV in the CLI); gh-summary = `## Gateway routes` header + 4-metric summary line (Routes / Deprecated / Sunset scheduled / From packs) + per-route Markdown table with informational sunset emoji `:warning: sunset <timestamp>` in Deprecated column for at-a-glance pre-sunset triage visibility; gh-summary verdict slot intentionally empty (no :white_check_mark:/:warning: based on deprecated counts) — routes list is a query surface not a gate; per-route sunset emoji is informational visibility not verdict; reused M4.15.p --no-header + M4.15.q --columns subset filter (with unknown-column validation showing full format-specific column list) + --csv-separator (rejects `"` + newlines); empty-state per format: csv/tsv/csv-full header-only (matches M4.15.t convention), gh-summary `_No routes registered._` italic, human preserves "no routes registered." message; rejected — add csv/gh-summary to gateway routes register + unregister (single-action commands no tabular shape; out of scope), include path_segments as JSON column in csv-full instead of rendered path string (rendered formatPath is what operators want for audit; raw segments already in --format json), join scopes with pipe `|` instead of semicolon (pipe is common custom separator; semicolons keep nested-delimiter convention universal), add per-method summary to gh-summary "GET: 5, POST: 3" (useful but defer; 4-metric summary covers common needs), surface sunset urgency as "sunset in N days" relative time (requires knowing "today" from context; absolute timestamps easier to audit), group gh-summary routes by sourcePack as <details> blocks (useful but adds complexity defer), add verdict emoji to gh-summary "⚠️ N deprecated routes detected" (converts query surface into pseudo-gate; operators wanting gate should pipe filtered output to own exit-on-count logic); drawbacks — empty-state behavior inconsistent between human + csv (matches M4.15.t precedent), gh-summary Deprecated column has 3 states no/:warning: yes/:warning: sunset <ts> rather than 2 (hides actionable sunset timestamp if collapsed), csv-full 15 cols widest in CLI (acceptable for spreadsheet + pandas), no --from-pack filter flag (operators pipe csv \| grep `,retail-fnb$`; defer); useful for API surface auditing + deprecation-window triage + pre-sunset alerting + per-pack route inventory + pandas pipeline integration; M4.15.u |
| 201 | 0323 | 1,500 | Drafted alongside per-tenant slug round-trip in JSON envelopes — closes ADR-0322 future Q3 (surface slug in JSON envelope); after M4.15.ai shipped slug round-trip in gh-summary Markdown headers for two surfaces (tenant housekeeping + retention list-policies), the corresponding JSON envelopes still echoed back ONLY the resolved UUID — programmatic consumers parsing JSON for audit-trail purposes (CI analytics dashboards, compliance reporting, incident-response playback) had no record of which slug the operator typed; this milestone adds tenantSlug as a conditional-emit field (omitted on UUID input for backward compatibility with M4.15.aa / M4.15.ag shapes); implementation: tenant.ts computes `tenantSlug = tenantFlag !== null && tenantFlag !== tenantId ? tenantFlag : undefined` once at dispatcher level (right after slug resolution), then threads through BOTH JSON emit sites (watch tick NDJSON line + single-tick printJson) via `...(tenantSlug !== undefined ? { tenantSlug } : {})` spread positioned right after the existing tenantId spread; the shared variable also feeds the gh-summary renderer (replacing M4.15.ai's inline ternary for consistency); same pattern in retention.ts runRetentionListPolicies — shared tenantSlug computed once near resolver output, threaded into the printStructured json/yaml envelope between tenantFilter and tableFilter fields; conditional-emit chosen over always-present-null because preserving pre-M4.15.aj envelope shape exactly for UUID callers matters more than matching the intra-surface convention (retention list-policies uses `tenantFilter: tenantFilter ?? null` always-present pattern but tenantSlug gets conditional-spread for cross-surface consistency with tenant housekeeping on the new field); rejected — always-present `tenantSlug ?? null` (would change envelope shape for UUID callers; conditional preserves backward compat exactly), match retention list-policies' "always-null" convention (would create intra-surface convention consistency at cost of new always-present field; cross-surface consistency on the new field wins), apply pattern to retention history/summary/gateway housekeeping/retention housekeeping in this milestone (those don't have gh-summary slug round-trip yet; adding JSON-only would create asymmetry; defer until both gh-summary AND JSON ship together), nested `tenantInput: { id, slug }` shape (would break backward compat with existing field names; flat field is simpler), nested `filter: { tenantId, tenantSlug }` sub-object (would restructure envelope shape; adjacent flat fields preserve existing shape), don't share tenantSlug variable between JSON envelope and gh-summary call site (would duplicate condition; computing once at dispatcher level cleaner and drift-free), always emit slug even for UUID input if meta.tenants has matching slug (reverse resolution needs extra query — defer pairs with ADR-0322 Q2); drawbacks — conditional spread cross-surface but diverges from retention list-policies' intra-surface convention (documented; cross-surface consistency on the new field wins), no envelope-shape versioning (consumers using strict JSON schema validation may fail on new field; operators should regenerate client code from updated schemas; envelope version field overkill for single field addition), no YAML-specific test beyond the one retention list-policies YAML test (printStructured serves both json+yaml uniformly), other --tenant-accepting surfaces (retention history, summary, gateway housekeeping, retention housekeeping) still echo only resolved UUID (documented as future Qs 4/5/6), no reverse resolution for UUID input (round-trip one-way slug-input-preserved not bidirectional; defer pairs with ADR-0322 Q2); 10 new tests across 2 describe blocks (5 in `tenant housekeeping JSON envelope slug round-trip (M4.15.aj)` covering --tenant <slug> + --format json emits tenantSlug alongside tenantId, --tenant <uuid> + --format json omits tenantSlug, --all-tenants omits both tenantId AND tenantSlug, no --tenant flag omits both, --watch + --tenant <slug> + --format json streams NDJSON with tenantSlug per tick verifying watch path has its own JSON emit site distinct from single-tick; 5 in `retention list-policies JSON envelope slug round-trip (M4.15.aj)` covering --tenant <slug> + --format json emits tenantSlug alongside tenantFilter, --tenant <uuid> + --format json omits tenantSlug, no --tenant flag omits tenantSlug tenantFilter null preserved, --tenant <slug> + --format yaml emits tenantSlug in YAML envelope verifying field surfaces through printStructured's YAML emit path, --tenant <slug> composes with --table filter all three fields surface correctly together); tenant.test.ts grows 182 → 187 tests (+5); retention.test.ts grows 810 → 815 tests (+5); workspace test count 10,142 → 10,152 (+10); ADR-0323 documents design + 7 rejected alternatives + 6 future Qs (reverse slug resolution for UUID input pairs with ADR-0322 Q2, apply pattern to diff/diff-history/diff-timeline envelopes pairs with ADR-0322 Q1, apply to retention history/summary when gh-summary surfaces ship, apply to gateway housekeeping + retention housekeeping (separate dashboards) JSON envelopes, apply to tenants list/get/resolve if those gain tenant-id echo, envelope shape versioning when demand emerges); useful for CI analytics dashboards parsing JSON envelopes for audit trails + compliance reporting pipelines + incident-response playback workflows where slug correlation matters without operator-side re-resolution + closes the audit-trail story for the two surfaces that already had gh-summary slug round-trip (consumers get slug whether they parse gh-summary Markdown OR JSON envelopes); closes ADR-0322 future Q3 (Phase 2); M4.15.aj |
| 200 | 0322 | 1,400 | Drafted alongside per-tenant slug round-trip in gh-summary headers — closes ADR-0314 future Q4 + ADR-0320 future Q3 (slug round-trip in tenant housekeeping + list-policies gh-summary headers deferred); after ADR-0273 (M4.14.o) gave operators `--tenant <uuid\|slug>` resolution, the gh-summary surfaces across the diff family + list surfaces echoed back ONLY the resolved UUID — the slug the operator typed was lost; CI step summaries rendered `**Tenant:** \`<36-char-uuid>\`` with no hint that `acme-prod` was the input; this milestone surfaces the operator's original `--tenant` input in gh-summary headers when it differs from the resolved UUID (i.e., the input was a slug); UUID input preserves existing bare-UUID shape verbatim (backward-compatible); two surfaces updated: `formatTenantHousekeepingReportGhSummary` (TenantHousekeepingReportGhSummaryInput gains optional `tenantSlug?: string`; header renders `**Tenant:** \`<uuid>\` (slug: \`<slug>\`)` when slug differs from UUID, otherwise bare-UUID M4.15.aa shape) and `formatPoliciesListGhSummary` (PoliciesListGhSummaryInput gains optional `tenantSlug?: string \| null`; `**Filtered:**` line surfaces `tenant=\`<uuid>\` (slug: \`<slug>\`)` when slug differs, otherwise bare-UUID M4.15.ag shape); call-site threading: dispatcher reads operator's raw `--tenant` input via `getStringFlag(command, "tenant")`, then after resolution passes `tenantSlug: rawInput !== null && rawInput !== resolvedUuid ? rawInput : undefined` — UUID input → equal → undefined → bare-UUID shape; slug input → differs → slug value → round-trip emitted; rejected — always surface slug (operators passing UUIDs wouldn't want redundant `(slug: \`<uuid>\`)` line; conditional matches operator intent), replace UUID with slug (would break audit reproducibility — gh-summary captures stored value), surface slug ONLY in separate metadata not in header (would require operators to read further down to find correlation), use custom delimiter `/` or `→` (parentheses are natural Markdown convention), resolve slug on the fly in renderer (would require passing async slug resolver into pure renderers — couples Markdown layer to resolver logic), apply pattern to --diff/--diff-history/--diff-timeline in this milestone (diff renderers take per-side `{tenantId, input}` shapes — threading per-side slugs through 4+ renderer signatures structurally invasive, defer to future Q1), echo slug in JSON envelope too (ADR-0314 + ADR-0320 envelopes currently omit; adding it would change envelope shape for programmatic consumers — defer); drawbacks — tightly-coupled pair of surfaces (one pattern, two renderers in one commit; future milestones applying the pattern to diff/diff-history/diff-timeline surfaces (Q1) will need per-side threading), no envelope exposure (slug appears only in gh-summary Markdown, not JSON/YAML envelopes — programmatic audit consumers re-resolve at their layer), no reverse resolution for UUID input (operators passing `--tenant <uuid>` won't see slug even if `meta.tenants` has one; round-trip is one-way slug-input-preserved not bidirectional UUID-input-enriched; adding reverse would require extra query defer), JSONB-shaped characters in slugs (meta-schema slug regex `^[a-z][a-z0-9-]*$` doesn't allow Markdown-special chars so parenthetical rendering is safe without escaping; if regex widens escaping needed); 11 new tests across 2 describe blocks (5 in `tenant housekeeping gh-summary slug round-trip (M4.15.ai)` covering integration --tenant <slug> → both UUID+slug in **Tenant:** header, integration --tenant <uuid> preserves bare-UUID M4.15.aa shape, direct renderer round-trip when tenantSlug !== tenantId, direct renderer omits suffix when tenantSlug === tenantId, direct renderer omits suffix when tenantSlug undefined; 6 in `retention list-policies gh-summary slug round-trip (M4.15.ai)` covering integration --tenant <slug> → both UUID+slug in **Filtered:** line, integration --tenant <uuid> preserves bare-UUID M4.15.ag shape, direct renderer round-trip when tenantSlug !== tenantFilter, direct renderer omits suffix when match, direct renderer omits suffix when undefined, direct renderer slug round-trip composes with --table filter `tenant=\`<uuid>\` (slug: \`<slug>\`) \| table=\`workflow_traces\``); tenant.test.ts grows 177 → 182 tests (+5); retention.test.ts grows 804 → 810 tests (+6); workspace test count 10,131 → 10,142 (+11); ADR-0322 documents design + 7 rejected alternatives + 5 future Qs (apply slug round-trip pattern to diff/diff-history/diff-timeline gh-summary surfaces, reverse slug resolution for UUID input, surface slug in JSON envelope pairs with reverse resolution, apply to retention history/summary gh-summary surfaces when they ship, surface slug in `**Scope:**` line under --all-tenants); useful for CI step summary readability + at-a-glance slug correlation + interactive debugging audit trails (operators typing `--tenant acme-prod` see `acme-prod` echoed back in the rendered Markdown rather than having to mentally map the UUID); closes ADR-0314 future Q4 + ADR-0320 future Q3 (Phase 2); M4.15.ai |
| 199 | 0321 | 1,100 | Drafted alongside `tenant housekeeping --diff --axis --format gh-summary` axis-aware rendering — closes ADR-0299 future Q1 + ADR-0306 future Q1 (housekeeping diff gh-summary axis-awareness deferred); mirrors M4.15.s policies axis-aware treatment to the 2-axis (gateway/retention) housekeeping diff family vs policies 3-axis (retention/costCeiling/tier); new HOUSEKEEPING_AXIS_LABELS Record<"gateway"|"retention", string> sentence-case labels Gateway/Retention; both renderHousekeepingDiffGhSummary + renderHousekeepingMultiDiffGhSummary gain optional axisFilter parameter; when axisFilter non-null 4 surfaces customize: title gets axis suffix `## Diff: tenant housekeeping (gateway axis)` or `(retention axis)`, section header gets axis label `### Gateway field changes (N)` or `### Retention field changes (N)`, table drops Axis column (saves horizontal space new 4-col shape `| Table | Field | Left | Right |`), verdict text scopes the divergence message `:warning: **Gateway divergence detected** — N field(s) differ` or `:white_check_mark: **No gateway differences** — both tenants match on this axis`; backward-compatible — axisFilter null preserves M4.15.e generic shape exactly (no parsers break); N-way parity (renderHousekeepingMultiDiffGhSummary gets same title-suffix + per-comparison Axis-column drop + axis-scoped success "All comparisons match on the gateway axis." / failure "Gateway divergence detected in at least one comparison." verdict text); type-narrow axisFilter parameter uses `"gateway" | "retention" | null = null` matching runtime axis values already type-narrowed through runTenantHousekeepingDiff's axisFlag validation; rejected — inline gh-summary axis logic at call sites instead of renderer parameter (renderer is right abstraction layer matches M4.15.s precedent), re-use shared renderDiffGhSummaryAxisAware helper between policies + housekeeping (data shapes differ housekeeping has Table column policies doesn't; code duplication minimal helper factoring not worth indirection), use --substrate instead of --axis (stuck with axis terminology from M4.15.h/M4.15.s for consistency across diff family), drop Axis column for ALL gh-summary output (would break M4.15.e shape; backward-compat preserved), use gateway/retention emoji :gear:/:floppy_disk: in verdict (non-accessible consistent with ADR-0306 rejection), add --axis field=<name> sub-axis filter (future Q); drawbacks — two rendering shapes per surface (pair vs N-way) × two modes (filtered vs not) = 4 effective shapes (same as policies side ADR-0306), operators grepping `'## Diff: tenant housekeeping$'` literal-match in CI pipelines may miss filtered runs (workaround prefix-match), axis label vocabulary must stay in sync with flag values + schema (Record exhaustiveness check catches drift), code duplication with policies side (acceptable given data-shape differences); 4 new tests under "tenant housekeeping --diff --axis <axis> --format gh-summary (M4.15.ah)" describe block + 1 existing M4.15.h test updated to assert new M4.15.ah shape (title suffix + 4-col table) since prior expectation of "| gateway |" in Axis column became invalid when column dropped; tenant.test.ts grows 173 → 177 tests (+4); workspace test count 10,127 → 10,131 (+4); ADR-0321 documents design + 6 rejected alternatives + 4 future Qs (--axis field=<name> sub-axis filter, cross-surface axis-aware verdict in non-diff path, per-axis color tinting for visual distinction, factor shared diff-axis helpers if more render targets land); the diff-family axis-awareness is now COMPLETE — both policies (3-axis) and housekeeping (2-axis) gh-summary surfaces emit identical title-suffix + section-header + Axis-column-drop + verdict-text-scope pattern; closes ADR-0299 future Q1 (Phase 2); M4.15.ah |
| 198 | 0320 | 1,300 | Drafted alongside `retention list-policies --format gh-summary` Markdown rendering — closes ADR-0319 future Q2 deferred policy-inventory surface; new formatPoliciesListGhSummary helper renders two-section Markdown report under `## Retention policies` title: optional `**Filtered:**` metadata line with `tenant=<uuid>` + `table=<name>` echoed when flags set (omitted entirely in common unfiltered case to keep header tight; parts joined with ` | ` separator matching sessions list cost-summary + gateway housekeeping convention), summary counts `**Platform defaults:** N | **Per-tenant overrides:** M`, ### Platform defaults section with 4-col table (Table backticked | Retention right-aligned `Nd` | Enabled yes/no | Last pruned backticked ISO or `_never_` italic distinguishing never-pruned from no-data), ### Per-tenant overrides section with 5-col table (Tenant backticked UUID | Table backticked | Retention right-aligned | Enabled yes/no | Opt-out collapsing 3-field TenantRetentionPolicyRow shape: false → `no`, true → `yes _(until <iso-or-"indefinite">; reason: <text-or-"<no reason>">)_` italic detail with fallback literals matching formatTenantOptOutSummary helper consistency); empty-state markers per section `_No platform defaults configured._` + `_No per-tenant overrides configured._` italic (both shown when both empty); no verdict emoji — list-policies is query surface not gate (matches ADR-0308 gateway routes list + ADR-0319 prune empty-policies precedent); rejected — combine both sections into unified table with Source column (would duplicate Last-pruned/Opt-out column asymmetry; two-section matches human format + keeps each column set tight), add verdict when per-tenant overrides == 0 "no tenant has customized retention" (confuses query-surface vs gate semantics), use :warning: for disabled platform defaults (disabled isn't problem it's configuration choice for compliance holds), render last-pruned as "5 days ago" relative time (requires knowing now from context; ISO absolute audit-friendly), add per-tenant group-by summary row "3 tenants with overrides spanning 5 tables" (defer to future Q same pattern as ADR-0319 future Q1), match human-format message exactly "Platform defaults (N total):" (too wordy for gh-summary), skip Filtered: line when no filter set (already accepted design via conditional rendering for unfiltered common case); drawbacks — per-tenant table width scales with tenant count + opt-out detail length (acceptable for CI step summary), tenant UUIDs 36 chars per row no slug resolution yet (deferred to future Q3), opt-out cell combines 3 fields into one (operators filtering by reason via grep need italic detail intact), no group-by summary "N tenants with overrides" (defer); 9 new tests across 2 describe blocks (3 integration covering empty-policies both sections empty-state no filter echo, both sections populated with platform 4-col + tenant 5-col tables verifying all cell formats, --tenant + --table filters echoed in metadata; 6 direct formatPoliciesListGhSummary unit tests for opt-out cell with set values, indefinite + <no reason> fallbacks, optOut=false renders no, filter echo only-tenant, filter echo only-table, both sections empty markers + no verdict emoji); retention.test.ts grows 795 → 804 tests (+9); workspace test count 10,118 → 10,127 (+9); useful for retention-configuration CI audit reports + at-a-glance policy inventory + tenant-override visibility for compliance workflows; closes ADR-0319 future Q2 (policy-inventory surface); M4.15.ag |
| 197 | 0319 | 1,400 | Drafted alongside `retention prune --format gh-summary` Markdown rendering — extends M4.15.x gateway prune-idempotency gh-summary pattern to the retention prune side covering both dry-run + actual-prune paths; shared formatRetentionPruneGhSummary helper takes discriminated-union input on dryRun boolean so TS narrows `results: ReadonlyArray<RetentionPreviewResult>` vs `ReadonlyArray<RetentionRunResult>` correctly inside renderer; Markdown shape `## Retention prune [(dry-run)]` title with conditional suffix + As of metadata + summary line (Entries N | Would prune/Pruned M | Skipped K [(breakdown alphabetical)] | Rows would be/are deleted X with toLocaleString thousands) + per-entry 6-col table (Table backticked | Tenant backticked UUID or _(platform)_ italic for null tenant | Status backticked with _(skipped)_ italic suffix for non-counted rows | Retention Nd literal | Would-delete/Deleted right-aligned toLocaleString or em-dash for skipped | Cutoff backticked ISO or em-dash for null) + verdict footer; verdict semantic identical to M4.15.x gateway prune (4 cases: empty results → `_No retention policies configured [(dry-run)]._` italic marker no verdict, dry-run with results → `_Dry-run: no rows deleted. Re-run without --dry-run to prune._` informational footer no verdict, actual prune all skipped → `:white_check_mark: **Nothing to prune** — all N entries skipped.`, actual prune > 0 deleted → `:white_check_mark: **Prune succeeded** — X row(s) deleted across N entries.`); skipped entries INCLUDED in per-entry table since skip status is actionable audit info (which tenants opted out, which policies disabled, unknown table refs); skip-status breakdown sorted alphabetically when skipped > 0 with multiple distinct statuses (matches formatPruneSummary convention); rejected — hide skipped entries (defeats auditing purpose), use :warning: for all-skipped (all-skipped is successful gate outcome not warning matches M4.15.x precedent), add per-tenant group-by summary row (defer to future Q), use <details> collapse for skipped-only table (adds complexity for audit workflows where skipped detail IS the point), match human-format message exactly (too wordy for gh-summary), skip summary line on empty results (italic marker IS the summary), combine pruned + skipped into single Total (operators need to distinguish for gate decisions — zero pruned from all-skipped is different signal from zero pruned due to error); drawbacks — per-entry table can be wide when many tenants have per-tenant policies (10+ rows with full UUIDs; CI step summary scroll handles it), cutoff timestamp cell takes ~25 chars (necessary for audit reproducibility), em-dash for null/skipped cells is Unicode (most parsers handle fine; matches M4.15 convention), discriminated-union input requires correctly-typed results (TS enforces at compile time but small cognitive overhead), no --tenant filter echo in header if retention prune gains --tenant flag (doesn't currently); 8 new tests across 2 describe blocks (4 integration covering --dry-run + 1 entry verifying title suffix + Would-prune labels + per-entry row + no verdict + informational footer, actual prune 2 entries 700 rows verifying Pruned labels + total + :white_check_mark: verdict, empty results no policies marker no per-entry table, all-skipped 2 entries with Skipped: 2 (1 skipped_disabled, 1 skipped_opt_out) breakdown + Nothing-to-prune verdict; 4 direct formatRetentionPruneGhSummary unit tests for dry-run with tenant-id + cutoff full shape, mixed pruned + skipped with em-dash markers, ISO timestamp vs em-dash for cutoff, alphabetical skip-status breakdown across 3 distinct skip reasons); retention.test.ts grows 787 → 795 tests (+8); workspace test count 10,110 → 10,118 (+8); useful for retention-sweep CI step summaries + at-a-glance verdict + skip-reason audit visibility + nightly retention job monitoring; M-maint.coverage pattern paused after M4.15.ae diminishing returns; M4.15.af |
| 196 | 0318 | 1,300 | Drafted alongside tenant.ts coverage maintenance pass — fifth M-maint.coverage milestone after M4.15.y/.ab/.ac/.ad; targets PG-env-missing exits + flag mutual-exclusivity checks pre-PG validation via 8 new tests: tenant housekeeping PG-env-missing exit 1 (lines 238-249 catch branch when parsePgEnvConfig throws), tenant policies PG-env-missing exit 1 (lines 857-868), tenant policies --diff PG-env-missing exit 1 (lines 954-965), --diff + --watch mutual exclusivity exit 2 (lines 175-180 v1 diff-loop garbling), --tenant + --all-tenants mutual exclusivity exit 2 (lines 127-132), policies --diff + --vs-tier rejected both define RHS pick one (lines 800-804), policies --vs-tier + --explain rejected synthetic RHS already answers what-if (lines 820-825), policies --add-tenant without --diff rejected extends diff RHS chain (lines 806-811); coverage delta statements 89.69% → 91.48% (+1.79pp), branches 86.75% → 86.90% (+0.15pp), functions 91.93% → 91.93% unchanged because closeConn async closures fire only when PG conn constructed successfully — env-missing exits return BEFORE closeConn assignment, lines 89.69% → 91.48% (+1.79pp); remaining 8.5% uncovered dominated by per-field switch-case branches lines 488-518 in readGatewayField/readRetentionField (each case requires alert spec targeting that field; coverage would need 10+ alert-per-field tests), watch-mode error-render paths 344-353 + 396-401 (fire when gather throws under --watch; requires provider stub throwing mid-tick), multi-tenant diff internal paths 885-887 + 1149-1151 + 1229-1231 + 1300-1302 (reached only when specific tenant-ID resolution combinations succeed/fail); rejected — add per-field alert tests covering 488-518 (would need 10+ tests per field set; defer to field-alert-specific pass), add watch-mode error-render tests for 344-353/396-401 (provider stubs throwing mid-tick under --watch + carefully-timed abort; substantial fixture work), use pgConnectionOverride with fake conn closing cleanly to cover closeConn (closures assigned but only called in finally blocks; existing tests using pgConnectionOverride exit without triggering parsePgEnvConfig path that sets closeConn ref), add --add-tenant + --vs-tier mutual exclusivity test lines 813-818 (branch effectively unreachable because --add-tenant requires --diff line 806-811 fires first AND --diff + --vs-tier conflict line 800-804 fires first; document as dead-code-under-current-check-ordering), mark dead-code branches with c8 ignore (hides legitimate defense-in-depth; accept gap honestly), co-locate tests inside existing per-command describe blocks (single maintenance block matches M4.15.y/.ab/.ac/.ad precedent); drawbacks — modest +1.79pp delta reflects diminishing returns consistent with M4.15.ad (+0.55pp), --add-tenant + --vs-tier check at lines 813-818 effectively unreachable under current check-ordering (either reorder to make reachable OR remove defense-in-depth branch; former right fix if operators can trigger via obscure flag combos), function-count coverage unchanged 91.93% → 91.93% because closeConn closures remain uncovered (requires tests exiting via finally path after real-conn-like setup), per-field alert coverage deferred (most significant uncovered surface area requires separate pass); 8 new tests under "tenant.ts coverage maintenance (M4.15.ae)" describe block; tenant.test.ts grows 165 → 173 tests (+8); workspace test count 10,102 → 10,110 (+8); ROI tracking across M-maint.coverage milestones: apply.ts +28.86pp / 14 tests → commands.ts +7.18pp / 14 tests → gateway.ts +7.59pp / 5 tests → commands (chat) +0.55pp / 6 tests → tenant.ts +1.79pp / 8 tests; pattern suggests M-maint.coverage series should either wind down OR pivot to targeted fixture-infrastructure work that unlocks remaining gaps in one substantial pass (real-PG-like stub); M4.15.ae / M-maint.coverage |
| 195 | 0317 | 1,300 | Drafted alongside commands.ts chat-surface coverage maintenance pass — follow-up to ADR-0315 which deferred chat-surface gaps to separate pass; targets tractable chat-loop gaps (lines 415-422 outer catch, 396-402 JSON envelope branch, 271-273 flag validation, 284-289 one-shot + allow-file-write check) via StubProvider extensions (ThrowingProvider class overriding complete() to throw) + targeted flag-validation tests; 6 new tests under existing "commands.ts coverage maintenance (M4.15.ab)" describe block: --max-tool-iterations non-numeric → exit 2 (lines 271-273), --allow-file-write + --one-shot without --auto-approve-writes → exit 2 explanatory error (lines 284-289), provider throws mid-stream Error → outer catch with chat: <message> exit 1 (lines 415-416), provider throws non-Error string → String(err) fallback, JSON envelope on success preserves providerKind=single + availableProviders via string matching since printJson pretty-prints multi-line per envelope making JSON.parse unreliable (lines 396-402), human-format success session-ended line without router suffix (line 410); coverage delta statements 93.64% → 94.19% (+0.55pp), branches 84.92% → 85.49% (+0.57pp), functions 100% → 100% (unchanged), lines 93.64% → 94.19% (+0.55pp); modest delta reflects diminishing-returns reality where remaining gaps are hardest to close (78-80 runInit emptyManifest catch unreachable without TS-bypass, 120-129 validateManifest throw effectively dead code since tryValidateManifest catches only throwable ManifestValidationError, 291-292 interactive approver requires interactive lineReader hanging gracefully, 348-349 ProviderBuilder rethrow buildChatCompleter internal, 407 router suffix requires both ANTHROPIC + OPENAI keys, 419-422 finally pgConnection close requires --persist + valid PG env); JSON streaming test uses string matching not JSON.parse due to printJson pretty-printing producing multi-line-per-envelope output shape (test-side workaround; actual chat code emits valid JSON); rejected — inject transcriptOverride wrapping fake pgConnection (chat code only sets internal pgConnection when transcript undefined AND --persist set so transcriptOverride bypasses branch entirely), add providerBuilderOverride returning providerKind=router (would require new injection point for one line of coverage; surface expansion not worth it), mark 78-80 + 120-129 defensive lines with c8 ignore (force higher coverage but hide legitimate guards; accept gap honestly), use vi.mock to stub buildChatCompleter (brittle couples tests to internal API), set real PG env vars + --persist + handle connection failure gracefully (would exercise PG env validation branch but not reach 419-422 since pgConnection creation throws before close path fires); drawbacks — 6 tests for +0.55pp worse ROI than M4.15.ab pass (14 tests for +7.18pp); still useful for hardening outer-catch paths against future regression; next maintenance pass should pivot to different file (chat.ts is bigger + separate); ThrowingProvider defined inline inside each test not as fixture (one-line override acceptable); JSON test uses string matching instead of JSON parsing; commands.test.ts grows 53 → 59 tests (+6); workspace test count 10,096 → 10,102 (+6); fourth M-maint.coverage milestone; M4.15.ad / M-maint.coverage |
| 194 | 0316 | 1,100 | Drafted alongside gateway.ts coverage maintenance pass — third M-maint.coverage milestone after M4.15.y apply.ts (74.58% → 92.24%) and M4.15.ab commands.ts (86.46% → 93.64%); 5 new tests targeting cheap wins via existing fakePgConnection + fakeServerFactory fixtures: prune-idempotency with pgConnectionOverride only (no idempotencyStoreOverride forces line 169 PostgresIdempotencyStore construction from injected conn), prune-idempotency PG-env-missing exit 1 with PG-required error (catch branch lines 163-167), runGatewayStart serverFactory rejection throws EADDRINUSE-style error → printError "failed to listen" exit 1 (lines 377-385), runGatewayStart --in-memory without runtimeOverride forces buildRuntime in-memory branch lines 527-552 (constructs InMemoryRouteRegistry + InMemoryPrincipalResolver + InMemoryIdempotencyStore + InMemoryRateLimitChecker; uses fakeServerFactory + waitForShutdown returning immediately), runGatewayStart --in-memory --format=json emits "kind":"started" JSON envelope (lines 424-431 non-human format branch); coverage delta statements 81.91% → 89.50% (+7.59pp), branches 79.10% → 81.29% (+2.19pp), functions 87.50% → 87.50% unchanged (2 uncovered functions beforeHandle line 581 + waitForShutdownSignal line 586 require real HTTP request handling + OS signal handling respectively neither testable without substantial fixture infrastructure), lines 81.91% → 89.50% (+7.59pp); remaining uncovered = PG-mode buildRuntime branch lines 568-579 (constructing PostgresRouteRegistry + PostgresRateLimitChecker + PostgresIdempotencyStore from real PG connection; defer to PG-stub maintenance pass) + waitForShutdownSignal lines 586-596 (hooks SIGINT+SIGTERM; defer); rejected — add PG-mode buildRuntime tests covering 568-579 (would require constructing fake PgConnection that satisfies PostgresRouteRegistry schema-probing queries + PostgresRateLimitChecker index-existence queries + advisory-lock contracts; defer to substantial PG-stub pass), add signalRegistrar injection to waitForShutdownSignal (would refactor signal-handling surface for coverage alone; acceptable to leave uncovered given exclusively reached when ctx.waitForShutdown undefined always true in tests), add chat-surface coverage tests instead (chat.ts already at 92.64% lines; gateway.ts lower-priority target), use vi.mock to stub InMemoryRouteRegistry constructors (brittle couples tests to internals; real constructions via buildRuntime honest), co-locate tests inside existing describe blocks (single maintenance block matches precedent + makes intent clear in test output), mark PG-mode buildRuntime lines with c8 ignore (hides legitimate production code; accept gap honestly); drawbacks — gateway.ts still has 10.5% uncovered awaiting PG-stub infrastructure, serverFactory rejection test uses contrived EADDRINUSE error message (actual EADDRINUSE on Linux comes with errno codes; test's string matching loose), in-memory buildRuntime test doesn't verify exact runtime components constructed (only that flow exits 0 + prints expected startup message; acceptable GatewayRuntime internals tested in api-gateway-runtime package); 5 new tests under "gateway.ts coverage maintenance (M4.15.ac)" describe block; gateway.test.ts grows 99 → 104 tests (+5); workspace test count 10,091 → 10,096 (+5); useful for restoring coverage hygiene on gateway.ts after M4.15.x prune-idempotency additions + establishing patterns for future PG-stub-based gateway tests; M4.15.ac / M-maint.coverage |
| 193 | 0315 | 1,200 | Drafted alongside commands.ts coverage maintenance pass — second M-maint.coverage-style milestone after M4.15.y apply.ts pass; targets specific uncovered statement ranges surfaced by v8 coverage report (78-80 runInit emptyManifest catch unreachable without TS-bypass, 110-129 runValidate tryValidateManifest ok=false JSON + validateManifest throw branches, 152-154 runDiff readManifestFile error, 179-181 runHash missing-path, 208-218 runPatch read/write catch branches, 230-232 runChat invalid --format, 240-242 runChat invalid --max-tokens); hardest gap closed via constructing a duplicate-trait manifest that passes ManifestSchema.parse but fails validateManifest's duplicate-name loop (mirrors how operators actually hit the ok=false branch in practice when authoring manifests with accidentally duplicated identifiers); coverage delta statements 86.46% → 93.64% (+7.18pp), branches 79.66% → 84.92% (+5.26pp), functions 100% → 100% (unchanged), lines 86.46% → 93.64% (+7.18pp); remaining uncovered = runInit emptyManifest catch (lines 78-80 hard to trigger as emptyManifest internally constructed from TS-typed params that don't reach runtime validation failures it guards against) + runChat chat-loop internal state transitions for per-chunk error rendering (lines 291-292/348-349/407/415-422 require provider stubs producing specific malformed chunk sequences; defer to chat-surface-specific pass); rejected — add chat-streaming tests covering per-chunk error paths (would double commands.test.ts size + requires constructing provider stubs that produce specific malformed chunk sequences; defer), mark runInit emptyManifest catch with c8 ignore pragma to force 100% (hides legitimate defensive guard; accept gap honestly), make runChat accept providerOverride generator simulating error chunks (already exists via StubProvider; uncovered chat paths need specific transitions like tool-call without matching pending), refactor commands.ts to extract error helpers (restructure surface for coverage alone not worth it), co-locate maintenance tests inside existing per-command describe blocks vs single maintenance block (maintenance-pass block makes intent clear matches M-maint.coverage precedent), use vi.mock to force manifestHash/emptyManifest to throw (brittle couples tests to internal module structure; real inputs honest); drawbacks — 6.36% still uncovered (intentional trade-off awaiting chat-surface pass), duplicate-trait test uses inline manifest construction not helper (acceptable for one-off; future invalid-manifest scenarios could extract makeInvalidManifest), runPatch --output refusal test relies on writeManifestFile "refusing to overwrite" error string (acceptable; user-facing intentionally stable); 14 new tests under "commands.ts coverage maintenance (M4.15.ab)" describe block; commands.test.ts grows 39 → 53 tests (+14); workspace test count 10,077 → 10,091 (+14); useful for restoring coverage hygiene on commands.ts after M4.14 error-handling additions + establishing per-command catch-branch tests + second M-maint.coverage milestone; M4.15.ab / M-maint.coverage |
| 192 | 0314 | 1,600 | Drafted alongside `tenant housekeeping --format gh-summary` cross-dashboard Markdown — mirrors M4.15.z gateway housekeeping gh-summary to the cross-substrate combined view; one-import lift via shared formatTrippedAlertsGhSummaryTable helper from M4.15.z; new formatTenantHousekeepingReportGhSummary helper renders ## Tenant housekeeping title + As of/Tenant/Scope/Gateway-Retention-table-counts metadata + two substrate sections (### Gateway substrate 5-col Table/Total rows right-aligned/Oldest/Would prune right-aligned/Retention with `_TTL-managed_` for expires_at + ### Retention substrate 6-col adding Enabled column yes/no/—) + ### Threshold alerts (N) section when tripped + verdict footer; column asymmetry between substrates (5 vs 6 cols) mirrors human-format two-section layout (gateway has no per-table enabled flag so forcing both into single 6-col unified table would require gateway side to always emit — adding noise); reuses identical formatTrippedAlertsGhSummaryTable from threshold-alert.ts so this surface and gateway-housekeeping emit identically-shaped alert tables (CI step summary parsers can treat both surfaces uniformly); verdict semantic identical to M4.15.z (alerts not evaluated → no verdict query surface ≠ gate, evaluated + none tripped → :white_check_mark: **All threshold alerts passed.**, tripped → :x: **N threshold alert(s) tripped** — exit 3 (CI gate failed)); --watch + gh-summary rejected via parseWatchFlags inherited check (no extra preflight needed); rejected — force both substrates into single 6-col unified table (would require gateway side to always emit — for Enabled adding noise), add third ### Cross-substrate summary section with row counts summed across both (over-engineered; operators can sum), render tenant metadata in <details> when --all-tenants (adds complexity; only 3+N tables), add per-tenant-override sub-table under each retention substrate row when --all-tenants (significantly complicates shape; defer if demand emerges), inline formatTenantHousekeepingReportGhSummary instead of exporting (exporting enables direct unit tests matching M4.15.z pattern), use gateway-side formatHousekeepingReportGhSummary helper for gateway substrate section (that helper emits ## Gateway housekeeping wrapper header not compatible with tenant cross-dashboard nesting; inlining grid logic simpler than refactoring gateway helper to accept embedded mode flag), defer --watch compatibility to future ADR (parseWatchFlags already handles); drawbacks — column asymmetry 5 vs 6 cols may surprise operators expecting uniform shapes (acceptable substrate data shapes genuinely differ), cross-dashboard Markdown is wide two 6-col tables + metadata + alerts wraps on narrow CI step summary displays, no --tenant-slug resolution in header echoes resolved UUID even when operator used --tenant acme-prod (matches json envelope behavior; acceptable for audit reproducibility), test fixtures duplicate HousekeepingReport + RetentionHousekeepingReport shape inline rather than reusing fakeConn flow (renderer-in-isolation testing); 12 new tests across 2 describe blocks (5 integration: ## title + As of + table-count + two substrate sections + Tenant: line; --all-tenants emits Scope: not Tenant: line; :white_check_mark: when alerts evaluated none tripped; :x: + tripped-alerts table when alerts trip exit 3; --watch + gh-summary rejected exit 2; 7 direct formatTenantHousekeepingReportGhSummary unit tests: gateway substrate Retention column Nd/_TTL-managed_; retention substrate Enabled column yes/no/—; null oldestAt → — in both substrates; no verdict when hadAlerts=false; :white_check_mark: when hadAlerts + no tripped; :x: + table when tripped > 0; omits Tenant + Scope lines when cross-tenant default); tenant.test.ts grows 153 → 165 tests (+12); workspace test count 10,065 → 10,077 (+12); useful for compliance-audit CI step summaries spanning both operator-domain (gateway) and substrate-centric (retention) views in one Markdown report + at-a-glance verdict for cross-dashboard threshold alerts; M4.15.aa |
| 191 | 0313 | 1,500 | Drafted alongside `gateway housekeeping --format gh-summary` Markdown rendering + threshold-alert Markdown helpers — original M4.15.z pick was "threshold-alert.ts survey + format coverage" but inspection showed threshold-alert.ts isn't a CLI surface (it's a library used by gateway-housekeeping + tenant-housekeeping for parsing + evaluating --threshold-alert flags); pivoted scope to add gh-summary format branch to gateway housekeeping (non-watch mode) + reusable Markdown helpers in threshold-alert.ts for tripped-alert rendering so tenant-housekeeping + future surfaces can reuse same shape; 3 new exported helpers: formatTrippedAlertGhSummaryRow (single alert → one Markdown table row with 5 cols Table | Field | Actual | Threshold | Age; compound AND/OR alerts render per-clause field+actual joined by `<br>` inside cell since GitHub Markdown tables don't support rowspan; spec cell suffixed with `_(compound)_` italic marker), formatTrippedAlertsGhSummaryTable (multi-alert table header + N rows; returns empty string on empty input so caller suppresses section), formatHousekeepingReportGhSummary (full dashboard: `## Gateway housekeeping` header + As of / Tenant / Scope / Tables count metadata + 5-col per-table grid Table | Total rows (right-aligned) | Oldest | Would prune (right-aligned) | Retention with `_TTL-managed_` for idempotency-style tables / `Nd` for retention-governed / `—` for unconfigured + `### Threshold alerts (N)` section when tripped + verdict footer); verdict semantic 3 cases: alerts not evaluated → no verdict (query surface ≠ gate ADR-0308 precedent), alerts evaluated none tripped → `:white_check_mark: **All threshold alerts passed.**`, alerts tripped → `:x: **N threshold alert(s) tripped** — exit 3 (CI gate failed)`; --watch + --format gh-summary rejected via existing parseWatchFlags format-validation (only allows human|json under --watch) — defensive M4.15.z preflight check removed during patch since parseWatchFlags runs first with broader message; null actual rendered as `\`null\` _(never set)_`; pipe-escape on cell content (defensive against operator-data containing `|`); right-aligned numeric cols via GitHub-flavored Markdown `|-------:|` alignment syntax (doesn't parse in CommonMark; acceptable since gh-summary targets GitHub specifically); rejected — gh-summary under --watch streaming Markdown (duplicate headers + concat-broken file; one-shot contract), verdict line when alerts not evaluated "run --threshold-alert to gate" (confuses ad-hoc inspection vs CI usage; ADR-0308 precedent), add "Alert" column to per-table grid showing trip status (table wide; clause+actual detail is the actionable info not boolean), <details> collapse for tables > 3 (only 3 tables; defer), render compound alerts as multiple rows one per tripped clause (inflates row count loses alert → spec mapping; <br>-joined preserves), spec cell yes/no instead of raw threshold expression (operators triaging need threshold), match human-format alert message exactly "trips threshold '<spec>'" (too wordy for table cell), defer threshold-alert Markdown helpers to tenant-housekeeping milestone (helpers surface-agnostic; export now means tenant surface reuses with one import); drawbacks — <br>-joined compound cells render inconsistently across Markdown parsers (GitHub supports; some CLI Markdown renderers strip; acceptable since gh-summary targets GitHub), right-aligned numeric cols require GitHub-flavored syntax not CommonMark (same constraint), no test for --tenant filter + gh-summary composition (covered implicitly), --watch incompatibility error from parseWatchFlags generic message not gh-summary-specific (test accepts either phrasing; future cleanup could add gh-summary preflight), empty alerts-evaluated zero-trip shows bare :white_check_mark: with no table (intentional but operators expecting table might be surprised); 10 new tests across 2 describe blocks (4 gateway housekeeping --format gh-summary integration: ## header + per-table grid + locale-formatted thousands + _TTL-managed_ retention cell + no verdict without alerts, :white_check_mark: when alerts evaluated none tripped, :x: + tripped-alerts table when alerts trip exit 3, --watch + gh-summary rejected exit 2 with explanatory error; 6 threshold-alert helpers unit tests: formatTrippedAlertGhSummaryRow single-clause numeric/timestamp-staleness/null actual/compound-with-br-joins, formatTrippedAlertsGhSummaryTable empty-input-empty-string + header+rows); gateway.test.ts grows 89 → 99 tests (+10); workspace test count 10,055 → 10,065 (+10); useful for CI step summary integration on gateway housekeeping cron jobs + at-a-glance verdict + threshold-tracked dashboard auditing + reusable threshold-alert Markdown helpers for tenant housekeeping future surface; M4.15.z |
| 190 | 0312 | 1,400 | Drafted alongside apply.ts coverage maintenance pass — adds ApplyContext interface extending RunContext with two test-only injection points (`pgConnectionOverride?: PgConnection` for skipping createNodePgConnection + `applierOverride?: { apply(): Promise<ApplyReport> }` for replacing MigrationApplier entirely with stub that returns manufactured ApplyReport); exports PackValidationError class so it can be directly unit-tested; when applierOverride is set PG env validation skipped (matches mental model: operators testing apply-report rendering don't have PG env vars set + don't want production-DB check getting in the way); when applierOverride NOT set production flow unchanged (env-validation + production-DB check fire normally); pgConnectionOverride still goes through production-DB check unlike applierOverride (only swaps underlying connection); 13 new tests covering 10 applierOverride scenarios (clean apply human format / clean apply --pack / --format json envelope with pack metadata / --format json no pack reports pack: null / --format gh-summary via live path / exit 1 on report.failed > 0 / exit 1 on preconditions.ok false / exit 1 on applier.apply throw Error + non-Error fallback / --format gh-summary precondition failure Apply blocked verdict) + 1 pgConnectionOverride real MigrationApplier construction test with throw-on-lock fake conn verifying conn.close fires in finally + 3 PackValidationError class tests (name + kind + summarized message / instanceof Error / handles empty errors list); apply.ts coverage delta: statements 74.58% → 92.24% (+17.66pp), branches 89.28% → 93.75% (+4.47pp), functions 85.71% → 100% (+14.29pp), lines 74.58% → 92.24% (+17.66pp); apply.test.ts grows 20 → 34 tests (+14); workspace test count 10,041 → 10,055 (+14); remaining 7.76% uncovered = error handlers for ExtendsCycleError / UnknownParentManifestError / PackValidationError in catch block (lines 64-80) + throw site inside buildPlan (lines 183-184) — both require pack-registry mocking that would need invasive changes to module-level pack constant without injection points (acceptable trade-off; 92.24% is highest apply.ts has ever been + these are defensive error handlers unlikely to regress silently); rejected — mock pack registry with vi.mock (brittle couples tests to internals), add test-only invalid pack to prod registry (pollutes production listings), make buildPlan accept pack-resolver function (adds injection point for limited benefit), use vitest.spyOn on resolvePack (ESM intricacies), mark uncovered handlers with c8 ignore pragma to force 100% (hides legitimate gaps; accept 92.24% honestly), use applierFactory instead of applierOverride (factory adds indirection; override matches pgConnectionOverride precedent), co-locate tests with M4.15.w block (maintenance vs feature; separate describe makes intent clear); drawbacks — applierOverride bypasses production-DB check (test-only by convention; named in TSDoc as test-only), 3 injection points on ApplyContext (env via RunContext inheritance + pgConnectionOverride + applierOverride; slight cognitive load but matches established test-injection patterns elsewhere); useful for raising apply.ts above 80% threshold + enabling future apply features without test-fixture-construction friction; M4.15.y / M-maint.coverage |
| 189 | 0311 | 1,300 | Drafted alongside `gateway prune-idempotency --format gh-summary` Markdown rendering — extends M4.15.v/w gh-summary pattern to the periodic-maintenance gateway action that DELETEs expired rows from meta.gateway_idempotency_records; shared formatPruneIdempotencyGhSummary renderer handles both dry-run + actual-delete paths with title suffix `(dry-run)` distinguishing the dry-run mode + count label "Would delete" vs "Deleted"; dry-run has no verdict emoji (operator hasn't decided yet; informational) + italic footer `_Dry-run: no records deleted. Re-run without --dry-run to delete._` nudging toward follow-up command; actual-delete branches by count: count > 0 → `:white_check_mark: **Prune succeeded** — N record(s) deleted`, count === 0 → `:white_check_mark: **Nothing to prune** — 0 records matched the scope` (both success verdicts since prune is operationally idempotent — 0 deleted is the same outcome as N from gate perspective; distinct phrasing avoids "Prune succeeded — 0 records deleted" misinterpretation as "scope filter swallowed everything"); scope line `**Scope:** operationId=\`<id>\` | method=\`<method>\` | limit=<N>` emitted only when at least one scope field is set (omitting on no-scope case keeps header tight; common bare-call gets 3-line header instead of 4); scope field order operationId/method/limit matches flag declaration order in help text; identifier values wrapped in backticks except limit which is numeric; rejected — include `--limit` warning when count == limit-value "hit the limit re-run for more" (useful but requires pre-limit count; operators use --dry-run), use :hourglass: for dry-run (would signal "still waiting" but dry-run is not pending it's informational), emit :warning: verdict when count > 1000 "large prune" (large prunes aren't error conditions for idempotency table designed to age out; operators wanting threshold gate should pipe through own check), show per-operation/per-method breakdown when no scope set (would require GROUP BY query not currently issued; defer), use :tada: for Nothing-to-prune verdict (too celebratory for noop; stick with :white_check_mark:), match human-format phrasing exactly "deleted N expired idempotency record(s)" (too verbose for gh-summary verdict; distilled to "Prune succeeded — N record(s) deleted."), skip asOf timestamp in header (would make gh-summary non-reproducible in audit trails); drawbacks — two success verdicts ("Prune succeeded" vs "Nothing to prune") means CI scripts grepping for single string need to handle both cases (accept the duality; distinct phrasing worth it for readability), dry-run with --limit showing preview count may underestimate real population (operators often set --limit for batch safety in actual apply not dry-run preview; inherited from M4.12 baseline behavior), scope line with only `limit` set looks oddly isolated (acceptable; operators using bare --limit understand semantic), no cohort filters beyond the 3 existing scope flags (out of scope; prune-idempotency isn't a list surface); 9 new tests covering --dry-run gh-summary title + Would delete + informational footer + no verdict, actual-delete success verdict with Deleted count, 0-deletes Nothing-to-prune verdict, scope line with --operation-id + --method + --limit set, direct formatPruneIdempotencyGhSummary unit tests for dry-run/success/nothing-to-prune/scope-line-conditional/scope-field-ordering; gateway.test.ts grows 80 → 89 tests (+9); workspace test count 10,032 → 10,041 (+9); coverage gate passes — gateway.ts at 81.91% statements / 79.1% branches / 87.5% functions / 81.91% lines (uptick from M4.15.w baseline 79.69% statements via gh-summary format branches in both dry-run + actual-delete paths + new exported formatter helper exercised by direct unit tests; all above 80/70/80/80 thresholds for first time on this file); useful for periodic-maintenance CI step summaries + at-a-glance prune verdict + scope-traceable audit; M4.15.x |
| 188 | 0310 | 1,500 | Drafted alongside `apply --format gh-summary` Markdown rendering — extends the M4.15.v workflow validate gh-summary pattern from a pre-publish validator to the foundational schema-migration command applying the kernel meta-schema + optional vertical pack DDL; covers both live + dry-run code paths with separate renderers (formatApplyReportGhSummary + formatApplyDryRunGhSummary) for distinct verdict semantics; live apply path branches by exit-code semantic: preconditions.ok=false renders `### Precondition problems (N)` table (Code | Message | Remedy) + `:x: **Apply blocked** — preconditions failed; no statements executed.` verdict (per-statement table doesn't appear since nothing ran), failed > 0 renders `### Failed statements (N)` table (Hash | Excerpt | Error) + `:x: **Apply halted at statement X/N**` verdict if haltedAt set (1-indexed matching operator mental model "how many applied") or `:x: **Apply completed with errors**` if no halt, clean apply emits `:white_check_mark: **Apply succeeded** — N executed, M skipped.`; successful statements OMITTED from failed-table (50+ rows of "succeeded: yes" pure noise; failure triage only cares about what broke); dry-run path is informational with no verdict (dry-run can't verify preconditions or detect collisions so any verdict would lie) — italic note `_Dry-run: no statements executed. Re-run without --dry-run and with --confirm to apply._` nudges operators toward correct follow-up; header reflects pack mode `## Apply: meta schema` no pack vs `## Apply: meta schema + pack \`<slug>\`` with pack (self-documenting without empty fields); failed-statement Hash column truncated to 8 chars git-convention short-hash (full hash in --format json for audit pivot); pipe-escape on excerpt + error message cells (defensive against operator-data containing `|`); errorMessage null → `(no error message)` fallback; remedy cell empty when null (matches PreconditionProblem shape); rejected — show all statements including succeeded (50+ rows pure noise), full 16-char hash (too wide column), per-statement duration column (perf triage future Q; available in JSON), precondition verdict on success "All N preconditions pass" (noisy for common case; absence is signal), `:fire:` for halted verdict (stuck with M4.15.e convention), collapse precondition table when > 5 (precondition problems rare typically 1-2), dry-run verdict "plan looks clean" (can't verify; would lie), use :white_check_mark: for dry-run to signal "plan parsed successfully" (operators would read as "apply succeeded"; reserve success verdict for actual apply); drawbacks — successful statement detail lost in gh-summary mode (operators use --format json), hash 8-char truncation has 2^32 collision risk (acceptable for triage scoping operators pivot via JSON), dry-run no verdict surprises CI pipelines expecting pass/fail (they should use --format json for programmatic consumption), PreconditionProblem null-remedy cell looks identical to "no remedy available" (acceptable consistency — pipe-escape literal-null marker would treat null + empty-string same anyway), 2 new exported helpers needed for independent unit testing + potential future surfaces (`apply --report <run-id>` if history becomes a thing); 11 new tests covering dry-run Markdown with planned-statement counts + informational footer + no verdict emoji + --pack header + Pack: line, formatApplyReportGhSummary success/precondition-failure/failed-statements-halted/successful-omitted/null-error-fallback/pipe-escape; apply.test.ts grows 9 → 20 tests (+11); workspace test count 10,021 → 10,032 (+11); coverage gate passes — apply.ts at 74.58% statements / 89.28% branches / 85.71% functions / 74.58% lines (uptick from M4.15.v baseline 63.38% statements via new gh-summary format branches in both runApply live path + emitDryRun + 2 new exported renderer helpers exercised by direct unit tests; still below the prior baseline of 80% but acceptable since the uncovered lines are PG-connection-dependent live-apply error paths that require fakePg infra not yet built); useful for CI step summary integration on schema-migration runs + at-a-glance verdict signaling + precondition-failure triage + failed-statement audit trail; M4.15.w |
| 187 | 0309 | 1,400 | Drafted alongside `workflow validate --format gh-summary` Markdown rendering for CI step output — extends the M4.15.t pivot from list-style surfaces (sessions, gateway routes) to a CI-oriented pre-publish validator surface; renders 4-col Markdown table (Severity / Code / Path / Message) plus verdict footer matching the exit-code semantic: clean definition → `:white_check_mark: **Definition is valid** — no issues found`, errors > 0 → `:x: **Validation failed** — N error(s) block publish` (exit 3), --strict + warnings > 0 → `:warning: **Strict validation failed** — N warning(s) under --strict` (exit 3 promoted), warnings > 0 without --strict → `:warning: **Validation passed with warnings** — N warning(s) (non-blocking)` (exit 0); errors-first row ordering so blocking issues surface at top of table when operators scan top-down in CI; schema-error path (pre-validateDefinition Zod parse failure) gets same gh-summary shape with definitionId/Key marked `(schema-rejected)` since ids couldn't be read + always :x: verdict (schema errors unconditionally blocking); severity-cell uses emoji-prefixed text labels `:x: error` / `:warning: warning` not just emoji (accessibility: scannable even when emoji rendering fails); message-cell pipe escape future-proofs against any message containing `|` (current validateDefinition messages are pipe-free); new ValidationIssueLike interface (severity/code/path/message) decouples renderer from WorkflowValidationIssue type so schema-error path's synthetic `schema_error` code (outside WorkflowValidationCode literal union) works without fabricating fake validation code; rejected — show only errors in table warnings in summary line below (operators triaging want warning detail in context), group issues by code in collapsible <details> blocks (over-engineered for typical 1-5 issue case), skip verdict footer on clean definitions just header suffices (operators want explicit confirmation), emit :warning: for schema-error path instead of :x: (schema errors structural parse failures no different from validation errors from gate semantic), drop message-cell pipe escape (future-proof against messages containing `|`), use :bug: / :exclamation: alternates for verdict emoji (stuck with M4.15.e :white_check_mark: / :warning: / :x: convention used across the diff family), emit GitHub workflow command annotations `::error file=def.json::message` for in-line PR markers (different feature; gh-summary targets step summary not PR diff margin; could be future Q --format github-annotations), include raw definition excerpt per issue (makes table wide; defer); drawbacks — gh-summary table width fixed at 4 cols long message text wraps in message cell (acceptable CI step summaries full-width), `(schema-rejected)` marker is English-text literal no i18n story yet, verdict for warnings-only + --strict uses :warning: not :x: intentionally to distinguish "strict gate caught this" from "actual error" but operators scanning by emoji might miss that :warning: with exit 3 is blocking (verdict text "Strict validation failed" carries signal), ValidationIssueLike new exported interface small surface expansion acceptable for type-decoupling enabled, no --columns or --no-header support (those apply to CSV surfaces; gh-summary is fixed-shape Markdown); 8 new tests covering clean-definition success verdict + unreachable-state warning emits passed-with-warnings verdict (exit 0), --strict promotes warnings to strict-failure verdict (exit 3), schema-error path emits gh-summary with (schema-rejected) markers, direct formatValidateGhSummary warnings-without-strict non-blocking verdict, direct warnings-with-strict strict-failure verdict, direct mixed-severity errors-first ordering verified (error row position < both warning positions), direct pipe-character escape in message; workflow.test.ts grows 13 → 21 tests (+8); workspace test count 10,013 → 10,021 (+8); coverage gate passes — workflow.ts at 100% statements / 95.31% branches / 100% functions / 100% lines (uptick from M4.15.u baseline via new formatValidateGhSummary helper + gh-summary format branch in both runWorkflowValidate + renderSchemaErrors); useful for CI step summary integration on workflow pre-publish gates + at-a-glance verdict signaling + errors-first triage; M4.15.v |
